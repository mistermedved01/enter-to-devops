[🏠 Главная](../../README.md) → [🐧 Linux](../../README.md#-linux) → [⚙️ L-03-Управление-процессами](../../README.md#-l-03-управление-процессами)

---

# ⚙️L-03-2-Сигналы-Signals

**Сигналы** — механизм взаимодействия между процессами и ядром, используются для уведомления процессов о событиях. Каждый сигнал имеет уникальный номер и имя, начинающееся с `SIG`.

### Основные сигналы:

```Shell
# Грациозное управление
SIGTERM (15) - запрос на завершение (по умолчанию для kill)
SIGHUP (1)   - перезагрузка конфигурации
SIGINT (2)   - прерывание с клавиатуры (Ctrl+C)

# Принудительное управление
SIGKILL (9)  - безусловное завершение (нельзя перехватить)
SIGSTOP (19) - приостановка процесса (нельзя перехватить)
SIGCONT (18) - возобновление приостановленного процесса

# Специальные сигналы
SIGCHLD (17) - уведомление о завершении дочернего процесса
SIGSEGV (11) - ошибка сегментации (access violation)
```

---

### **Команды для работы с сигналами:**

```Shell
# Отправка сигналов
kill -TERM <PID>                  # Грациозное завершение
kill -KILL <PID>                  # Принудительное завершение
pkill -f "pattern"                # Сигнал процессам по шаблону
killall process_name              # Сигнал всем процессам с именем

# Просмотр обрабатываемых сигналов
kill -l                          # Список всех сигналов
cat /proc/<PID>/status | grep SigCgt  # Маска обрабатываемых сигналов
```

---

### **Практические примеры:**

```Shell
# Грациозное завершение веб-сервера
kill -TERM $(pgrep nginx)
systemctl stop nginx

# Принудительное завершение зависшего процесса
kill -KILL 1234

# Перезагрузка конфигурации
kill -HUP $(pgrep nginx)
systemctl reload nginx

# Приостановка и возобновление процесса
kill -STOP 1234                  # Приостановить
kill -CONT 1234                  # Возобновить

# Завершение всех процессов пользователя
pkill -u username
```

---

### **Обработка сигналов в скриптах:**

```Shell
#!/bin/bash

# Функция для обработки сигналов
cleanup() {
    echo "Получен сигнал завершения, очищаем ресурсы..."
    # Здесь код очистки
    exit 0
}

# Регистрация обработчиков
trap cleanup SIGTERM SIGINT SIGQUIT

# Основной код скрипта
while true; do
    echo "Работаем..."
    sleep 1
done
```

---

✍️**Вопросы**

1. Что такое сигналы в Linux?

2. В чем разница между SIGTERM и SIGKILL?

3. Какие сигналы нельзя перехватить?

4. Как отправить сигнал процессу?

5. Как завершить все процессы с определенным именем?

6. Что происходит при получении SIGSTOP?

7. Как перезагрузить конфигурацию демона?

8. Как обработать сигналы в bash-скрипте?

9. Что такое зомби-процесс и как с ним бороться?

10. Как посмотреть, какие сигналы обрабатывает процесс?
