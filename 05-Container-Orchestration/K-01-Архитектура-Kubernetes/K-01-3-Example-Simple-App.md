[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [📚 K-01-Архитектура-Kubernetes](../../README.md#-k-01-архитектура-kubernetes)

---

# 🚀K-01-3-Example-Simple-App
>Практический пример развертывания приложения в Kubernetes: полный стек компонентов, который используется в production (Deployment, Service, Ingress, ConfigMap, Secret, HPA)

---

<details>
<summary><b>🎯Обзор: что нужно для production приложения</b></summary>

---

## Базовый "скелет" приложения в Kubernetes

**В проде почти всегда есть следующие компоненты:**

1. ✅ **Deployment** — управляет жизнью Pod'ов
2. ✅ **Pod** — контейнер с приложением (создается через Deployment)
3. ✅ **Service** — стабильная точка доступа и балансировщик
4. ✅ **Ingress** — внешний вход в приложение
5. ✅ **Ingress Controller** — тот, кто реально роутит трафик
6. ✅ **ConfigMap** — настройки приложения
7. ✅ **Secret** — приватные данные (пароли, токены, TLS)
8. ✅ **Namespace** — изоляция окружений (dev, stage, prod)
9. ✅ **HPA** (по желанию, но типично) — автоувеличение подов по нагрузке

**Это базовый "скелет" любого продового приложения. Даже самый простой сайт в k8s обычно включает эти сущности.**

---

## Поток трафика в production

```
Пользователь
  └→ Ingress Controller (nginx/traefik)
        └→ Ingress rule (mysite.com)
              └→ Service (ClusterIP)
                    └→ Pod'ы внутри Deployment (копии nginx)
```

**В стороне живут:**
- ConfigMap — настройки
- Secret — пароли и TLS
- HPA — автоскейлинг
- Namespace — изоляция

---

</details>

<details>
<summary><b>1️⃣ Deployment — управляет жизнью Pod'ов</b></summary>

---

## Что такое Deployment?

**Deployment запускает нужное число Pod'ов и следит, чтобы они не упали.**

**В проде без него вообще никто не живёт.**

**Что даёт:**
- ✅ Поднятие копий (replicas)
- ✅ Обновления (rolling updates)
- ✅ Откаты (rollbacks)
- ✅ Самовосстановление (self-healing)

---

## Пример Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: production
  labels:
    app: web-app
spec:
  replicas: 3  # Количество копий
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.21
        ports:
        - containerPort: 80
        env:
        - name: ENV
          valueFrom:
            configMapKeyRef:
              name: web-config
              key: environment
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: web-secrets
              key: api-key
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
```

**Ключевые моменты:**
- `replicas: 3` — запускает 3 копии приложения
- `selector.matchLabels` — выбирает Pod'ы по меткам
- `template` — шаблон для создания Pod'ов
- Использует ConfigMap и Secret для конфигурации

---

</details>

<details>
<summary><b>2️⃣ Pod — контейнер с приложением</b></summary>

---

## Что такое Pod?

**Pod — это твой контейнер с сайтом.**

**Сам по себе ты Pod почти не создаёшь, он появляется через Deployment.**

**Что даёт:**
- ✅ Среда, где реально работает nginx (или другое приложение)
- ✅ Изоляция контейнеров
- ✅ Общие ресурсы (volumes, network)

---

## Pod создается автоматически

**Pod создается автоматически Deployment'ом на основе template:**

```yaml
# Это часть Deployment (template)
spec:
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.21
        ports:
        - containerPort: 80
```

**После создания Deployment, Pod'ы появятся автоматически:**

```bash
kubectl get pods -n production
```

**Вывод:**

```
NAME                      READY   STATUS    RESTARTS   AGE
web-app-7d4b8c9f-abc      1/1     Running   0          2m
web-app-7d4b8c9f-def      1/1     Running   0          2m
web-app-7d4b8c9f-ghi      1/1     Running   0          2m
```

---

## Почему не создавать Pod напрямую?

**Pod сам по себе:**
- ❌ Не восстанавливается при падении
- ❌ Не масштабируется
- ❌ Не обновляется автоматически

**Deployment делает всё это автоматически.**

---

</details>

<details>
<summary><b>3️⃣ Service — стабильная точка доступа</b></summary>

---

## Что такое Service?

**У Pod'ов IP плавающие (динамические).**

**Service — это стабильная точка входа к ним.**

**Он балансирует трафик между копиями.**

**В проде обычно ClusterIP, а наружу выходит через Ingress.**

---

## Пример Service (ClusterIP)

```yaml
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: production
  labels:
    app: web-app
spec:
  type: ClusterIP  # Внутренний сервис
  selector:
    app: web-app  # Выбирает Pod'ы с этой меткой
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
```

**Ключевые моменты:**
- `type: ClusterIP` — доступен только внутри кластера
- `selector.app: web-app` — находит все Pod'ы с меткой `app: web-app`
- `port: 80` — порт Service'а
- `targetPort: 80` — порт контейнера

---

## Как работает Service?

```
┌─────────────────────────────────────────────────────────┐
│              Service балансирует трафик                  │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Service (web-service:80)                               │
│           │                                              │
│           ├─→ Pod 1 (10.244.1.5:80)                     │
│           ├─→ Pod 2 (10.244.1.6:80)                     │
│           └─→ Pod 3 (10.244.1.7:80)                     │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Service автоматически распределяет запросы между Pod'ами.**

---

</details>

<details>
<summary><b>4️⃣ Ingress — внешняя дверь в приложение</b></summary>

---

## Что такое Ingress?

**Ingress — это внешняя дверь в приложение.**

**Ingress говорит:**
- Если пришёл запрос на `mysite.com` — отправь его в Service `web-service`
- Если пришёл запрос на `api.mysite.com` — отправь его в Service `api-service`

**В проде без этого никак.**

---

## Пример Ingress

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
  namespace: production
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - mysite.com
    - www.mysite.com
    secretName: mysite-tls
  rules:
  - host: mysite.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 80
  - host: www.mysite.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 80
```

**Ключевые моменты:**
- `host: mysite.com` — домен для входящих запросов
- `backend.service.name: web-service` — куда направлять трафик
- `tls` — настройка HTTPS (TLS сертификаты)
- `ingressClassName: nginx` — какой Ingress Controller использовать

---

</details>

<details>
<summary><b>5️⃣ Ingress Controller — роутинг трафика</b></summary>

---

## Что такое Ingress Controller?

**Сам Ingress не умеет ничего делать.**

**Ingress Controller — это nginx или traefik, который:**
- ✅ Читает правила Ingress
- ✅ Обрабатывает трафик
- ✅ Роутит запросы к нужным Service'ам

**Это прям обязательная штука в любом проде.**

---

## Развертывание Ingress Controller

**Пример развертывания NGINX Ingress Controller:**

```bash
kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.8.1/deploy/static/provider/cloud/deploy.yaml
```

**Или через Helm:**

```bash
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm install ingress-nginx ingress-nginx/ingress-nginx
```

---

## Как работает Ingress Controller?

```
┌─────────────────────────────────────────────────────────┐
│              Поток трафика через Ingress                │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Пользователь                                            │
│       │                                                   │
│       ▼                                                   │
│  Ingress Controller (nginx/traefik)                      │
│       │                                                   │
│       ├─ Читает правила Ingress                          │
│       │                                                   │
│       ▼                                                   │
│  Ingress rule (mysite.com → web-service)                │
│       │                                                   │
│       ▼                                                   │
│  Service (ClusterIP)                                      │
│       │                                                   │
│       ▼                                                   │
│  Pod'ы внутри Deployment                                 │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Популярные Ingress Controller'ы

- ✅ **NGINX Ingress Controller** — самый популярный
- ✅ **Traefik** — простой и современный
- ✅ **HAProxy** — для высокой нагрузки
- ✅ **Istio Gateway** — для service mesh

---

</details>

<details>
<summary><b>6️⃣ ConfigMap — настройки приложения</b></summary>

---

## Что такое ConfigMap?

**ConfigMap хранит настройку:**
- Порты
- Переменные окружения
- Какие-то пути
- Конфигурационные файлы

**Обычно фронту много не нужно, но в проде ConfigMap всё равно почти всегда присутствует.**

---

## Пример ConfigMap

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: web-config
  namespace: production
data:
  environment: "production"
  log_level: "info"
  api_url: "https://api.mysite.com"
  nginx.conf: |
    server {
        listen 80;
        server_name mysite.com;
        root /usr/share/nginx/html;
        index index.html;
    }
```

**Использование в Deployment:**

```yaml
spec:
  template:
    spec:
      containers:
      - name: nginx
        env:
        - name: ENV
          valueFrom:
            configMapKeyRef:
              name: web-config
              key: environment
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: web-config
              key: log_level
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
      volumes:
      - name: nginx-config
        configMap:
          name: web-config
```

---

## Преимущества ConfigMap

- ✅ Отделение конфигурации от кода
- ✅ Легко обновлять без пересборки образа
- ✅ Можно использовать в разных окружениях
- ✅ Версионирование через Git

---

</details>

<details>
<summary><b>7️⃣ Secret — приватные данные</b></summary>

---

## Что такое Secret?

**Secret хранит:**
- Пароли к внешним сервисам
- Токены
- Сертификаты TLS
- API ключи

**В проде без секретов никак.**

---

## Пример Secret

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: web-secrets
  namespace: production
type: Opaque
data:
  api-key: YWJjMTIzNDU2Nzg5MA==  # base64 encoded
  db-password: cGFzc3dvcmQxMjM=  # base64 encoded
  tls.crt: LS0tLS1CRUdJTi...     # TLS certificate (base64)
  tls.key: LS0tLS1CRUdJTi...     # TLS private key (base64)
```

**Создание Secret через kubectl:**

```bash
kubectl create secret generic web-secrets \
  --from-literal=api-key=abc1234567890 \
  --from-literal=db-password=password123 \
  --namespace=production
```

**Использование в Deployment:**

```yaml
spec:
  template:
    spec:
      containers:
      - name: nginx
        env:
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: web-secrets
              key: api-key
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: web-secrets
              key: db-password
```

---

## Безопасность Secret

**Важно:**
- ⚠️ Secret хранятся в base64 (не шифрование!)
- ⚠️ В production используй внешние системы (Vault, Sealed Secrets)
- ⚠️ Не коммить Secret'ы в Git
- ✅ Используй RBAC для ограничения доступа

---

</details>

<details>
<summary><b>8️⃣ Namespace — изоляция окружений</b></summary>

---

## Что такое Namespace?

**Namespace нужен, чтобы не смешивать окружения:**
- `dev` — разработка
- `stage` — тестирование
- `prod` или `production` — продакшн

---

## Пример Namespace

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    environment: production
    team: backend
```

**Создание через kubectl:**

```bash
kubectl create namespace production
```

**Использование в манифестах:**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: production  # Указываем namespace
  ...
```

---

## Преимущества Namespace

- ✅ Изоляция ресурсов
- ✅ Разделение прав доступа (RBAC)
- ✅ Удобное управление (dev/stage/prod)
- ✅ Лимиты ресурсов (ResourceQuota)

---

</details>

<details>
<summary><b>9️⃣ HPA — автоскейлинг (по желанию, но типично)</b></summary>

---

## Что такое HPA?

**HorizontalPodAutoscaler — автоувеличение и уменьшение Pod'ов по нагрузке.**

**В проде это почти стандарт.**

---

## Пример HPA

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-app-hpa
  namespace: production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web-app
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
      - type: Percent
        value: 100
        periodSeconds: 60
```

**Ключевые моменты:**
- `minReplicas: 2` — минимум 2 Pod'а
- `maxReplicas: 10` — максимум 10 Pod'ов
- `averageUtilization: 70` — масштабировать при 70% CPU
- `scaleTargetRef` — какой Deployment масштабировать

---

## Как работает HPA?

```
┌─────────────────────────────────────────────────────────┐
│              Автоматическое масштабирование              │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Нагрузка увеличивается                                 │
│           │                                              │
│           ▼                                              │
│  HPA видит: CPU > 70%                                   │
│           │                                              │
│           ▼                                              │
│  Увеличивает количество Pod'ов (2 → 4 → 6)              │
│           │                                              │
│           ▼                                              │
│  Нагрузка уменьшается                                   │
│           │                                              │
│           ▼                                              │
│  HPA видит: CPU < 70%                                   │
│           │                                              │
│           ▼                                              │
│  Уменьшает количество Pod'ов (6 → 4 → 2)               │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

</details>

<details>
<summary><b>🔗Полная картина: как всё работает вместе</b></summary>

---

## Полный стек для production

**Берём домен `mysite.com`.**

**В проде картинка выглядит примерно так:**

```
Пользователь
  └→ Ingress Controller (nginx/traefik)
        └→ Ingress rule (mysite.com)
              └→ Service (ClusterIP)
                    └→ Pod'ы внутри Deployment (копии nginx)
```

**В стороне живут:**
- ConfigMap — настройки
- Secret — пароли и TLS
- HPA — автоскейлинг
- Namespace — изоляция

---

## Схема полного стека

```
┌─────────────────────────────────────────────────────────┐
│              Полная архитектура приложения              │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────┐          │
│  │  Пользователь (mysite.com)                │          │
│  └──────────────┬───────────────────────────┘          │
│                 │                                        │
│                 ▼                                        │
│  ┌──────────────────────────────────────────┐          │
│  │  Ingress Controller (nginx)               │          │
│  │  - Читает правила Ingress                 │          │
│  │  - Роутит трафик                          │          │
│  └──────────────┬───────────────────────────┘          │
│                 │                                        │
│                 ▼                                        │
│  ┌──────────────────────────────────────────┐          │
│  │  Ingress (mysite.com → web-service)       │          │
│  │  - Правила маршрутизации                  │          │
│  │  - TLS сертификаты                        │          │
│  └──────────────┬───────────────────────────┘          │
│                 │                                        │
│                 ▼                                        │
│  ┌──────────────────────────────────────────┐          │
│  │  Service (ClusterIP: web-service)          │          │
│  │  - Стабильный IP                          │          │
│  │  - Балансировка нагрузки                  │          │
│  └──────────────┬───────────────────────────┘          │
│                 │                                        │
│                 ├─→ Pod 1 (nginx)                       │
│                 ├─→ Pod 2 (nginx)                       │
│                 └─→ Pod 3 (nginx)                       │
│                                                          │
│  ┌──────────────────────────────────────────┐          │
│  │  Deployment (web-app)                     │          │
│  │  - Управляет Pod'ами                      │          │
│  │  - Самовосстановление                     │          │
│  │  - Rolling updates                        │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  ┌──────────────────────────────────────────┐          │
│  │  ConfigMap (web-config)                    │          │
│  │  - Настройки приложения                    │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  ┌──────────────────────────────────────────┐          │
│  │  Secret (web-secrets)                     │          │
│  │  - Пароли, токены, TLS                    │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  ┌──────────────────────────────────────────┐          │
│  │  HPA (web-app-hpa)                        │          │
│  │  - Автоматическое масштабирование         │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
│  ┌──────────────────────────────────────────┐          │
│  │  Namespace (production)                   │          │
│  │  - Изоляция окружения                     │          │
│  └──────────────────────────────────────────┘          │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

</details>

<details>
<summary><b>📝Практический пример: развертывание</b></summary>

---

## Шаг 1: Создание Namespace

```bash
kubectl create namespace production
```

---

## Шаг 2: Создание ConfigMap

```bash
kubectl apply -f - <<EOF
apiVersion: v1
kind: ConfigMap
metadata:
  name: web-config
  namespace: production
data:
  environment: "production"
  log_level: "info"
EOF
```

---

## Шаг 3: Создание Secret

```bash
kubectl create secret generic web-secrets \
  --from-literal=api-key=abc1234567890 \
  --namespace=production
```

---

## Шаг 4: Создание Deployment

```bash
kubectl apply -f - <<EOF
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.21
        ports:
        - containerPort: 80
        env:
        - name: ENV
          valueFrom:
            configMapKeyRef:
              name: web-config
              key: environment
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: web-secrets
              key: api-key
EOF
```

---

## Шаг 5: Создание Service

```bash
kubectl apply -f - <<EOF
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: web-app
  ports:
  - port: 80
    targetPort: 80
EOF
```

---

## Шаг 6: Создание Ingress

```bash
kubectl apply -f - <<EOF
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
  namespace: production
spec:
  ingressClassName: nginx
  rules:
  - host: mysite.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 80
EOF
```

---

## Шаг 7: Создание HPA (опционально)

```bash
kubectl apply -f - <<EOF
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-app-hpa
  namespace: production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web-app
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
EOF
```

---

## Проверка развертывания

```bash
# Проверка Pod'ов
kubectl get pods -n production

# Проверка Service
kubectl get svc -n production

# Проверка Ingress
kubectl get ingress -n production

# Проверка HPA
kubectl get hpa -n production

# Проверка Deployment
kubectl get deployment -n production
```

---

</details>

<details>
<summary><b>📚Короткая памятка</b></summary>

---

## Что нужно запомнить

✅ **Deployment** — управляет жизнью Pod'ов
- Поднятие копий, обновления, откаты, самовосстановление

✅ **Pod** — контейнер внутри k8s
- Сам по себе Pod почти не создаёшь, он появляется через Deployment

✅ **Service** — стабильная точка доступа и балансировщик
- У Pod'ов IP плавающие, Service даёт стабильный доступ

✅ **Ingress** — внешний вход
- Говорит: если пришёл запрос на mysite.com → отправь в Service web

✅ **Ingress Controller** — тот, кто реально роутит трафик
- nginx или traefik, который читает правила Ingress

✅ **ConfigMap** — настройки
- Порты, переменные окружения, пути

✅ **Secret** — приватные данные
- Пароли, токены, сертификаты TLS

✅ **Namespace** — окружение
- dev, stage, prod — чтобы не смешивать

✅ **HPA** — автоувеличение Pod'ов
- HorizontalPodAutoscaler — по нагрузке (CPU, память)

---

## Поток трафика

```
Пользователь
  └→ Ingress Controller
        └→ Ingress rule
              └→ Service (ClusterIP)
                    └→ Pod'ы (Deployment)
```

---

</details>

---

## Резюме

✅ **Базовый стек production приложения в Kubernetes:**

1. **Deployment** — управляет Pod'ами (replicas, updates, rollbacks)
2. **Pod** — контейнер с приложением (создается автоматически)
3. **Service** — стабильная точка доступа (ClusterIP, балансировка)
4. **Ingress** — внешний вход (правила маршрутизации)
5. **Ingress Controller** — роутинг трафика (nginx/traefik)
6. **ConfigMap** — настройки приложения
7. **Secret** — приватные данные (пароли, токены, TLS)
8. **Namespace** — изоляция окружений (dev/stage/prod)
9. **HPA** — автоскейлинг по нагрузке (опционально, но типично)

✅ **Поток трафика:**
```
Пользователь → Ingress Controller → Ingress → Service → Pod'ы (Deployment)
```

✅ **В стороне:**
- ConfigMap — настройки
- Secret — пароли и TLS
- HPA — автоскейлинг
- Namespace — изоляция

✅ **Практика:**
- Всегда используй Deployment (не Pod напрямую)
- Service обязателен для доступа к Pod'ам
- Ingress + Ingress Controller для внешнего доступа
- ConfigMap и Secret для конфигурации
- Namespace для изоляции окружений
- HPA для автоматического масштабирования

> 💡 **Совет:** Это базовый "скелет" любого продового приложения. Даже самый простой сайт в k8s обычно включает эти сущности. Начни с Deployment + Service, затем добавь Ingress для внешнего доступа, ConfigMap/Secret для конфигурации, и HPA для автоматического масштабирования.

---

