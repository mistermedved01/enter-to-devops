[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔧 K-10-Техобслуживание](../../README.md#-k-10-техобслуживание)

---

# 🔧K-10-2-K8s-Software-Versions
>Версионирование Kubernetes: структура версий, типы релизов (stable, beta, alpha), управление версиями компонентов кластера

---

<details>
<summary><b>📋Введение в версионирование Kubernetes</b></summary>

---

Kubernetes использует семантическое версионирование для управления релизами. При установке кластера устанавливается определенная версия Kubernetes, которую можно проверить командой:

```bash
kubectl get nodes
```

Версия отображается в колонке `VERSION`, например: `v1.20.0`.

---

</details>

<details>
<summary><b>🔢Структура версий Kubernetes</b></summary>

---

## Формат версии

Версии Kubernetes следуют стандарту семантического версионирования (Semantic Versioning) и состоят из трех частей:

```
v<MAJOR>.<MINOR>.<PATCH>
```

### Компоненты версии

- **MAJOR (мажорная версия)** — первая часть номера версии
  - Изменяется при несовместимых изменениях API
  - Редко изменяется (первая версия 1.0 была выпущена в июле 2015 года)

- **MINOR (минорная версия)** — вторая часть номера версии
  - Изменяется при добавлении новых функций и возможностей
  - Выпускается каждые несколько месяцев
  - Обратно совместима с предыдущими минорными версиями

- **PATCH (версия исправлений)** — третья часть номера версии
  - Изменяется при исправлении критических ошибок и багов
  - Выпускается чаще, чем минорные версии
  - Обратно совместима

### Пример

```
v1.20.0
│ │  │
│ │  └─ PATCH: исправления ошибок
│ └──── MINOR: новые функции
└────── MAJOR: основная версия
```

---

</details>

<details>
<summary><b>📦Типы релизов Kubernetes</b></summary>

---

## Стабильные релизы (Stable Releases)

Стабильные релизы — это готовые к продакшену версии Kubernetes с полной поддержкой и обратной совместимостью.

### Характеристики

- Все функции протестированы и стабильны
- Обратная совместимость гарантирована
- Рекомендуются для продакшен-окружений
- Полная поддержка сообщества

### Пример

```
v1.20.0  ← стабильный релиз
```

## Бета-релизы (Beta Releases)

Бета-релизы содержат функции, которые находятся в стадии тестирования перед включением в стабильный релиз.

### Характеристики

- Код хорошо протестирован
- Новые функции включены по умолчанию
- API может изменяться в будущих версиях
- Не рекомендуется для продакшена

### Пример

```
v1.21.0-beta.1  ← бета-релиз
```

## Альфа-релизы (Alpha Releases)

Альфа-релизы содержат экспериментальные функции на ранней стадии разработки.

### Характеристики

- Новые функции по умолчанию отключены
- Могут содержать ошибки и нестабильность
- API может кардинально измениться
- Не рекомендуется для продакшена или тестирования

### Пример

```
v1.22.0-alpha.1  ← альфа-релиз
```

## Жизненный цикл релиза

```
┌─────────────────────────────────────────────────────┐
│  Alpha Release                                       │
│  - Функции отключены по умолчанию                   │
│  - Экспериментальный код                             │
│  └───────────────────────────────────────────────┐ │
│                                                    │ │
│  Beta Release                                      │ │
│  - Функции включены по умолчанию                   │ │
│  - Код протестирован                                │ │
│  └───────────────────────────────────────────────┐ │ │
│                                                   │ │ │
│  Stable Release                                   │ │ │
│  - Готов к продакшену                             │ │ │
│  - Полная поддержка                               │ │ │
│  └───────────────────────────────────────────────┘ │ │
└─────────────────────────────────────────────────────┘ │
                                                         │
┌────────────────────────────────────────────────────────┘
│  Релизы Kubernetes на GitHub
│  https://github.com/kubernetes/kubernetes/releases
└────────────────────────────────────────────────────────
```

---

</details>

<details>
<summary><b>📥Получение релизов Kubernetes</b></summary>

---

## Способы получения релизов

### 1. Прямая загрузка из GitHub

Релизы Kubernetes доступны на странице релизов репозитория:

```
https://github.com/kubernetes/kubernetes/releases
```

Можно скачать архив `kubernetes.tar.gz`, который содержит:

- Скомпилированные компоненты control plane
- Утилиты управления кластером
- Инструменты для настройки кластера
- Все компоненты одной версии

### 2. Скрипт установки

Упрощенный способ получения последнего релиза:

```bash
# Скачать скрипт установки
curl -sS https://get.k8s.io | bash
```

Скрипт автоматически:

- Определяет последнюю стабильную версию
- Загружает необходимые компоненты
- Настраивает окружение

### 3. Установка через пакетные менеджеры

#### Ubuntu/Debian

```bash
# Добавить репозиторий
curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
echo "deb https://apt.kubernetes.io/ kubernetes-xenial main" | sudo tee /etc/apt/sources.list.d/kubernetes.list

# Установить kubectl, kubelet, kubeadm
sudo apt-get update
sudo apt-get install -y kubelet kubeadm kubectl
```

#### CentOS/RHEL

```bash
# Добавить репозиторий
cat <<EOF | sudo tee /etc/yum.repos.d/kubernetes.repo
[kubernetes]
name=Kubernetes
baseurl=https://packages.cloud.google.com/yum/repos/kubernetes-el7-x86_64
enabled=1
gpgcheck=1
repo_gpgcheck=1
gpgkey=https://packages.cloud.google.com/yum/doc/yum-key.gpg https://packages.cloud.google.com/yum/doc/rpm-package-key.gpg
EOF

# Установить компоненты
sudo yum install -y kubelet kubeadm kubectl
```

---

</details>

<details>
<summary><b>🔍Проверка версий компонентов кластера</b></summary>

---

## Команды для проверки версий

### Версия узлов кластера

```bash
# Версии всех узлов
kubectl get nodes

# Детальная информация об узле
kubectl get node <node-name> -o yaml | grep kubeletVersion
```

### Версия kubectl

```bash
# Версия клиента kubectl
kubectl version --client

# Полная информация о версиях
kubectl version
```

### Версия компонентов control plane

```bash
# Версия API сервера
kubectl version --short

# Версия через API
kubectl get --raw /version
```

### Версия kubeadm (если используется)

```bash
kubectl version -o json | jq '.serverVersion'
```

---

</details>

<details>
<summary><b>🔗Версии зависимых компонентов</b></summary>

---

## Внешние зависимости

Control plane Kubernetes включает компоненты, которые не являются частью основного проекта Kubernetes и имеют собственные версии:

### etcd

etcd — распределенное хранилище ключ-значение для хранения данных кластера.

```bash
# Проверить версию etcd
kubectl get pods -n kube-system | grep etcd
kubectl describe pod <etcd-pod-name> -n kube-system | grep Image
```

### CoreDNS

CoreDNS — DNS-сервер для разрешения имен сервисов в кластере.

```bash
# Проверить версию CoreDNS
kubectl get deployment coredns -n kube-system -o jsonpath='{.spec.template.spec.containers[0].image}'
```

### Container Runtime

Container runtime (например, containerd, CRI-O) также имеет свою версию:

```bash
# Версия containerd
containerd --version

# Версия CRI-O
crio version
```

## Совместимость версий

> **📌 Важно:** Примечания к выпуску каждого релиза Kubernetes предоставляют информацию о поддерживаемых версиях внешних зависимостей, таких как:
>
> - etcd
> - CoreDNS
> - Container Runtime
> - CNI plugins
>
> Перед обновлением кластера необходимо проверить совместимость версий всех компонентов.

### Таблица совместимости

| Kubernetes | etcd | CoreDNS | containerd | CRI-O |
|------------|------|---------|------------|-------|
| 1.28.x     | 3.5.x| 1.10.x  | 1.7.x      | 1.28.x|
| 1.27.x     | 3.5.x| 1.10.x  | 1.6.x      | 1.27.x|
| 1.26.x     | 3.5.x| 1.9.x   | 1.6.x      | 1.26.x|

> **⚠️ Примечание:** Таблица приведена для примера. Актуальную информацию о совместимости версий см. в официальной документации Kubernetes.

---

</details>

<details>
<summary><b>📊Частота релизов Kubernetes</b></summary>

---

## График выпуска релизов

Kubernetes следует регулярному циклу выпуска релизов:

### Минорные релизы

- **Частота:** каждые 3-4 месяца
- **Содержание:** новые функции, улучшения, исправления
- **Поддержка:** ~12 месяцев для последних 3 минорных версий

### Патч-релизы

- **Частота:** по мере необходимости (обычно ежемесячно)
- **Содержание:** критические исправления безопасности и багов
- **Поддержка:** до следующего минорного релиза

### Пример графика

```
┌─────────────────────────────────────────────────────┐
│  Время                                              │
│                                                      │
│  v1.20.0 ──┐                                        │
│            │                                        │
│            ├─ v1.20.1 (патч)                       │
│            ├─ v1.20.2 (патч)                       │
│            │                                        │
│  v1.21.0 ──┼─┐                                      │
│            │ │                                      │
│            │ ├─ v1.21.1 (патч)                     │
│            │ │                                      │
│  v1.22.0 ──┼─┼─┐                                    │
│            │ │ │                                    │
│            │ │ └─ v1.22.1 (патч)                   │
│            │ │                                      │
│            │ └─ v1.21.2 (патч)                     │
│            │                                        │
│            └─ v1.20.3 (патч)                       │
└─────────────────────────────────────────────────────┘
```

---

</details>

<details>
<summary><b>🔐Политика поддержки версий</b></summary>

---

## Поддерживаемые версии

Kubernetes поддерживает последние **3 минорные версии**:

- Текущая стабильная версия
- Предыдущая минорная версия
- Предыдущая-предыдущая минорная версия

### Пример

Если текущая версия — `1.28.x`, то поддерживаются:

- `1.28.x` (текущая)
- `1.27.x` (предыдущая)
- `1.26.x` (предыдущая-предыдущая)

### Прекращение поддержки

После выхода новой минорной версии поддержка самой старой из трех версий прекращается:

```
┌─────────────────────────────────────────────────────┐
│  Поддерживаемые версии:                             │
│                                                       │
│  v1.28.x ✅ (текущая)                               │
│  v1.27.x ✅ (предыдущая)                            │
│  v1.26.x ✅ (старая)                                │
│                                                       │
│  → Выпуск v1.29.x                                   │
│                                                       │
│  v1.29.x ✅ (текущая)                               │
│  v1.28.x ✅ (предыдущая)                            │
│  v1.27.x ✅ (старая)                                │
│  v1.26.x ❌ (поддержка прекращена)                  │
└─────────────────────────────────────────────────────┘
```

> **⚠️ Важно:** Использование неподдерживаемых версий небезопасно, так как они не получают исправления безопасности и багов.

---

</details>

---

## Связанные темы

- **[K-10-1-OS-Upgrades](K-10-1-OS-Upgrades.md)** — обновление ОС узлов
- **[K-10-2-K8s-Software-Versions](K-10-2-K8s-Software-Versions.md)** — версионирование Kubernetes **← Текущая тема**
- **[K-10-3-Cluster-Upgrade-Process](K-10-3-Cluster-Upgrade-Process.md)** — процесс обновления кластера
- **[K-10-4-Backup-and-Restore-Methods](K-10-4-Backup-and-Restore-Methods.md)** — методы резервного копирования и восстановления

---

## Что дальше?

В следующей теме [K-10-3-Cluster-Upgrade-Process](K-10-3-Cluster-Upgrade-Process.md) мы рассмотрим процесс обновления кластера Kubernetes с одной версии на другую, включая совместимость версий компонентов, стратегии обновления и процедуры обновления с помощью kubeadm.

---