[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔧 K-10-Техобслуживание](../../README.md#-k-10-техобслуживание)

---

# 🔧K-10-4-Backup-and-Restore-Methods
>Методы резервного копирования и восстановления: резервное копирование через kube-api, резервное копирование ETCD, восстановление кластера

---

<details>
<summary><b>📋Введение в резервное копирование кластера</b></summary>

---

Резервное копирование кластера Kubernetes — критически важная операция для обеспечения непрерывности работы и возможности восстановления после сбоев.

Основные объекты для резервного копирования:

- **Конфигурации ресурсов** — файлы определений (Deployments, Services, Pods, ConfigMaps, Secrets и т.д.)
- **Состояние кластера в etcd** — вся информация о кластере, узлах и ресурсах
- **Persistent Volumes** — данные приложений, использующих постоянное хранилище

---

</details>

<details>
<summary><b>📝Декларативный vs императивный подход</b></summary>

---

## Декларативный подход (предпочтительный)

При декларативном подходе сначала создается файл определения ресурса, затем применяется через `kubectl apply`:

```bash
# Создать файл определения
cat > deployment.yaml <<EOF
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  ...
EOF

# Применить конфигурацию
kubectl apply -f deployment.yaml
```

### Преимущества

- ✅ Все объекты хранятся в виде файлов
- ✅ Легко использовать повторно
- ✅ Можно версионировать в Git
- ✅ Легко делиться с командой
- ✅ Простое восстановление — применить файлы заново

### Рекомендации

- Хранить файлы конфигурации в репозитории исходного кода (GitHub, GitLab)
- Использовать версионирование для отслеживания изменений
- Организовать файлы по приложениям или namespace

---

## Императивный подход (не рекомендуется для продакшена)

При императивном подходе объекты создаются напрямую через команды:

```bash
# Создание namespace
kubectl create namespace my-namespace

# Создание secret
kubectl create secret generic my-secret --from-literal=key=value

# Создание service через expose
kubectl expose deployment my-app --port=80
```

### Проблемы

- ❌ Конфигурация не документирована
- ❌ Сложно отследить созданные объекты
- ❌ Невозможно легко восстановить
- ❌ Сложно делиться с командой

### Решение

Если объекты созданы императивно, необходимо экспортировать их конфигурацию через API:

```bash
# Экспортировать все ресурсы в YAML
kubectl get all --all-namespaces -o yaml > backup.yaml
```

---

</details>

<details>
<summary><b>🔌Резервное копирование через kube-api</b></summary>

---

## Экспорт ресурсов через kubectl

### Экспорт всех ресурсов

```bash
# Получить все ресурсы во всех namespace
kubectl get all --all-namespaces -o yaml > backup-all.yaml
```

> **⚠️ Ограничение:** Команда `kubectl get all` возвращает только Pods, Services, Deployments, ReplicaSets и StatefulSets. Многие другие типы ресурсов не включаются.

### Экспорт конкретных типов ресурсов

```bash
# Экспорт Deployments
kubectl get deployments --all-namespaces -o yaml > deployments.yaml

# Экспорт Services
kubectl get services --all-namespaces -o yaml > services.yaml

# Экспорт ConfigMaps
kubectl get configmaps --all-namespaces -o yaml > configmaps.yaml

# Экспорт Secrets
kubectl get secrets --all-namespaces -o yaml > secrets.yaml

# Экспорт PersistentVolumeClaims
kubectl get pvc --all-namespaces -o yaml > pvc.yaml
```

### Экспорт ресурсов конкретного namespace

```bash
# Экспорт всех ресурсов в namespace
kubectl get all -n my-namespace -o yaml > my-namespace-backup.yaml
```

### Скрипт для полного резервного копирования

```bash
#!/bin/bash

BACKUP_DIR="k8s-backup-$(date +%Y%m%d-%H%M%S)"
mkdir -p "$BACKUP_DIR"

# Список всех типов ресурсов
RESOURCES=(
    "deployments"
    "services"
    "configmaps"
    "secrets"
    "persistentvolumeclaims"
    "statefulsets"
    "daemonsets"
    "jobs"
    "cronjobs"
    "ingresses"
    "networkpolicies"
    "roles"
    "rolebindings"
    "serviceaccounts"
)

# Экспорт каждого типа ресурса
for resource in "${RESOURCES[@]}"; do
    echo "Backing up $resource..."
    kubectl get "$resource" --all-namespaces -o yaml > "$BACKUP_DIR/$resource.yaml"
done

# Экспорт всех namespace
kubectl get namespaces -o yaml > "$BACKUP_DIR/namespaces.yaml"

echo "Backup completed: $BACKUP_DIR"
```

---

## Прямой доступ к API

Можно также обращаться напрямую к Kubernetes API:

```bash
# Получить все ресурсы через API
curl -k https://<api-server>:6443/api/v1/namespaces \
  --header "Authorization: Bearer $TOKEN" \
  | jq . > api-backup.json
```

---

</details>

<details>
<summary><b>🛠️Инструменты для резервного копирования</b></summary>

---

## Velero (бывший Heptio Ark)

Velero — популярный инструмент для резервного копирования и восстановления кластеров Kubernetes.

### Возможности

- Резервное копирование всего кластера или отдельных ресурсов
- Резервное копирование Persistent Volumes
- Планирование автоматических резервных копий
- Восстановление в другой кластер
- Миграция между кластерами

### Установка

```bash
# Установка Velero CLI
wget https://github.com/vmware-tanzu/velero/releases/download/v1.11.0/velero-v1.11.0-linux-amd64.tar.gz
tar -xzf velero-v1.11.0-linux-amd64.tar.gz
sudo mv velero-v1.11.0-linux-amd64/velero /usr/local/bin/

# Установка Velero в кластер
velero install \
    --provider aws \
    --bucket my-backup-bucket \
    --secret-file ./credentials-velero
```

### Использование

```bash
# Создать резервную копию
velero backup create my-backup

# Восстановить из резервной копии
velero restore create --from-backup my-backup
```

---

## Kasten K10

Kasten K10 — коммерческое решение для резервного копирования и восстановления Kubernetes.

### Возможности

- Автоматическое резервное копирование
- Восстановление на уровне приложения
- Миграция между кластерами
- Интеграция с облачными хранилищами

---

## Longhorn

Longhorn — распределенная система хранения для Kubernetes с возможностями резервного копирования.

### Возможности

- Резервное копирование Persistent Volumes
- Репликация данных
- Снимки (snapshots) томов
- Восстановление томов

---

</details>

<details>
<summary><b>💾Резервное копирование etcd</b></summary>

---

## Роль etcd в кластере

etcd хранит всю информацию о состоянии кластера Kubernetes:

- Информация о кластере
- Информация об узлах
- Все ресурсы (Pods, Deployments, Services и т.д.)
- Конфигурации и метаданные

### Расположение etcd

В большинстве случаев кластер etcd размещается на master узлах (stacked topology). При конфигурации etcd указывается каталог данных, где хранятся все данные.

---

## Создание снимка etcd

### Команда snapshot save

etcd поставляется со встроенным инструментом для создания снимков:

```bash
# Создать снимок базы данных etcd
etcdctl snapshot save snapshot.db
```

### Указание пути

```bash
# Создать снимок в указанном каталоге
etcdctl snapshot save /backup/etcd-snapshot-$(date +%Y%m%d).db
```

### С параметрами аутентификации

```bash
# Создать снимок с указанием сертификатов
etcdctl \
  --endpoints=https://127.0.0.1:2379 \
  --cacert=/etc/etcd/ca.crt \
  --cert=/etc/etcd/etcd-server.crt \
  --key=/etc/etcd/etcd-server.key \
  snapshot save /backup/snapshot.db
```

> **📌 Примечание:** В etcdctl версии 3.4+ не требуется префикс `ETCDCTL_API=3`. В версиях ниже 3.4 необходимо указывать `ETCDCTL_API=3 etcdctl ...`.

---

## Проверка состояния снимка

### Команда snapshot status

```bash
# Проверить состояние резервной копии
etcdctl snapshot status snapshot.db
```

### Вывод команды

```
4183472
4183472
healthy
```

Где:
- Первое число — размер файла снимка
- Второе число — количество ключей
- `healthy` — статус снимка

---

## Автоматизация резервного копирования

### Скрипт для автоматического резервного копирования

```bash
#!/bin/bash

BACKUP_DIR="/backup/etcd"
DATE=$(date +%Y%m%d-%H%M%S)
SNAPSHOT_FILE="$BACKUP_DIR/etcd-snapshot-$DATE.db"

# Создать каталог для резервных копий
mkdir -p "$BACKUP_DIR"

# Создать снимок
etcdctl \
  --endpoints=https://127.0.0.1:2379 \
  --cacert=/etc/etcd/ca.crt \
  --cert=/etc/etcd/etcd-server.crt \
  --key=/etc/etcd/etcd-server.key \
  snapshot save "$SNAPSHOT_FILE"

# Проверить статус
etcdctl snapshot status "$SNAPSHOT_FILE"

# Удалить старые резервные копии (старше 7 дней)
find "$BACKUP_DIR" -name "etcd-snapshot-*.db" -mtime +7 -delete

echo "Backup completed: $SNAPSHOT_FILE"
```

### Настройка cron для регулярного резервного копирования

```bash
# Добавить в crontab
0 2 * * * /usr/local/bin/etcd-backup.sh
```

---

</details>

<details>
<summary><b>🔄Восстановление из резервной копии etcd</b></summary>

---

## Процесс восстановления

### Шаг 1: Остановка kube-apiserver

Восстановление требует перезапуска кластера etcd, поэтому необходимо сначала остановить kube-apiserver, который зависит от etcd:

```bash
# Остановить kube-apiserver
systemctl stop kube-apiserver
```

### Шаг 2: Восстановление из снимка

```bash
# Восстановить кластер из снимка
etcdctl snapshot restore snapshot.db \
  --data-dir=/var/lib/etcd-backup \
  --initial-cluster-token=etcd-cluster-1 \
  --initial-cluster=master=https://127.0.0.1:2380 \
  --initial-advertise-peer-urls=https://127.0.0.1:2380
```

### Параметры восстановления

- `--data-dir` — новый каталог данных для восстановленного кластера
- `--initial-cluster-token` — новый токен кластера (важно для предотвращения присоединения к существующему кластеру)
- `--initial-cluster` — начальная конфигурация кластера
- `--initial-advertise-peer-urls` — URL для объявления участников кластера

> **⚠️ Важно:** При восстановлении etcd инициализирует новую конфигурацию кластера и настраивает участников как новых членов нового кластера. Это предотвращает случайное присоединение к существующему кластеру.

### Шаг 3: Обновление конфигурации etcd

Обновить файл конфигурации службы etcd для использования:

- Нового токена кластера
- Нового каталога данных (`/var/lib/etcd-backup`)

```bash
# Редактировать конфигурацию etcd
vim /etc/etcd/etcd.conf
```

### Шаг 4: Перезапуск служб

```bash
# Перезапустить службу etcd
systemctl daemon-reload
systemctl restart etcd

# Запустить kube-apiserver
systemctl start kube-apiserver
```

### Шаг 5: Проверка восстановления

```bash
# Проверить статус узлов
kubectl get nodes

# Проверить ресурсы
kubectl get all --all-namespaces
```

---

## Особенности восстановления

### Stacked topology vs External etcd

В примере выше рассматривался случай, когда kube-apiserver и etcd находятся на одной машине (stacked topology).

Если etcd находится на отдельной машине (external etcd), необходимо:

- Обновить конфигурацию kube-apiserver для указания нового IP адреса etcd
- Убедиться, что сетевые настройки корректны

### Полный пример восстановления

```bash
#!/bin/bash

SNAPSHOT_FILE="/backup/etcd-snapshot-20240101.db"
ETCD_DATA_DIR="/var/lib/etcd-backup"
ETCD_NAME="master"
ETCD_INITIAL_CLUSTER="master=https://127.0.0.1:2380"

# 1. Остановить kube-apiserver
systemctl stop kube-apiserver

# 2. Восстановить из снимка
etcdctl snapshot restore "$SNAPSHOT_FILE" \
  --data-dir="$ETCD_DATA_DIR" \
  --initial-cluster-token=etcd-cluster-restored \
  --initial-cluster="$ETCD_INITIAL_CLUSTER" \
  --initial-advertise-peer-urls=https://127.0.0.1:2380 \
  --name="$ETCD_NAME"

# 3. Обновить конфигурацию etcd
# (вручную отредактировать /etc/etcd/etcd.conf)

# 4. Перезапустить службы
systemctl daemon-reload
systemctl restart etcd
systemctl start kube-apiserver

# 5. Проверить восстановление
kubectl get nodes
kubectl get all --all-namespaces
```

---

</details>

<details>
<summary><b>📊Сравнение методов резервного копирования</b></summary>

---

## Резервное копирование через kube-api

### Преимущества

- ✅ Работает в управляемых средах (GKE, EKS, AKS)
- ✅ Не требует доступа к etcd
- ✅ Можно выбрать конкретные ресурсы
- ✅ Легко восстановить отдельные ресурсы
- ✅ Можно версионировать в Git

### Недостатки

- ❌ Не включает все типы ресурсов автоматически
- ❌ Требует ручного экспорта каждого типа
- ❌ Не включает состояние etcd напрямую
- ❌ Может быть медленным для больших кластеров

### Когда использовать

- Управляемые кластеры Kubernetes
- Когда нужен выборочный экспорт ресурсов
- Когда нужно версионировать конфигурации в Git

---

## Резервное копирование etcd

### Преимущества

- ✅ Полное резервное копирование всего состояния кластера
- ✅ Быстрое создание снимков
- ✅ Встроенный инструмент (etcdctl)
- ✅ Восстановление всего кластера из одной копии

### Недостатки

- ❌ Требует доступа к etcd (может быть недоступен в управляемых средах)
- ❌ Восстановление требует остановки кластера
- ❌ Сложнее восстановить отдельные ресурсы
- ❌ Не включает данные Persistent Volumes

### Когда использовать

- Self-managed кластеры
- Когда нужен полный бэкап состояния кластера
- Для disaster recovery сценариев

---

## Сравнительная таблица

| Характеристика | kube-api | etcd | Инструменты (Velero) |
|----------------|----------|------|----------------------|
| Доступ в managed K8s | ✅ Да | ❌ Нет | ✅ Да |
| Полное резервное копирование | ⚠️ Частично | ✅ Да | ✅ Да |
| Резервное копирование PV | ❌ Нет | ❌ Нет | ✅ Да |
| Восстановление отдельных ресурсов | ✅ Да | ❌ Нет | ✅ Да |
| Автоматизация | ⚠️ Ручная | ⚠️ Ручная | ✅ Да |
| Планирование | ❌ Нет | ❌ Нет | ✅ Да |

---

</details>

<details>
<summary><b>📝Резервное копирование Persistent Volumes</b></summary>

---

## Важность резервного копирования PV

Если приложения используют Persistent Volumes для хранения данных, необходимо также резервное копирование этих томов.

### Методы резервного копирования PV

#### 1. Резервное копирование на уровне хранилища

Если используется облачное хранилище (AWS EBS, GCP Persistent Disk, Azure Disk), можно использовать встроенные возможности резервного копирования:

```bash
# AWS EBS snapshot
aws ec2 create-snapshot --volume-id vol-1234567890abcdef0

# GCP Disk snapshot
gcloud compute disks snapshot DISK_NAME --snapshot-names=SNAPSHOT_NAME
```

#### 2. Резервное копирование данных внутри Pod

```bash
# Подключиться к Pod и создать резервную копию данных
kubectl exec -it <pod-name> -- tar czf /backup/data.tar.gz /data

# Скопировать резервную копию с Pod
kubectl cp <pod-name>:/backup/data.tar.gz ./data-backup.tar.gz
```

#### 3. Использование Velero

Velero может автоматически создавать резервные копии Persistent Volumes:

```bash
# Создать резервную копию с включением PV
velero backup create my-backup --include-resources persistentvolumes,persistentvolumeclaims
```

---

</details>

<details>
<summary><b>✅Best Practices</b></summary>

---

## Рекомендации по резервному копированию

### 1. Комбинированный подход

Использовать оба метода для максимальной защиты:

- **kube-api** — для конфигураций ресурсов (версионирование в Git)
- **etcd** — для полного резервного копирования состояния кластера
- **PV snapshots** — для данных приложений

### 2. Регулярность резервного копирования

- **etcd snapshots**: ежедневно (или чаще для критичных кластеров)
- **Конфигурации**: при каждом изменении (через Git)
- **PV snapshots**: в зависимости от критичности данных

### 3. Хранение резервных копий

- Хранить резервные копии вне кластера
- Использовать несколько локаций (3-2-1 правило)
- Шифровать чувствительные данные
- Регулярно тестировать восстановление

### 4. Документация

- Документировать процедуры резервного копирования
- Документировать процедуры восстановления
- Регулярно проводить учения по восстановлению

### 5. Мониторинг

- Мониторить успешность резервного копирования
- Настроить алерты при сбоях
- Регулярно проверять целостность резервных копий

---

</details>

---

## Связанные темы

- **[K-10-1-OS-Upgrades](K-10-1-OS-Upgrades.md)** — обновление ОС узлов
- **[K-10-2-K8s-Software-Versions](K-10-2-K8s-Software-Versions.md)** — версионирование Kubernetes
- **[K-10-3-Cluster-Upgrade-Process](K-10-3-Cluster-Upgrade-Process.md)** — процесс обновления кластера
- **[K-10-4-Backup-and-Restore-Methods](K-10-4-Backup-and-Restore-Methods.md)** — методы резервного копирования и восстановления **← Текущая тема**

---

## Что дальше?

В следующих темах мы рассмотрим дополнительные аспекты технического обслуживания кластера Kubernetes, включая мониторинг состояния кластера и управление конфигурацией.

---