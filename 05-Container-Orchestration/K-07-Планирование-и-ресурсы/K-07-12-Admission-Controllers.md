[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🎯 K-07-Планирование-и-ресурсы](../../README.md#-k-07-планирование-и-ресурсы)

---

# 🛡️K-07-12-Admission-Controllers
>Admission Controllers (контроллеры допуска) — механизм Kubernetes для перехвата и валидации/модификации запросов к API Server после аутентификации и авторизации, но до сохранения объектов в etcd

---

<details>
<summary><b>🔍Что такое Admission Controllers?</b></summary>

---

## Процесс обработки запроса в Kubernetes

Когда мы запускаем команды из командной строки с помощью утилиты `kubectl` и выполняем различные операции в кластере Kubernetes, каждый раз при отправке запроса на создание Pod'а происходит следующее:

1. **Запрос отправляется на API Server (kube-apiserver)**
2. **Создается Pod, информация сохраняется в базе данных etcd**

Когда запрос попадает на API Server, он проходит процесс **аутентификации** (Authentication).

---

## Аутентификация (Authentication)

**Аутентификация** — процесс идентификации пользователя, который стоит за запросом, и проверка, действительно ли он тот, за кого себя выдает.

**В большинстве случаев это делается с помощью сертификатов:**
- Если запрос был отправлен через `kubectl`, мы знаем, что эти сертификаты настроены внутри файла `~/.kube/config`
- Процесс аутентификации занимается тем, чтобы идентифицировать пользователя и проверить его подлинность

---

## Авторизация (Authorization / RBAC)

После аутентификации наступает очередь процесса **авторизации**.

Именно тогда Kubernetes проверяет, есть ли у пользователя разрешение на выполнение указанной в запросе операции.

**Это реализуется за счет управления доступом на основе ролей (RBAC - Role-Based Access Control):**

- Если пользователю была назначена роль `developer`, то ему разрешено выполнять операции:
  - `list` (список)
  - `get` (получение)
  - `create` (создание)
  - `update` (обновление)
  - `delete` (удаление)
  
  на Pod'ах

- Если пришедший запрос соответствует любому из этих условий, то он может произойти
- В противном случае он отвергается

**Пример:** Если мы делаем запрос на создание Pod'а, то нашему пользователю `dev-user`, который использует роль `developer`, это будет разрешено.

---

## Ограничения RBAC

С помощью управления доступом на основе ролей (RBAC) мы можем накладывать различные ограничения:

✅ **Что можно делать:**
- Разрешать или запрещать тем, у кого есть определенная роль, выполнять операции `create`, `list` или `delete` на различные виды объектов
- Это такие объекты вроде Pod'ов, Deployments или Secrets
- Мы даже можем ограничить доступ по названиям ресурсов
- Например, разработчик может работать только с Pod'ами, которые называются `blue` или `orange`
- Или ограничить доступ к каким-то пространствам имен (namespace'ам)

❌ **Ограничения:**
- Обрати внимание, что большинство таких правил, которые мы можем создать с RBAC, находится на уровне Kubernetes API
- То есть пользователям разрешен только доступ к какому-то типу операций с API
- **Дальше функции ограничения заканчиваются**

---

## Зачем нужны Admission Controllers?

**Проблема:** Что если мы хотим сделать больше, чем просто определить, какой тип доступа пользователь имеет к объекту?

**Примеры задач, которые RBAC не решает:**

1. **Проверка образов контейнеров**
   - Когда приходит запрос на создание Pod'а, мы хотели бы просмотреть файл конфигурации
   - Проверить имя образа и каким-то методом не разрешить запускать образы из общедоступного реестра Docker Hub

2. **Запрет использования тега latest**
   - Запретить использование тега `latest` для каких-то образов

3. **Безопасность контейнеров**
   - Внедрить практику запрета запуска контейнеров от root пользователя
   - Не разрешать запуск других контейнеров в кластере

4. **Linux Capabilities**
   - Определить список Linux capabilities, которые будут доступны в запущенном контейнере

5. **Метаданные объектов**
   - Обеспечить, чтобы у метаданных объектов всегда присутствовали метки

**Это некоторые из вещей, которых мы не сможем достичь с помощью существующей реализации управления доступом на основе ролей в Kubernetes.**

---

## Решение: Admission Controllers

**Admission Controllers (контроллеры допуска)** помогают нам:
- ✅ Внедрить более эффективные меры безопасности
- ✅ Обеспечить соблюдение дополнительных правил при использовании кластера
- ✅ Помимо простой проверки конфигурации, эти контроллеры могут делать гораздо больше

**Что могут делать Admission Controllers:**
- ✅ Изменять сам запрос
- ✅ Выполнять дополнительные операции до того, как Pod будет создан

---

## Как работают Admission Controllers?

```
┌─────────────────────────────────────────────────────────┐
│          Процесс обработки запроса в API Server          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Запрос к API Server                                 │
│           │                                              │
│           ▼                                              │
│  2. Аутентификация (Authentication)                     │
│     (идентификация пользователя через сертификаты)      │
│           │                                              │
│           ▼                                              │
│  3. Авторизация (Authorization / RBAC)                  │
│     (проверка разрешений на операцию)                    │
│           │                                              │
│           ▼                                              │
│  4. Admission Controllers                               │
│     (валидация/модификация запроса)                      │
│           │                                              │
│           ▼                                              │
│  5. Сохранение в etcd                                   │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Важно:** Admission Controllers выполняются **после** аутентификации и авторизации, но **до** сохранения объекта в etcd.

---

</details>

<details>
<summary><b>🏗️Встроенные Admission Controllers</b></summary>

---

## Список встроенных контроллеров

Существует ряд Admission Controllers, которые изначально встроены в Kubernetes:

### AlwaysPullImages

**Назначение:** Гарантирует, что каждый раз, когда создается Pod, образ будет скачиваться из registry.

**Как работает:**
- Фактически устанавливает `imagePullPolicy: Always` в политику скачивания образов
- Полезно для безопасности в multi-tenant окружениях

---

### DefaultStorageClass

**Назначение:** Наблюдает за созданными PVC (PersistentVolumeClaim) и автоматически добавляет к ним класс хранения по умолчанию, если не определен какой-то отдельный класс.

---

### EventRateLimit

**Назначение:** Может помочь в установке ограничений на запросы, которые API Server может обрабатывать за раз.

**Цель:** Предотвратить request flooding на API Server.

---

### NamespaceAutoProvision

**Назначение:** Для создания namespace'ов на лету (автоматически).

**Особенность:** Не включен по умолчанию.

**Как работает:** Автоматически создаст namespace, если такой namespace не существует.

---

### NamespaceExists

**Назначение:** Для запросов к несуществующим пространствам имен.

**Особенность:** Встроенный контроллер допуска, включен по умолчанию.

**Как работает:** Отклоняет запросы к несуществующим namespace'ам.

> ⚠️ **Важно:** `NamespaceAutoProvision` и `NamespaceExists` уже устарели и заменены общим контроллером **NamespaceLifecycle**.

---

### PodSecurityPolicy

**Назначение:** Для работы с политиками безопасности Pod'ов.

**Статус:** Устарел, заменен на Pod Security Standards.

---

### PodSecurity

**Назначение:** Более новый контроллер для работы с Open Policy Agent.

**Статус:** Заменяет устаревший PodSecurityPolicy.

---

### Другие контроллеры

Есть еще много доступных контроллеров допуска. Можешь почитать о них на страницах документации Kubernetes.

---

</details>

<details>
<summary><b>📝Пример: NamespaceExists</b></summary>

---

## Как работает NamespaceExists

Давай возьмем в качестве примера и рассмотрим более подробно **NamespaceExists**.

**Сценарий:** Допустим, мы решили создать Pod в пространстве имен `test`, которого не существует.

**Команда:**
```bash
kubectl run nginx --image=nginx -n test
```

**Результат:**
```
Error from server (NotFound): namespaces "test" not found
```

---

## Что происходит при запросе?

```
┌─────────────────────────────────────────────────────────┐
│              Процесс обработки запроса                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Запрос: kubectl run nginx --image=nginx -n test     │
│           │                                              │
│           ▼                                              │
│  2. Аутентификация                                      │
│     (идентификация пользователя)                        │
│           │                                              │
│           ▼                                              │
│  3. Авторизация (RBAC)                                  │
│     (проверка разрешений)                               │
│           │                                              │
│           ▼                                              │
│  4. Admission Controllers                               │
│     └─ NamespaceExists                                  │
│        Проверяет: доступно ли пространство имен test?  │
│        Результат: НЕТ → Отклоняет запрос ❌            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**NamespaceExists** — это встроенный контроллер допуска, и он включен по умолчанию.

---

</details>

<details>
<summary><b>⚙️Просмотр и настройка Admission Controllers</b></summary>

---

## Просмотр включенных контроллеров

**Чтобы просмотреть список контроллеров допуска, включенных по умолчанию:**

```bash
kube-apiserver --help | grep enable-admission-plugins
```

**Для kubeadm кластера:**

Если ты смотришь Kubernetes на основе установки с kubeadm, то данную команду нужно запустить внутри Pod'а с API Server:

```bash
kubectl exec -n kube-system kube-apiserver-<node-name> -- kube-apiserver --help | grep enable-admission-plugins
```

**Вывод:** Здесь ты увидишь список контроллеров допуска, которые включены по умолчанию.

---

## Включение Admission Controllers

**Чтобы добавить контроллеры допуска, которые тебе потребовались:**

### Для kubeadm кластера

**Обнови флаг в файле манифеста статического Pod'а kube-apiserver:**

```yaml
# /etc/kubernetes/manifests/kube-apiserver.yaml
apiVersion: v1
kind: Pod
metadata:
  name: kube-apiserver
  namespace: kube-system
spec:
  containers:
  - name: kube-apiserver
    command:
    - kube-apiserver
    - --enable-admission-plugins=NamespaceLifecycle,LimitRanger,ServiceAccount,ResourceQuota,DefaultStorageClass,NamespaceAutoProvision
    # Добавь название контроллера в список
    ...
```

**После обновления:** Когда API Server работает в Pod'е, изменения применятся автоматически (kubelet перезапустит Pod).

---

### Для системного сервиса

**Если API Server работает как служба:**

```bash
# Редактировать файл конфигурации службы
# /etc/systemd/system/kube-apiserver.service

# Добавить флаг:
--enable-admission-plugins=NamespaceLifecycle,LimitRanger,ServiceAccount,ResourceQuota,DefaultStorageClass,NamespaceAutoProvision
```

**После обновления:** Перезапусти службу.

---

## Отключение Admission Controllers

**Точно также, чтобы отключить Admission Controllers, можно использовать флаг:**

```yaml
--disable-admission-plugins=PodSecurityPolicy
```

---

</details>

<details>
<summary><b>📋Пример: NamespaceAutoProvision</b></summary>

---

## Как работает NamespaceAutoProvision

**NamespaceAutoProvision** — контроллер допуска, который **не включен по умолчанию**.

**Назначение:** Он автоматически создаст namespace, если такой namespace не существует.

---

## Включение NamespaceAutoProvision

**Шаг 1: Включить контроллер**

Добавь `NamespaceAutoProvision` в список `--enable-admission-plugins`:

```yaml
# /etc/kubernetes/manifests/kube-apiserver.yaml
apiVersion: v1
kind: Pod
metadata:
  name: kube-apiserver
  namespace: kube-system
spec:
  containers:
  - name: kube-apiserver
    command:
    - kube-apiserver
    - --enable-admission-plugins=...,NamespaceAutoProvision
    ...
```

**Шаг 2: После обновления**

Когда в следующий раз мы запустим команду для создания Pod'а в пространстве имен, которого еще не существует:

```bash
kubectl run nginx --image=nginx -n test
```

---

## Что происходит?

```
┌─────────────────────────────────────────────────────────┐
│              Процесс обработки запроса                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Запрос: kubectl run nginx --image=nginx -n test    │
│           │                                              │
│           ▼                                              │
│  2. Аутентификация                                      │
│           │                                              │
│           ▼                                              │
│  3. Авторизация                                         │
│           │                                              │
│           ▼                                              │
│  4. Admission Controllers                               │
│     └─ NamespaceAutoProvision                          │
│        Понимает: пространства имен test не существует  │
│        Действие: автоматически создает namespace ✅    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Результат:**

```bash
kubectl get namespaces
NAME              STATUS   AGE
test              Active   1m  # Автоматически создан!
```

---

## Важное замечание

**Проблема:** Запрос может выполниться не совсем успешно.

**Причина:** Это произошло не из-за namespace, а из-за сервисного аккаунта.

**Что произошло:**
- Если мы посмотрим на список, то увидим, что пространство имен `test` уже есть
- Но мы его не создавали сами — его автоматически создал наш контроллер допуска
- **Однако он забыл создать дефолтный ServiceAccount для namespace'а**
- Поэтому создание Pod'а завершилось ошибкой

---

## Вывод

**Это один из примеров того, как работают Admission Controllers:**

- ✅ Они могут не только проверять и отклонять запросы от пользователей
- ✅ Но и выполнять операции в бэкенде
- ✅ Или изменять сам запрос

---

</details>

<details>
<summary><b>🔄NamespaceLifecycle (замена устаревших контроллеров)</b></summary>

---

## Замена устаревших контроллеров

**Обрати внимание:** `NamespaceAutoProvision` и `NamespaceExists` уже устарели и заменены общим контроллером **NamespaceLifecycle**.

---

## NamespaceLifecycle

**Контроллер допуска NamespaceLifecycle** гарантирует:

1. ✅ **Запрос к несуществующему пространству имен будет отклонен**
   - Предотвращает создание объектов в несуществующих namespace'ах

2. ✅ **Пространства имен по умолчанию не могут быть удалены**
   - Namespace'ы по умолчанию, такие как:
     - `default`
     - `kube-system`
     - `kube-public`
   - Не могут быть удалены

---

## Использование

**NamespaceLifecycle включен по умолчанию** в современных версиях Kubernetes и заменяет функциональность обоих устаревших контроллеров.

---

</details>

---

## Резюме

✅ **Admission Controllers (контроллеры допуска)** — механизм Kubernetes для перехвата и валидации/модификации запросов к API Server

✅ **Процесс обработки запроса:**
1. Запрос к API Server
2. **Аутентификация** — идентификация пользователя (через сертификаты в `~/.kube/config`)
3. **Авторизация (RBAC)** — проверка разрешений на выполнение операции
4. **Admission Controllers** — валидация/модификация запроса
5. Сохранение в etcd

✅ **Ограничения RBAC:**
- RBAC работает только на уровне Kubernetes API
- Разрешает/запрещает доступ к типам операций с API
- **Не может:** проверять образы контейнеров, запрещать теги `latest`, запрещать root контейнеры, проверять Linux capabilities, требовать обязательные метки

✅ **Зачем нужны Admission Controllers:**
- Внедрение более эффективных мер безопасности
- Обеспечение соблюдения дополнительных правил при использовании кластера
- Проверка и модификация конфигурации объектов
- Выполнение дополнительных операций до создания Pod'а

✅ **Встроенные Admission Controllers:**
- **AlwaysPullImages** — принудительно устанавливает `imagePullPolicy: Always`
- **DefaultStorageClass** — автоматически добавляет default StorageClass к PVC
- **EventRateLimit** — ограничивает количество запросов к API Server
- **NamespaceAutoProvision** — автоматически создает namespace'ы (устарел)
- **NamespaceExists** — отклоняет запросы к несуществующим namespace'ам (устарел)
- **NamespaceLifecycle** — управление жизненным циклом namespace'ов (заменяет устаревшие)
- **PodSecurityPolicy** — работа с политиками безопасности (устарел)
- **PodSecurity** — новый контроллер для работы с Open Policy Agent

✅ **Управление Admission Controllers:**
- Просмотр: `kube-apiserver --help | grep enable-admission-plugins`
- Для kubeadm: `kubectl exec -n kube-system kube-apiserver-<node> -- kube-apiserver --help | grep enable-admission-plugins`
- Включение: `--enable-admission-plugins=Controller1,Controller2`
- Отключение: `--disable-admission-plugins=Controller1`

✅ **Примеры работы:**
- **NamespaceExists** — отклоняет запросы к несуществующим namespace'ам
- **NamespaceAutoProvision** — автоматически создает namespace'ы (может забыть создать ServiceAccount)
- **NamespaceLifecycle** — заменяет оба устаревших контроллера

✅ **Важно:**
- Admission Controllers могут не только проверять и отклонять запросы
- Но и выполнять операции в бэкенде
- Или изменять сам запрос

> 💡 **Совет:** Admission Controllers — мощный инструмент для обеспечения политик и стандартов в кластере. Они работают после аутентификации и авторизации, но до сохранения объекта в etcd, что позволяет внедрять дополнительные меры безопасности и контроля.

---
