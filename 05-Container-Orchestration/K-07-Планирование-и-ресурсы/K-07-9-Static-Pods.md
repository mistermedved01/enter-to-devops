[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🎯 K-07-Планирование-и-ресурсы](../../README.md#-k-07-планирование-и-ресурсы)

---

# 🔧K-07-9-Static-Pods
>Static Pods — Pod'ы, созданные kubelet напрямую из файлов манифестов без участия API-сервера, используемые для развертывания компонентов control plane

---

<details>
<summary><b>🔍Что такое Static Pods?</b></summary>

---

## Концепция Static Pods

**Static Pods** — это Pod'ы, созданные kubelet напрямую из файлов манифестов, **без участия API-сервера** и других компонентов control plane.

**Ключевые особенности:**
- ✅ Создаются kubelet напрямую из файлов манифестов
- ✅ Не требуют API-сервера, scheduler'а или etcd
- ✅ Могут работать на изолированном узле (без кластера)
- ✅ Автоматически перезапускаются при сбое
- ✅ Автоматически обновляются при изменении файлов

---

## Сценарий: Узел без кластера

**Вопрос:** Что делать, если узел не является частью кластера Kubernetes?

```
┌─────────────────────────────────────────────────────────┐
│              Стандартный кластер Kubernetes              │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Control Plane (Master)                                  │
│  ┌─────────────┐                                        │
│  │ kube-apiserver                                      │
│  │ kube-scheduler                                      │
│  │ kube-controller-manager                            │
│  │ etcd                                                │
│  └─────────────┘                                        │
│           │                                              │
│           │ Инструкции                                   │
│           ▼                                              │
│  Worker Node                                             │
│  ┌─────────────┐                                        │
│  │ kubelet     │ ← Получает инструкции от API-сервера   │
│  │ Docker      │                                        │
│  │ Pods        │                                        │
│  └─────────────┘                                        │
│                                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│              Изолированный узел (без кластера)          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────┐                                        │
│  │ kubelet     │ ← Нет API-сервера!                     │
│  │ Docker      │                                        │
│  │             │ Откуда брать инструкции?               │
│  └─────────────┘                                        │
│                                                          │
│  Решение: Static Pods из файлов манифестов              │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Решение:** Static Pods — kubelet читает файлы манифестов из специальной директории на хосте.

---

## Как работает Static Pod?

```
┌─────────────────────────────────────────────────────────┐
│                    Static Pod Workflow                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Файл манифеста в директории                         │
│     /etc/kubernetes/manifests/my-pod.yaml               │
│           │                                              │
│           ▼                                              │
│  2. kubelet периодически проверяет директорию            │
│           │                                              │
│           ▼                                              │
│  3. kubelet читает файл манифеста                       │
│           │                                              │
│           ▼                                              │
│  4. kubelet создает контейнер через Docker               │
│           │                                              │
│           ▼                                              │
│  5. kubelet гарантирует, что Pod останется в живых      │
│     - Автоматический перезапуск при сбое                │
│     - Автоматическое обновление при изменении файла     │
│     - Автоматическое удаление при удалении файла        │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Процесс:**
1. Файл манифеста Pod'а помещается в специальную директорию
2. kubelet периодически проверяет эту директорию
3. Если находит файл → читает его и создает Pod
4. Если Pod падает → kubelet автоматически перезапускает его
5. Если файл изменен → kubelet воссоздает Pod
6. Если файл удален → kubelet удаляет Pod

---

</details>

<details>
<summary><b>📁Настройка директории Static Pods</b></summary>

---

## Параметр pod-manifest-path

**Способ 1: Прямой параметр в kubelet.service**

```bash
# Файл: /etc/systemd/system/kubelet.service
[Service]
ExecStart=/usr/bin/kubelet \
  --pod-manifest-path=/etc/kubernetes/manifests \
  ...
```

**Параметр:**
- `--pod-manifest-path` — путь к директории с манифестами Static Pods

---

## Параметр staticPodPath в конфигурационном файле

**Способ 2: Через конфигурационный файл (используется kubeadm)**

```bash
# Файл: /etc/systemd/system/kubelet.service
[Service]
ExecStart=/usr/bin/kubelet \
  --config=/var/lib/kubelet/config.yaml \
  ...
```

**Конфигурационный файл: `/var/lib/kubelet/config.yaml`**

```yaml
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
staticPodPath: /etc/kubernetes/manifests
```

**Параметр:**
- `--config` — путь к конфигурационному файлу
- `staticPodPath` — путь к директории с манифестами в конфигурационном файле

> 💡 **Факт:** `kubeadm` использует второй способ (конфигурационный файл).

---

## Как найти путь к директории Static Pods?

**Шаг 1: Проверьте параметр pod-manifest-path**

```bash
# Проверьте файл службы kubelet
cat /etc/systemd/system/kubelet.service

# Или используйте systemctl
systemctl cat kubelet
```

**Шаг 2: Если нет pod-manifest-path, проверьте параметр --config**

```bash
# Найдите параметр --config
grep -i config /etc/systemd/system/kubelet.service

# Откройте конфигурационный файл
cat /var/lib/kubelet/config.yaml
```

**Шаг 3: Найдите staticPodPath в конфигурационном файле**

```bash
# Проверьте конфигурационный файл
grep staticPodPath /var/lib/kubelet/config.yaml
```

**Пример вывода:**
```yaml
staticPodPath: /etc/kubernetes/manifests
```

---

## Стандартные пути

**Типичные пути к директории Static Pods:**
- `/etc/kubernetes/manifests` — стандартный путь (используется kubeadm)
- `/var/lib/kubelet/manifests` — альтернативный путь
- Любой другой путь, указанный в конфигурации

> ⚠️ **Важно:** Путь может быть разным в зависимости от способа настройки кластера. Всегда проверяйте конфигурацию kubelet.

---

</details>

<details>
<summary><b>🛠️Создание Static Pod</b></summary>

---

## Базовая структура манифеста

Static Pod — это обычный манифест Pod'а в YAML формате:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-static-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
```

**Ключевые моменты:**
- ✅ Используется стандартный формат манифеста Pod'а
- ✅ Файл должен быть в формате YAML
- ✅ Имя файла может быть любым (например, `my-pod.yaml`)

---

## Пример: Static Pod с веб-сервером

**Файл: `/etc/kubernetes/manifests/web-server.yaml`**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: web-server
  labels:
    app: web-server
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
```

**После создания файла:**
1. kubelet автоматически обнаружит файл
2. Создаст Pod через Docker
3. Начнет мониторить состояние Pod'а

---

## Что можно создавать как Static Pod?

**✅ Можно:**
- Pod'ы (только Pod'ы)

**❌ Нельзя:**
- ReplicaSet
- Deployment
- Service
- ConfigMap
- Secret
- Другие ресурсы Kubernetes

**Почему?**
- kubelet работает на уровне Pod'ов
- Другие ресурсы требуют контроллеров и API-сервера
- Static Pods создаются напрямую kubelet, без участия других компонентов

---

## Проверка Static Pod

**Поскольку нет API-сервера, используйте Docker:**

```bash
# Просмотр запущенных контейнеров
docker ps

# Вывод:
# CONTAINER ID   IMAGE          COMMAND                  CREATED         STATUS
# abc123def456   nginx:latest   "nginx -g 'daemon of…"   5 minutes ago   Up 5 minutes
```

**Идентификация Static Pod:**
- Контейнеры Static Pod'ов имеют префикс в имени
- Формат имени: `<pod-name>_<namespace>_<pod-uid>_<container-name>`

---

</details>

<details>
<summary><b>🔄Static Pods в кластере</b></summary>

---

## Может ли kubelet создавать оба типа Pod'ов?

**Да!** kubelet может создавать Pod'ы из двух источников одновременно:

```
┌─────────────────────────────────────────────────────────┐
│                    Источники Pod'ов для kubelet          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Static Pods (из файлов манифестов)                  │
│     ┌──────────────────────┐                            │
│     │ /etc/kubernetes/     │                            │
│     │ manifests/*.yaml     │                            │
│     └──────────────────────┘                            │
│           │                                              │
│           ▼                                              │
│     kubelet создает Pod                                  │
│                                                          │
│  2. Pods от API-сервера                                 │
│     ┌──────────────────────┐                            │
│     │ kube-apiserver       │                            │
│     │ (через HTTP API)     │                            │
│     └──────────────────────┘                            │
│           │                                              │
│           ▼                                              │
│     kubelet создает Pod                                  │
│                                                          │
│  Оба типа Pod'ов могут работать одновременно!           │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Источники:**
1. **Static Pods** — из директории манифестов на хосте
2. **API-сервер** — через HTTP API endpoint

---

## Зеркальные объекты (Mirror Pods)

**Вопрос:** Видит ли API-сервер Static Pods?

**Ответ:** Да! kubelet создает зеркальные объекты в API-сервере.

```
┌─────────────────────────────────────────────────────────┐
│              Static Pod в кластере                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Worker Node                                             │
│  ┌──────────────────────┐                                │
│  │ /etc/kubernetes/    │                                │
│  │ manifests/pod.yaml  │                                │
│  └──────────────────────┘                                │
│           │                                              │
│           ▼                                              │
│  kubelet создает Static Pod                              │
│           │                                              │
│           ├──────────────────────────────────────┐       │
│           │                                      │       │
│           ▼                                      ▼       │
│  Docker контейнер (реальный)   Зеркальный объект (API)   │
│  ┌──────────────────────┐    ┌──────────────────────┐  │
│  │ Pod: my-pod           │    │ Pod: my-pod-node01   │  │
│  │ (реальный)            │    │ (read-only)          │  │
│  └──────────────────────┘    └──────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Особенности зеркальных объектов:**
- ✅ Видны через `kubectl get pods`
- ✅ Доступны только для чтения (read-only)
- ❌ Нельзя редактировать или удалять через `kubectl`
- ✅ Имя автоматически дополняется именем узла (например, `my-pod-node01`)

---

## Проверка Static Pod в кластере

```bash
# Просмотр Pod'ов (включая зеркальные объекты Static Pods)
kubectl get pods

# Вывод:
# NAME              READY   STATUS    RESTARTS   AGE
# my-pod-node01     1/1     Running   0          5m

# Детальная информация
kubectl describe pod my-pod-node01

# Вывод покажет:
# Annotations: kubernetes.io/config.source=file
```

**Идентификация Static Pod:**
- Annotation `kubernetes.io/config.source=file` указывает на Static Pod
- Имя содержит имя узла в конце

---

## Управление Static Pod

**Через kubectl (только просмотр):**
```bash
# Просмотр (read-only)
kubectl get pods
kubectl describe pod my-pod-node01
kubectl logs my-pod-node01
```

**Через файлы манифестов (редактирование):**
```bash
# Редактирование манифеста
vi /etc/kubernetes/manifests/my-pod.yaml

# После сохранения kubelet автоматически обновит Pod

# Удаление Pod'а
rm /etc/kubernetes/manifests/my-pod.yaml

# После удаления файла kubelet автоматически удалит Pod
```

> ⚠️ **Важно:** Static Pods можно управлять только через файлы манифестов на узле, где они запущены.

---

</details>

<details>
<summary><b>🎯Use Case: Развертывание Control Plane</b></summary>

---

## Зачем использовать Static Pods для Control Plane?

**Проблема:** Как развернуть компоненты control plane (kube-apiserver, etcd, kube-scheduler)?

**Решение:** Использовать Static Pods!

**Преимущества:**
- ✅ Не требуют работающего API-сервера
- ✅ Автоматический перезапуск при сбое
- ✅ Простое управление через файлы манифестов
- ✅ Не нужно настраивать systemd-сервисы

---

## Процесс развертывания Control Plane

```
┌─────────────────────────────────────────────────────────┐
│         Развертывание Control Plane через Static Pods   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Шаг 1: Установка kubelet на master-узлы                │
│           │                                              │
│           ▼                                              │
│  Шаг 2: Создание манифестов компонентов control plane   │
│           │                                              │
│           ├─ kube-apiserver.yaml                        │
│           ├─ etcd.yaml                                  │
│           ├─ kube-controller-manager.yaml               │
│           └─ kube-scheduler.yaml                         │
│           │                                              │
│           ▼                                              │
│  Шаг 3: Размещение манифестов в директории Static Pods  │
│           │                                              │
│           ▼                                              │
│  Шаг 4: kubelet автоматически создает Pod'ы             │
│           │                                              │
│           ▼                                              │
│  Шаг 5: Control plane готов!                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Пример: kube-apiserver как Static Pod

**Файл: `/etc/kubernetes/manifests/kube-apiserver.yaml`**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: kube-apiserver
  namespace: kube-system
spec:
  containers:
  - name: kube-apiserver
    image: k8s.gcr.io/kube-apiserver:v1.24.0
    command:
    - kube-apiserver
    - --advertise-address=192.168.1.100
    - --allow-privileged=true
    - --authorization-mode=Node,RBAC
    - --client-ca-file=/etc/kubernetes/pki/ca.crt
    - --enable-admission-plugins=NodeRestriction
    - --etcd-servers=https://127.0.0.1:2379
    - --etcd-cafile=/etc/kubernetes/pki/etcd/ca.crt
    - --kubelet-client-certificate=/etc/kubernetes/pki/apiserver-kubelet-client.crt
    - --kubelet-client-key=/etc/kubernetes/pki/apiserver-kubelet-client.key
    - --tls-cert-file=/etc/kubernetes/pki/apiserver.crt
    - --tls-private-key-file=/etc/kubernetes/pki/apiserver.key
    volumeMounts:
    - name: etc-ca
      mountPath: /etc/kubernetes/pki
    - name: etc-ca2
      mountPath: /etc/ssl/certs
  volumes:
  - name: etc-ca
    hostPath:
      path: /etc/kubernetes/pki
  - name: etc-ca2
    hostPath:
      path: /etc/ssl/certs
  hostNetwork: true
```

---

## Проверка компонентов Control Plane

**После развертывания через Static Pods:**

```bash
# Просмотр Pod'ов control plane
kubectl get pods -n kube-system

# Вывод:
# NAME                                 READY   STATUS    RESTARTS   AGE
# etcd-master01                         1/1     Running   0          5m
# kube-apiserver-master01               1/1     Running   0          5m
# kube-controller-manager-master01     1/1     Running   0          5m
# kube-scheduler-master01               1/1     Running   0          5m
```

**Идентификация:**
- Имена содержат имя узла (например, `-master01`)
- Annotation `kubernetes.io/config.source=file` указывает на Static Pod

> 💡 **Факт:** `kubeadm` использует Static Pods для развертывания компонентов control plane. Поэтому в кластерах, созданных через `kubeadm`, компоненты control plane видны как Pod'ы в namespace `kube-system`.

---

</details>

<details>
<summary><b>📊Static Pods vs DaemonSets</b></summary>

---

## Сравнительная таблица

| Критерий | Static Pods | DaemonSets |
|----------|-------------|------------|
| **Создание** | Напрямую kubelet из файлов | Через DaemonSet Controller и API-сервер |
| **Требования** | Только kubelet и Docker | Требуется API-сервер, контроллеры |
| **Управление** | Через файлы манифестов на узле | Через `kubectl` и API-сервер |
| **Распределение** | Только на узле, где размещены файлы | На всех узлах кластера (или выбранных) |
| **Use cases** | Компоненты control plane | Агенты мониторинга, сетевые плагины |
| **Scheduler** | Игнорируется (прямое размещение) | Игнорируется (Node Affinity) |
| **Редактирование** | Через файлы на узле | Через `kubectl` или API-сервер |
| **Видимость в API** | Зеркальные объекты (read-only) | Обычные объекты (read-write) |

---

## Когда использовать Static Pods?

**Используйте Static Pods для:**
- ✅ Развертывания компонентов control plane
- ✅ Системных компонентов на конкретном узле
- ✅ Сценариев без кластера (изолированные узлы)

**Не используйте Static Pods для:**
- ❌ Приложений (используйте Deployment)
- ❌ Агентов на всех узлах (используйте DaemonSet)
- ❌ Ресурсов, требующих контроллеров

---

## Когда использовать DaemonSets?

**Используйте DaemonSets для:**
- ✅ Агентов мониторинга на всех узлах
- ✅ Сетевых плагинов на всех узлах
- ✅ Сборщиков логов на всех узлах

**Не используйте DaemonSets для:**
- ❌ Компонентов control plane (используйте Static Pods)
- ❌ Приложений (используйте Deployment)

---

## Общие черты

**Оба типа Pod'ов:**
- ✅ Игнорируются kube-scheduler
- ✅ Размещаются напрямую (Static Pods через файлы, DaemonSets через Node Affinity)
- ✅ Автоматически перезапускаются при сбое
- ✅ Могут работать без участия других компонентов (Static Pods полностью, DaemonSets частично)

---

</details>

<details>
<summary><b>🛠️Практические примеры</b></summary>

---

## Пример 1: Простой Static Pod

**Файл: `/etc/kubernetes/manifests/simple-pod.yaml`**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: simple-app
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
```

**После создания файла:**
```bash
# Проверка через Docker
docker ps | grep nginx

# Проверка в кластере (если узел в кластере)
kubectl get pods | grep simple-app
```

---

## Пример 2: Static Pod с volumes

**Файл: `/etc/kubernetes/manifests/app-with-volume.yaml`**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: app-with-storage
spec:
  containers:
  - name: app
    image: myapp:latest
    volumeMounts:
    - name: data
      mountPath: /data
  volumes:
  - name: data
    hostPath:
      path: /var/lib/app-data
      type: DirectoryOrCreate
```

---

## Пример 3: Static Pod с ресурсами

**Файл: `/etc/kubernetes/manifests/resource-pod.yaml`**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: resource-app
spec:
  containers:
  - name: app
    image: myapp:latest
    resources:
      requests:
        memory: "256Mi"
        cpu: "200m"
      limits:
        memory: "512Mi"
        cpu: "500m"
```

---

## Пример 4: Static Pod с несколькими контейнерами

**Файл: `/etc/kubernetes/manifests/multi-container.yaml`**

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: multi-container-app
spec:
  containers:
  - name: app
    image: myapp:latest
  - name: sidecar
    image: sidecar:latest
```

---

</details>

<details>
<summary><b>🔧Проверка и отладка</b></summary>

---

## Проверка Static Pod через Docker

**На узле (без API-сервера):**

```bash
# Просмотр всех контейнеров
docker ps

# Фильтрация по имени Pod'а
docker ps | grep my-static-pod

# Просмотр логов
docker logs <container-id>

# Проверка состояния контейнера
docker inspect <container-id>
```

---

## Проверка Static Pod в кластере

**В кластере (через kubectl):**

```bash
# Просмотр Pod'ов (включая зеркальные объекты)
kubectl get pods -A

# Детальная информация
kubectl describe pod my-pod-node01

# Проверка аннотаций
kubectl get pod my-pod-node01 -o yaml | grep config.source

# Вывод:
# kubernetes.io/config.source: file
```

---

## Проверка конфигурации kubelet

```bash
# Проверка параметра pod-manifest-path
systemctl cat kubelet | grep pod-manifest-path

# Проверка параметра --config
systemctl cat kubelet | grep -- --config

# Проверка конфигурационного файла
cat /var/lib/kubelet/config.yaml | grep staticPodPath
```

---

## Отладка проблем

### Проблема 1: Static Pod не создается

**Проверьте:**
1. Существует ли директория манифестов?
2. Правильный ли путь указан в конфигурации kubelet?
3. Корректный ли формат YAML файла?
4. Есть ли права на чтение файла?

```bash
# Проверка директории
ls -la /etc/kubernetes/manifests/

# Проверка синтаксиса YAML
kubectl apply --dry-run=client -f /etc/kubernetes/manifests/my-pod.yaml
```

### Проблема 2: Static Pod не перезапускается

**Проверьте:**
1. Работает ли kubelet?
2. Есть ли ошибки в логах kubelet?

```bash
# Проверка статуса kubelet
systemctl status kubelet

# Просмотр логов kubelet
journalctl -u kubelet -f
```

### Проблема 3: Зеркальный объект не появляется в API

**Проверьте:**
1. Является ли узел частью кластера?
2. Доступен ли API-сервер?
3. Работает ли kubelet?

```bash
# Проверка подключения к API-серверу
kubectl cluster-info

# Проверка статуса kubelet
systemctl status kubelet
```

---

</details>

<details>
<summary><b>💡Лучшие практики</b></summary>

---

## Рекомендации по использованию Static Pods

### 1. Используйте для компонентов control plane

**Правильно:**
- kube-apiserver
- etcd
- kube-controller-manager
- kube-scheduler

**Неправильно:**
- Приложения (используйте Deployment)
- Агенты на всех узлах (используйте DaemonSet)

---

### 2. Храните манифесты в версионном контроле

```bash
# Используйте Git для управления манифестами
cd /etc/kubernetes/manifests
git init
git add *.yaml
git commit -m "Initial control plane manifests"
```

**Преимущества:**
- Отслеживание изменений
- Возможность отката
- Документирование конфигурации

---

### 3. Используйте правильные пути к сертификатам

```yaml
volumeMounts:
- name: certs
  mountPath: /etc/kubernetes/pki
volumes:
- name: certs
  hostPath:
    path: /etc/kubernetes/pki
```

**Почему:**
- Компоненты control plane требуют сертификатов
- Убедитесь, что пути к сертификатам корректны

---

### 4. Проверяйте синтаксис перед сохранением

```bash
# Проверка синтаксиса YAML
kubectl apply --dry-run=client -f my-pod.yaml

# Или используйте yamllint
yamllint my-pod.yaml
```

**Почему:**
- Ошибки в YAML могут привести к неработающим Pod'ам
- Лучше найти ошибки до сохранения

---

### 5. Мониторьте логи kubelet

```bash
# Просмотр логов kubelet
journalctl -u kubelet -f

# Или через systemd
systemctl status kubelet
```

**Почему:**
- kubelet показывает ошибки при создании Static Pods
- Логи помогают диагностировать проблемы

---

## Типичные ошибки

### Ошибка 1: Неправильный путь к директории манифестов

**Проблема:**
- Static Pod не создается

**Решение:**
- Проверьте конфигурацию kubelet
- Убедитесь, что путь указан правильно

### Ошибка 2: Ошибка синтаксиса YAML

**Проблема:**
- Static Pod не создается или работает некорректно

**Решение:**
- Проверьте синтаксис YAML перед сохранением
- Используйте `kubectl apply --dry-run`

### Ошибка 3: Попытка создать не-Pod ресурс

**Проблема:**
- Размещение Deployment или Service в директории Static Pods

**Решение:**
- Помните: Static Pods поддерживают только Pod'ы
- Используйте другие механизмы для других ресурсов

---

</details>

---

## Резюме

✅ **Static Pods** — Pod'ы, созданные kubelet напрямую из файлов манифестов без участия API-сервера

✅ **Ключевые особенности:**
- Создаются kubelet из файлов манифестов
- Не требуют API-сервера, scheduler'а или etcd
- Автоматически перезапускаются при сбое
- Автоматически обновляются при изменении файлов

✅ **Настройка:**
- Параметр `--pod-manifest-path` в kubelet.service
- Параметр `staticPodPath` в конфигурационном файле kubelet
- Стандартный путь: `/etc/kubernetes/manifests`

✅ **Use cases:**
- Развертывание компонентов control plane (kube-apiserver, etcd, и т.д.)
- Системные компоненты на конкретном узле
- Изолированные узлы без кластера

✅ **Static Pods vs DaemonSets:**
- Static Pods: для компонентов control plane, управление через файлы
- DaemonSets: для агентов на всех узлах, управление через API-сервер

✅ **Лучшие практики:**
- Используйте для компонентов control plane
- Храните манифесты в версионном контроле
- Проверяйте синтаксис перед сохранением
- Мониторьте логи kubelet

---