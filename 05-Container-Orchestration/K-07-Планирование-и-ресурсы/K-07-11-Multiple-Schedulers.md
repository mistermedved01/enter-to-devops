[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🎯 K-07-Планирование-и-ресурсы](../../README.md#-k-07-планирование-и-ресурсы)

---

# 🔄K-07-11-Multiple-Schedulers
>Multiple Schedulers — использование нескольких планировщиков в кластере Kubernetes, включая кастомные планировщики для специфических требований к размещению Pod'ов

---

<details>
<summary><b>🔍Что такое Multiple Schedulers?</b></summary>

---

## Концепция Multiple Schedulers

**Multiple Schedulers** — это возможность использовать несколько планировщиков одновременно в одном кластере Kubernetes.

**Ключевые особенности:**
- ✅ Kubernetes поддерживает несколько планировщиков одновременно
- ✅ Каждый Pod может указать, какой планировщик его должен разместить
- ✅ Дефолтный планировщик — `default-scheduler`
- ✅ Можно создать кастомные планировщики для специфических задач

---

## Зачем нужны Multiple Schedulers?

**Проблема:** Дефолтный планировщик Kubernetes использует общие алгоритмы, которые могут не подходить для всех случаев.

```
┌─────────────────────────────────────────────────────────┐
│              Ограничения дефолтного планировщика        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Дефолтный планировщик:                                  │
│  ✅ Учитывает ресурсы (CPU, память)                     │
│  ✅ Учитывает Node Affinity                              │
│  ✅ Учитывает Taints и Tolerations                      │
│  ❌ Не учитывает специфические метрики                  │
│  ❌ Не может использовать внешние данные                │
│  ❌ Не может применять кастомные алгоритмы              │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Решение:** Кастомные планировщики позволяют:
- ✅ Использовать специфические алгоритмы планирования
- ✅ Учитывать внешние метрики (например, стоимость ресурсов)
- ✅ Применять бизнес-логику при размещении
- ✅ Оптимизировать для конкретных workload'ов

---

## Когда использовать Multiple Schedulers?

**Сценарии использования:**

1. **Специфические алгоритмы планирования**
   - Размещение на основе стоимости ресурсов
   - Учет географического расположения
   - Оптимизация для ML/AI workload'ов

2. **Интеграция с внешними системами**
   - Использование данных из мониторинга
   - Учет метрик производительности
   - Интеграция с системами управления ресурсами

3. **Тестирование и разработка**
   - Тестирование новых алгоритмов планирования
   - A/B тестирование стратегий размещения
   - Эксперименты с оптимизацией

> ⚠️ **Важно:** Кастомные планировщики требуют разработки и поддержки. Используйте их только если дефолтный планировщик не решает ваши задачи.

---

</details>

<details>
<summary><b>🏗️Архитектура Multiple Schedulers</b></summary>

---

## Как работают Multiple Schedulers?

```
┌─────────────────────────────────────────────────────────┐
│              Архитектура Multiple Schedulers            │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────┐                                    │
│  │  API Server     │                                    │
│  └────────┬────────┘                                    │
│           │                                             │
│           ├──────────────────┬──────────────────┐       │
│           │                  │                  │       │
│           ▼                  ▼                  ▼       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   default-   │  │   custom-    │  │   ml-        │ │
│  │  scheduler   │  │  scheduler   │  │  scheduler   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│           │                  │                  │       │
│           └──────────────────┴──────────────────┘       │
│                          │                              │
│                          ▼                              │
│                  ┌──────────────┐                      │
│                  │  Worker Nodes │                      │
│                  └──────────────┘                      │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Процесс:**
1. Pod создается с указанием `schedulerName`
2. API Server сохраняет Pod в etcd
3. Все планировщики мониторят Pod'ы
4. Только планировщик с соответствующим именем обрабатывает Pod
5. Планировщик назначает Pod на узел

---

## Поле schedulerName в Pod

Каждый Pod может указать, какой планировщик должен его разместить:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  schedulerName: default-scheduler  # Дефолтный планировщик
  containers:
  - name: app
    image: myapp:latest
```

**Если `schedulerName` не указан:**
- Используется дефолтный планировщик (`default-scheduler`)

**Если указан кастомный планировщик:**
- Только этот планировщик будет обрабатывать Pod
- Если планировщик не запущен → Pod останется в `Pending`

---

## Дефолтный планировщик

**Имя:** `default-scheduler`

**Особенности:**
- ✅ Запускается автоматически в кластере
- ✅ Обрабатывает Pod'ы без указанного `schedulerName`
- ✅ Использует стандартные алгоритмы Kubernetes

**Пример Pod без указания планировщика:**
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: default-pod
spec:
  # schedulerName не указан → используется default-scheduler
  containers:
  - name: app
    image: myapp:latest
```

---

</details>

<details>
<summary><b>🔧Создание кастомного планировщика</b></summary>

---

## Что такое кастомный планировщик?

**Кастомный планировщик** — это приложение, которое:
- ✅ Подключается к Kubernetes API
- ✅ Мониторит Pod'ы с определенным `schedulerName`
- ✅ Применяет кастомные алгоритмы планирования
- ✅ Назначает Pod'ы на узлы через API

---

## Архитектура кастомного планировщика

```
┌─────────────────────────────────────────────────────────┐
│          Компоненты кастомного планировщика             │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────┐              │
│  │   Custom Scheduler                    │              │
│  │                                        │              │
│  │  ┌────────────────────────────────┐  │              │
│  │  │  Watch Pods (schedulerName)    │  │              │
│  │  └────────────────────────────────┘  │              │
│  │              │                         │              │
│  │              ▼                         │              │
│  │  ┌────────────────────────────────┐  │              │
│  │  │  Filter Nodes                  │  │              │
│  │  └────────────────────────────────┘  │              │
│  │              │                         │              │
│  │              ▼                         │              │
│  │  ┌────────────────────────────────┐  │              │
│  │  │  Score Nodes (Custom Logic)    │  │              │
│  │  └────────────────────────────────┘  │              │
│  │              │                         │              │
│  │              ▼                         │              │
│  │  ┌────────────────────────────────┐  │              │
│  │  │  Bind Pod to Node              │  │              │
│  │  └────────────────────────────────┘  │              │
│  └──────────────────────────────────────┘              │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Простой пример кастомного планировщика (Go)

```go
package main

import (
    "context"
    "fmt"
    "time"
    
    v1 "k8s.io/api/core/v1"
    metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
    "k8s.io/client-go/kubernetes"
    "k8s.io/client-go/tools/clientcmd"
)

func main() {
    // Подключение к кластеру
    config, _ := clientcmd.BuildConfigFromFlags("", "/path/to/kubeconfig")
    clientset, _ := kubernetes.NewForConfig(config)
    
    // Мониторинг Pod'ов с schedulerName: "my-custom-scheduler"
    watch, _ := clientset.CoreV1().Pods("").Watch(context.TODO(), metav1.ListOptions{
        FieldSelector: "spec.schedulerName=my-custom-scheduler,status.phase=Pending",
    })
    
    for event := range watch.ResultChan() {
        pod := event.Object.(*v1.Pod)
        
        // Получение списка узлов
        nodes, _ := clientset.CoreV1().Nodes().List(context.TODO(), metav1.ListOptions{})
        
        // Простой алгоритм: выбираем первый доступный узел
        for _, node := range nodes.Items {
            if isNodeAvailable(&node) {
                bindPodToNode(clientset, pod, node.Name)
                break
            }
        }
    }
}

func isNodeAvailable(node *v1.Node) bool {
    // Проверка готовности узла
    for _, condition := range node.Status.Conditions {
        if condition.Type == v1.NodeReady && condition.Status != v1.ConditionTrue {
            return false
        }
    }
    return true
}

func bindPodToNode(clientset *kubernetes.Clientset, pod *v1.Pod, nodeName string) {
    binding := &v1.Binding{
        ObjectMeta: metav1.ObjectMeta{
            Name: pod.Name,
        },
        Target: v1.ObjectReference{
            Kind: "Node",
            Name: nodeName,
        },
    }
    
    err := clientset.CoreV1().Pods(pod.Namespace).Bind(context.TODO(), binding, metav1.CreateOptions{})
    if err != nil {
        fmt.Printf("Error binding pod: %v\n", err)
    } else {
        fmt.Printf("Pod %s bound to node %s\n", pod.Name, nodeName)
    }
}
```

---

## Развертывание кастомного планировщика

Кастомный планировщик можно развернуть как:
- ✅ Deployment в кластере
- ✅ DaemonSet (если нужен на каждом узле)
- ✅ Статический Pod (для control plane)

**Пример Deployment для кастомного планировщика:**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: custom-scheduler
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: custom-scheduler
  template:
    metadata:
      labels:
        app: custom-scheduler
    spec:
      serviceAccountName: custom-scheduler
      containers:
      - name: scheduler
        image: my-custom-scheduler:latest
        command:
        - /custom-scheduler
        - --kubeconfig=/etc/kubernetes/scheduler.conf
        volumeMounts:
        - name: kubeconfig
          mountPath: /etc/kubernetes
          readOnly: true
      volumes:
      - name: kubeconfig
        configMap:
          name: scheduler-kubeconfig
```

---

## ServiceAccount и RBAC

Кастомный планировщик нуждается в правах для:
- ✅ Чтения Pod'ов
- ✅ Чтения Node'ов
- ✅ Создания Binding объектов

**Пример ServiceAccount и ClusterRole:**

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: custom-scheduler
  namespace: kube-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: custom-scheduler
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["pods/binding"]
  verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: custom-scheduler
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: custom-scheduler
subjects:
- kind: ServiceAccount
  name: custom-scheduler
  namespace: kube-system
```

---

</details>

<details>
<summary><b>📝Использование кастомного планировщика</b></summary>

---

## Указание планировщика в Pod

Чтобы использовать кастомный планировщик, укажите `schedulerName` в спецификации Pod:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  schedulerName: my-custom-scheduler  # Имя кастомного планировщика
  containers:
  - name: app
    image: myapp:latest
```

---

## Пример: Pod с дефолтным планировщиком

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: default-pod
spec:
  schedulerName: default-scheduler  # Явное указание дефолтного
  containers:
  - name: app
    image: myapp:latest
```

**Или без указания:**
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: default-pod
spec:
  # schedulerName не указан → используется default-scheduler
  containers:
  - name: app
    image: myapp:latest
```

---

## Пример: Pod с кастомным планировщиком

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: custom-pod
spec:
  schedulerName: my-custom-scheduler
  containers:
  - name: app
    image: myapp:latest
    resources:
      requests:
        memory: "1Gi"
        cpu: "1"
```

---

## Использование в Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      schedulerName: my-custom-scheduler  # Кастомный планировщик
      containers:
      - name: app
        image: myapp:latest
```

---

## Проверка планировщика Pod'а

**Посмотреть, какой планировщик назначен Pod'у:**

```bash
kubectl get pod my-pod -o jsonpath='{.spec.schedulerName}'
```

**Вывод:**
```
my-custom-scheduler
```

**Посмотреть все Pod'ы с определенным планировщиком:**

```bash
kubectl get pods --all-namespaces -o json | \
  jq '.items[] | select(.spec.schedulerName=="my-custom-scheduler") | .metadata.name'
```

---

## Отладка кастомного планировщика

**Проверить логи планировщика:**

```bash
kubectl logs -n kube-system deployment/custom-scheduler
```

**Проверить события Pod'а:**

```bash
kubectl describe pod my-pod
```

**Проверить, запущен ли планировщик:**

```bash
kubectl get pods -n kube-system | grep scheduler
```

---

</details>

<details>
<summary><b>🎯Практические примеры</b></summary>

---

## Пример 1: Планировщик для ML workload'ов

**Задача:** Размещать ML Pod'ы на узлах с GPU.

**Кастомный планировщик:**
```go
func scheduleMLPod(pod *v1.Pod, nodes []v1.Node) string {
    for _, node := range nodes {
        // Проверка наличия GPU
        if hasGPU(&node) {
            return node.Name
        }
    }
    return ""  // Не найден подходящий узел
}

func hasGPU(node *v1.Node) bool {
    labels := node.Labels
    return labels["accelerator"] == "nvidia-tesla-k80"
}
```

**Pod с ML планировщиком:**
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ml-training
spec:
  schedulerName: ml-scheduler
  containers:
  - name: training
    image: ml-training:latest
    resources:
      requests:
        nvidia.com/gpu: 1
```

---

## Пример 2: Планировщик на основе стоимости

**Задача:** Размещать Pod'ы на самых дешевых узлах.

**Кастомный планировщик:**
```go
func scheduleByCost(pod *v1.Pod, nodes []v1.Node) string {
    cheapestNode := ""
    minCost := math.MaxFloat64
    
    for _, node := range nodes {
        cost := getNodeCost(&node)
        if cost < minCost && isNodeAvailable(&node) {
            minCost = cost
            cheapestNode = node.Name
        }
    }
    
    return cheapestNode
}

func getNodeCost(node *v1.Node) float64 {
    // Получение стоимости из меток узла
    costStr := node.Labels["node-cost"]
    cost, _ := strconv.ParseFloat(costStr, 64)
    return cost
}
```

**Pod с cost-aware планировщиком:**
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: cost-optimized-pod
spec:
  schedulerName: cost-scheduler
  containers:
  - name: app
    image: myapp:latest
```

---

## Пример 3: Планировщик для multi-zone размещения

**Задача:** Распределять Pod'ы равномерно по зонам.

**Кастомный планировщик:**
```go
func scheduleMultiZone(pod *v1.Pod, nodes []v1.Node) string {
    zoneCounts := make(map[string]int)
    
    // Подсчет Pod'ов в каждой зоне
    for _, node := range nodes {
        zone := node.Labels["topology.kubernetes.io/zone"]
        zoneCounts[zone] = getPodCountInZone(zone)
    }
    
    // Выбор зоны с минимальным количеством Pod'ов
    minZone := ""
    minCount := math.MaxInt
    
    for zone, count := range zoneCounts {
        if count < minCount {
            minCount = count
            minZone = zone
        }
    }
    
    // Выбор узла в выбранной зоне
    for _, node := range nodes {
        if node.Labels["topology.kubernetes.io/zone"] == minZone {
            return node.Name
        }
    }
    
    return ""
}
```

---

</details>

<details>
<summary><b>⚙️Конфигурация и настройка</b></summary>

---

## Конфигурация дефолтного планировщика

Дефолтный планировщик можно настроить через:
- ✅ Command-line аргументы
- ✅ ConfigMap
- ✅ Policy файлы

**Пример ConfigMap для дефолтного планировщика:**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: scheduler-config
  namespace: kube-system
data:
  scheduler-config.yaml: |
    apiVersion: kubescheduler.config.k8s.io/v1
    kind: KubeSchedulerConfiguration
    profiles:
    - schedulerName: default-scheduler
      plugins:
        score:
          enabled:
          - name: NodeResourcesFit
            weight: 1
          - name: NodeAffinity
            weight: 1
```

---

## Запуск дефолтного планировщика с параметрами

**В kubeadm кластере:**

```yaml
apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
scheduler:
  extraArgs:
    config: /etc/kubernetes/scheduler-config.yaml
```

**Вручную:**

```bash
kube-scheduler \
  --kubeconfig=/etc/kubernetes/scheduler.conf \
  --config=/etc/kubernetes/scheduler-config.yaml \
  --v=2
```

---

## Мониторинг планировщиков

**Метрики планировщика:**

```bash
# Метрики дефолтного планировщика
curl http://localhost:10259/metrics

# Метрики кастомного планировщика (если экспортирует)
curl http://custom-scheduler:8080/metrics
```

**Основные метрики:**
- `scheduler_schedule_attempts_total` — количество попыток планирования
- `scheduler_schedule_attempts_result` — результат планирования
- `scheduler_scheduling_duration_seconds` — время планирования

---

</details>

<details>
<summary><b>⚠️Проблемы и решения</b></summary>

---

## Проблема 1: Pod остается в Pending

**Симптомы:**
```bash
kubectl get pods
NAME        READY   STATUS    RESTARTS   AGE
my-pod      0/1     Pending   0          5m
```

**Причины:**
- ❌ Кастомный планировщик не запущен
- ❌ Неправильное имя планировщика в `schedulerName`
- ❌ Планировщик не имеет прав для создания Binding
- ❌ Нет подходящих узлов

**Решение:**

1. **Проверить, запущен ли планировщик:**
```bash
kubectl get pods -n kube-system | grep scheduler
```

2. **Проверить имя планировщика:**
```bash
kubectl get pod my-pod -o jsonpath='{.spec.schedulerName}'
```

3. **Проверить логи планировщика:**
```bash
kubectl logs -n kube-system deployment/custom-scheduler
```

4. **Проверить права планировщика:**
```bash
kubectl get clusterrolebinding custom-scheduler -o yaml
```

---

## Проблема 2: Планировщик не видит Pod'ы

**Симптомы:**
- Планировщик запущен, но не обрабатывает Pod'ы

**Причины:**
- ❌ Неправильный фильтр в Watch
- ❌ Проблемы с подключением к API Server
- ❌ Неправильный kubeconfig

**Решение:**

1. **Проверить подключение к API:**
```bash
kubectl --kubeconfig=/path/to/kubeconfig get nodes
```

2. **Проверить фильтр Watch:**
```go
watch, err := clientset.CoreV1().Pods("").Watch(context.TODO(), metav1.ListOptions{
    FieldSelector: "spec.schedulerName=my-custom-scheduler,status.phase=Pending",
})
```

3. **Проверить логи:**
```bash
kubectl logs -n kube-system deployment/custom-scheduler
```

---

## Проблема 3: Конфликт между планировщиками

**Симптомы:**
- Pod обрабатывается несколькими планировщиками
- Непредсказуемое поведение

**Причина:**
- ❌ Несколько планировщиков с одинаковым именем
- ❌ Планировщик обрабатывает Pod'ы с другим `schedulerName`

**Решение:**

1. **Убедиться, что имена уникальны:**
```yaml
spec:
  schedulerName: my-unique-scheduler-name  # Уникальное имя
```

2. **Проверить фильтры в планировщике:**
```go
// Правильный фильтр
FieldSelector: "spec.schedulerName=my-custom-scheduler,status.phase=Pending"
```

3. **Проверить количество реплик:**
```yaml
spec:
  replicas: 1  # Только одна реплика планировщика
```

---

## Проблема 4: Производительность кастомного планировщика

**Симптомы:**
- Медленное планирование Pod'ов
- Высокая нагрузка на API Server

**Причины:**
- ❌ Неэффективные алгоритмы
- ❌ Слишком частые запросы к API
- ❌ Отсутствие кэширования

**Решение:**

1. **Кэшировать данные узлов:**
```go
nodeCache := make(map[string]*v1.Node)
// Обновлять кэш периодически, а не при каждом планировании
```

2. **Использовать индексы:**
```go
// Индексировать узлы по меткам для быстрого поиска
```

3. **Оптимизировать алгоритмы:**
```go
// Использовать эффективные структуры данных
// Минимизировать количество итераций
```

---

</details>

<details>
<summary><b>✅Best Practices</b></summary>

---

## Рекомендации по использованию Multiple Schedulers

### 1. Используйте дефолтный планировщик по умолчанию

✅ **Делайте:**
- Используйте `default-scheduler` для большинства Pod'ов
- Создавайте кастомные планировщики только при необходимости

❌ **Не делайте:**
- Не создавайте кастомные планировщики "на всякий случай"
- Не заменяйте дефолтный планировщик без веской причины

---

### 2. Правильное именование планировщиков

✅ **Делайте:**
- Используйте описательные имена: `ml-scheduler`, `cost-scheduler`
- Используйте префиксы для группировки: `team-a-scheduler`

❌ **Не делайте:**
- Не используйте общие имена: `scheduler`, `custom`
- Не используйте имена, конфликтующие с системными

---

### 3. Мониторинг и логирование

✅ **Делайте:**
- Логируйте все решения планировщика
- Экспортируйте метрики для мониторинга
- Отслеживайте время планирования

❌ **Не делайте:**
- Не игнорируйте ошибки планирования
- Не создавайте планировщики без логирования

---

### 4. Тестирование кастомных планировщиков

✅ **Делайте:**
- Тестируйте на тестовом кластере
- Используйте небольшое количество Pod'ов для начала
- Проверяйте edge cases

❌ **Не делайте:**
- Не развертывайте сразу в production
- Не тестируйте на критичных workload'ах

---

### 5. Безопасность и RBAC

✅ **Делайте:**
- Используйте минимальные необходимые права
- Используйте отдельный ServiceAccount для каждого планировщика
- Регулярно проверяйте права доступа

❌ **Не делайте:**
- Не давайте планировщику права cluster-admin
- Не используйте один ServiceAccount для всех планировщиков

---

### 6. Высокая доступность

✅ **Делайте:**
- Используйте Deployment с несколькими репликами
- Настройте health checks
- Используйте Pod Disruption Budgets

❌ **Не делайте:**
- Не используйте одиночные Pod'ы для планировщиков
- Не игнорируйте health checks

---

### 7. Документация

✅ **Делайте:**
- Документируйте алгоритмы планирования
- Описывайте, когда использовать планировщик
- Ведите changelog изменений

❌ **Не делайте:**
- Не создавайте планировщики без документации
- Не забывайте обновлять документацию

---

</details>

---

## Резюме

✅ **Multiple Schedulers** — возможность использовать несколько планировщиков одновременно в кластере Kubernetes

✅ **Ключевые особенности:**
- Каждый Pod может указать планировщик через `schedulerName`
- Дефолтный планировщик — `default-scheduler`
- Кастомные планировщики для специфических задач

✅ **Когда использовать:**
- Специфические алгоритмы планирования
- Интеграция с внешними системами
- Оптимизация для конкретных workload'ов

✅ **Создание кастомного планировщика:**
- Подключение к Kubernetes API
- Мониторинг Pod'ов с определенным `schedulerName`
- Применение кастомных алгоритмов
- Создание Binding объектов

✅ **Лучшие практики:**
- Используйте дефолтный планировщик по умолчанию
- Правильное именование планировщиков
- Мониторинг и логирование
- Тестирование перед production
- Минимальные права доступа (RBAC)
- Высокая доступность (несколько реплик)
- Документация алгоритмов

> 💡 **Совет:** Кастомные планировщики — мощный инструмент, но требуют разработки и поддержки. Используйте их только если дефолтный планировщик не решает ваши задачи.

---

