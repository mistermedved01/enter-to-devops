[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🎯 K-07-Планирование-и-ресурсы](../../README.md#-k-07-планирование-и-ресурсы)

---

# 🔄K-07-13-Admission-Controller-Types
>Типы контроллеров допуска: Validating (проверяющие) и Mutating (мутирующие), а также настройка собственных контроллеров через Webhook Admission Controllers

---

<details>
<summary><b>🔍Типы контроллеров допуска</b></summary>

---

## Два типа контроллеров допуска

Всего есть **два типа контроллеров допуска**:

1. **Mutating Admission Controllers (мутирующие контроллеры допуска)**
   - Могут изменить запрос
   - Модифицируют объект до его создания

2. **Validating Admission Controllers (проверяющие контроллеры допуска)**
   - Валидируют запрос
   - Могут разрешить или отклонить запрос

**Также контроллеры могут быть обоих видов одновременно:**
- Сначала что-то изменить в запросе
- Потом еще и проверить его

---

## Пример: Validating Admission Controller

**NamespaceExists** — это пример **проверяющего контроллера допуска** (Validating Admission Controller).

**Как работает:**
- Может помочь проверить, существует ли пространство имен
- Отклонить запрос, если оно не существует
- **Не может изменять запрос**, только валидировать

---

## Пример: Mutating Admission Controller

**DefaultStorageClass** — это пример **мутирующего контроллера допуска** (Mutating Admission Controller).

**Это Admission Controller включен по умолчанию.**

---

</details>

<details>
<summary><b>📋Пример: DefaultStorageClass (Mutating Controller)</b></summary>

---

## Как работает DefaultStorageClass

**Сценарий:** Мы отправляем запрос на создание PVC (PersistentVolumeClaim).

**Процесс:**

1. Запрос проходит аутентификацию
2. Запрос проходит авторизацию
3. Запрос попадает на контроллер допуска

**Что делает DefaultStorageClass:**

- Контроллер плагина будет отслеживать запрос на создание PVC
- Проверяет: упоминается ли в нем `storageClass`?
- Если нет (что верно в нашем случае), он **изменит наш запрос**
- Сам добавит класс хранения по умолчанию к нашему манифесту

---

## Настройка Default StorageClass

**Важно:**
- Любой StorageClass может быть настроен в качестве дефолтного в нашем кластере
- Но для начала нам его придется создать
- **Без созданного дефолтного класса это работать не будет**

---

## Пример манифеста PVC

**Манифест без указания storageClass:**

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName не указан
```

**После обработки DefaultStorageClass:**

Когда PVC создан, мы проверим его и увидим, что к нему добавился класс хранения `standard`, хотя мы ничего об этом не упоминали в своем манифесте:

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # Добавлено автоматически!
```

**Проверка:**

```bash
kubectl get pvc my-pvc -o yaml
```

**Вывод покажет, что `storageClassName: standard` был добавлен автоматически.**

---

## Вывод

**DefaultStorageClass** представляет собой другой тип контроллера допуска, известный как **мутирующий контроллер допуска** (Mutating Admission Controller).

Он может изменить или мутировать сам объект до его создания.

---

</details>

<details>
<summary><b>⚙️Порядок применения контроллеров</b></summary>

---

## Последовательность выполнения

**Что касается порядка применения контроллеров:**

1. **Сначала вызывается мутирующий контроллер допуска**
2. **Затем проверяющие контроллеры**

**Почему именно так?**

Это делается для того, чтобы любое изменение, сделанное контроллером, могло быть учтено в процессе проверки.

---

## Пример порядка выполнения

**В этом примере:**

1. Сначала запускается контроллер допуска **NamespaceAutoProvision**, который является **мутирующим**
   - Он создает namespace, если его не существует

2. Затем запускаются **проверяющие контроллеры**, например **NamespaceExists**
   - Он проверяет существование namespace'а

**Что было бы, если порядок был другой?**

Если бы это было запущено по-другому, то **NamespaceExists** контроллер всегда бы отклонял запрос, так как namespace еще не существует, а контроллер **NamespaceAutoProvision** для создания отсутствующего namespace никогда бы не вызвался.

---

## Процесс обработки запроса

```
┌─────────────────────────────────────────────────────────┐
│          Порядок выполнения Admission Controllers       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Запрос к API Server                                    │
│           │                                              │
│           ▼                                              │
│  1. Mutating Admission Controllers                      │
│     (модифицируют объект)                               │
│     └─ NamespaceAutoProvision                           │
│        (создает namespace, если не существует)          │
│           │                                              │
│           ▼                                              │
│  2. Validating Admission Controllers                    │
│     (валидируют объект)                                 │
│     └─ NamespaceExists                                  │
│        (проверяет существование namespace'а)           │
│           │                                              │
│           ▼                                              │
│  Сохранение в etcd                                      │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Обработка отклонений

**Наш запрос проходит через все включенные контроллеры допуска.**

**Если какой-либо из них отклоняет запрос:**
- Работа останавливается
- Пользователю отправляется сообщение об ошибке

---

</details>

<details>
<summary><b>🌐Webhook Admission Controllers</b></summary>

---

## Встроенные vs. Кастомные контроллеры

**Все рассмотренные контроллеры являются встроенными контроллерами допуска:**
- Они являются частью исходного кода Kubernetes
- Они компилируются и поставляются вместе с Kubernetes

**Но что если нам нужен собственный контроллер допуска?**
- С нашими собственными мутациями и проверками
- Которые имеют собственную логику

---

## Решение: Webhook Admission Controllers

**Для поддержки внешних контроллеров допуска доступны два особенных Admission Controllers:**

1. **MutatingAdmissionWebhook**
2. **ValidatingAdmissionWebhook**

Эти специальные контроллеры называются **Webhook Admission Controllers**.

---

## Как работают Webhook'и?

**Процесс:**

1. Запрос успешно проходит через все встроенные контроллеры допуска
2. Затем он инициирует настроенный webhook
3. Webhook выполняет вызов веб-сервера
4. Передает для проверки объект с типом `AdmissionReview` в формате JSON

**Объект AdmissionReview содержит:**
- Пользователь, сделавший запрос
- Тип операции, который пользователь пытается выполнить
- Какой объект
- Сведения о самом объекте

**При получении запроса:**
- Admission Webhook сервер отвечает объектом такого же типа `AdmissionReview` с результатом
- Разрешен ли этот запрос или нет

**Если в ответе сервера в секции `response` поле `allowed` имеет значение `true`:**
- Запрос разрешается

**Если он установлен `false`:**
- Запрос отклоняется

---

## Схема работы Webhook

```
┌─────────────────────────────────────────────────────────┐
│              Схема работы Webhook Admission Controller  │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Запрос проходит встроенные контроллеры             │
│           │                                              │
│           ▼                                              │
│  2. Webhook инициируется                                │
│           │                                              │
│           ▼                                              │
│  3. Вызов веб-сервера                                   │
│     Передача объекта AdmissionReview (JSON)            │
│           │                                              │
│           ▼                                              │
│  4. Веб-сервер обрабатывает запрос                      │
│     (наша логика)                                        │
│           │                                              │
│           ▼                                              │
│  5. Ответ AdmissionReview                                │
│     response.allowed: true/false                         │
│           │                                              │
│           ├─ true → Запрос разрешен ✅                  │
│           └─ false → Запрос отклонен ❌                 │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Размещение веб-сервера

**Мы можем настроить эти webhook'и так, чтобы они указывали на сервер, размещенный:**

1. **В самом кластере Kubernetes**
   - В виде Deployment
   - С собственной службой (Service)

2. **За пределами кластера**
   - На физическом или виртуальном сервере
   - В облаке (serverless код)

**Наш сервер будет иметь собственную службу для webhook, работающую с нашим собственным кодом и логикой.**

---

</details>

<details>
<summary><b>🔧Настройка Webhook Admission Controller</b></summary>

---

## Шаг 1: Развернуть веб-сервер

**Первый шаг:** Развернуть наш собственный веб-сервер.

**Это может быть:**
- Сервер API, который можно построить на любой платформе
- Пример кода веб-сервера со страниц документации Kubernetes написан на языке программирования Go
- При необходимости можно разработать свой собственный сервер на других языках
- Или поискать что-то готовое

**Единственное требование:**
- Он должен принимать HTTP вызовы `mutate` и `validate`
- Отвечать на них объектом JSON такого вида, который ожидает Kubernetes

---

## Пример: Python реализация

**Вот пример процедур Python программы, реализующие этот API:**

### Validating Webhook

```python
def validate_pod(request):
    # Получает запрос от webhook проверки
    # Проверяет: есть ли в спецификации шаблона Pod'а readiness probe?
    
    if not has_readiness_probe(request):
        # Если probe отсутствует, отправляет обратно JSON с allowed в значении false
        # Что фактически отклоняет запрос
        return {
            "response": {
                "allowed": False,
                "status": {
                    "message": "Pod must have readiness probe"
                }
            }
        }
    
    return {
        "response": {
            "allowed": True
        }
    }
```

**Это просто пример использования, написанный на Python. Чтобы он заработал, нам нужно дописать остальное, но он наглядно показывает, что мы можем делать с поступающими запросами.**

---

### Mutating Webhook

**Если посмотреть ниже, мы увидим мутирующий webhook:**

```python
def mutate_pod(request):
    # Получает имя пользователя
    # Отвечает операцией patch для исправления JSON
    # Добавляя имя пользователя в качестве метки к любому полученному запросу
    
    username = request.user_info.username
    
    # Объект patch представляет собой список объектов для операции исправления
    patch = [
        {
            "op": "add",  # Тип операции
            "path": "/metadata/labels/user",  # Путь в объекте JSON
            "value": username  # Значение, которое необходимо добавить
        }
    ]
    
    # Затем patch кодируется как base64
    patch_base64 = base64.b64encode(json.dumps(patch).encode()).decode()
    
    return {
        "response": {
            "allowed": True,
            "patch": patch_base64,
            "patchType": "JSONPatch"
        }
    }
```

---

## Операции JSON Patch

**Если ты внимательно посмотришь на этот конкретный фрагмент кода:**

- Объект `patch` представляет собой список объектов для операции исправления
- Тип самой операции описан полем `op`
- Операции имеют такой же смысл, как и в JSON Patch

**Каждая операция может:**
- `add` — добавлять
- `remove` — удалять
- `replace` — заменять
- `copy` — копировать
- `move` — перемещать
- `test` — тестировать

**За операцией мы указываем:**
- Путь в объекте JSON, куда нацелена эта операция
- В данном случае это `/metadata/labels/user`
- Последнее — это значение, которое необходимо будет добавить

**Так как мы указали операцию `add` в этом случае, так что это будет значение для метки `user`, добавленное в metadata.**

Затем patch кодируется как base64, добавляется в response и отправляется обратно.

---

## Важное замечание

**Если тебе не все понятно с точки зрения языка, не беспокойся:**
- На экзаменах тебя не попросят разработать какой-либо код
- Здесь главное понять принцип работы
- И что примерно нужно сделать для реализации своих кастомных Admission Controllers

**Все, что нам нужно:**
- Подготовить веб-сервер допуска
- То есть сервер, который содержит логику или код для разрешения или отклонения запроса
- Он должен иметь возможность получать и отвечать соответствующими ответами, которые ожидают webhook на стороне Kubernetes

---

## Шаг 2: Размещение веб-сервера

**После того как мы разработали собственный веб-сервер, следующим шагом будет его размещение.**

**Мы можем:**
1. **Захостить его где-нибудь** как физический или виртуальный сервер
2. **Разместить serverless код в облаке**
3. **Поместить в контейнер и развернуть в кластере Kubernetes как Deployment**

**Если он поднят в виде Deployment:**
- Для доступа к нему потребуется служба (Service)
- У нас есть служба с названием `hook-service`

---

</details>

<details>
<summary><b>⚙️Конфигурация Webhook в Kubernetes</b></summary>

---

## Шаг 3: Настройка кластера

**Следующим шагом является настройка нашего кластера:**
- Чтобы обратиться к службе
- И передать запрос для операции валидации или мутации

**Нам нужно создать объект конфигурации webhook.**

---

## Структура конфигурации

**У нас здесь знакомые разделы:**
- `apiVersion`
- `kind`
- `metadata`

**И не очень знакомый:**
- `webhooks`

---

## ValidatingWebhookConfiguration

**Мы начинаем с версии API для объекта конфигурации:**
- Она будет `admissionregistration.k8s.io/v1`

**Далее `kind`:**
- Это `ValidatingWebhookConfiguration`
- По аналогии для запроса к мутации: `MutatingWebhookConfiguration`

**Название:**
- Назовем его `k8s-cookbook-account`

---

## Раздел webhooks

**В разделе `webhooks` мы можем настроить несколько webhook'ов.**

**У каждого из них должно быть:**
- Имя
- Конфигурация клиента
- Набор правил

**Имя будет:** `k8s-cookbook-account`

**Как видишь, webhook'ов можно внести несколько.**

---

## Параметр timeoutSeconds

**Поскольку webhook'и увеличивают задержку запросов к API Server, есть параметр `timeoutSeconds`:**
- Который задает время ожидания ответа от сервера webhook'а
- По достижению этого таймаута вызов проваливается

---

## clientConfig: Размещение вне кластера

**В `clientConfig` мы настраиваем расположение нашего веб-сервера допуска.**

**Если мы самостоятельно развернули этот сервер где-то снаружи и он не выполняется внутри нашего кластера Kubernetes:**

```yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: k8s-cookbook-account
webhooks:
- name: k8s-cookbook-account
  clientConfig:
    url: https://external-webhook.example.com:8443/validate
    # Указали также порт, отличный от стандартного 443 порта для защищенного
  timeoutSeconds: 5
  ...
```

**После установления соединения запрос будет передан по этому URL.**

---

## clientConfig: Размещение в кластере

**Вариант, если выполняется в кластере:**

**Как ты помнишь, все соединения внутри кластера также шифрованы, поэтому для связи между сервером и сервером webhook'ов должен использоваться TLS.**

**Нам потребуется настроить пакет сертификатов, чтобы сервера верили друг другу:**
- Настроим созданный сертификат на admission webhook server
- Создадим цепочку сертификатов
- Положим в `clientConfig` под полем `caBundle`

**Теперь наш API Server будет доверять сертификату сервера webhook'ов.**

---

## Указание Service

**Если мы сделали это, как показано на схеме, то есть в кластере в виде Deployment и службы:**

**То нам следует указать данные службы:**
- А именно ее namespace
- Имя службы
- Которые в нашем случае это `hook-namespace` и `hook-service` соответственно

**Для выбора типа проверки мы укажем путь и порт, на котором слушает HTTP сервер webhook'ов:**

```yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: k8s-cookbook-account
webhooks:
- name: k8s-cookbook-account
  clientConfig:
    service:
      name: hook-service
      namespace: hook-namespace
      path: /validate
      port: 443
    caBundle: <base64-encoded-ca-cert>
  timeoutSeconds: 5
  ...
```

---

## Правила (rules)

**Далее мы должны указать, когда вызывать наш webhook.**

**Для этого выставим правило.**

**При обработке запросов:**
- Каким ресурсом мы хотим, чтобы наш веб-сервер с webhook'ами вызывался для проверки?

**Обычно нам не требуется делать это для всех вызовов, которые поступают в API Server.**

**Например, мы хотим, чтобы наш webhook вызывался:**
- Только при создании Pod'ов
- Или при удалении Pod'ов
- Или при создании Deployments

**Все параметры вызова можно описать в виде правила в разделе `rules`.**

---

## Пример правила

**В этом примере мы указали, что webhook, описанный этой конфигурацией, можно вызывать только при вызовах, которые пытаются создать Pod в любом namespace:**

```yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: k8s-cookbook-account
webhooks:
- name: k8s-cookbook-account
  clientConfig:
    service:
      name: hook-service
      namespace: hook-namespace
      path: /validate
      port: 443
    caBundle: <base64-encoded-ca-cert>
  timeoutSeconds: 5
  rules:
  - operations: ["CREATE"]
    apiGroups: [""]
    apiVersions: ["v1"]
    resources: ["pods"]
  admissionReviewVersions: ["v1"]
  sideEffects: None
  failurePolicy: Fail
```

**Параметры правила:**
- `apiGroups` — группы API
- `apiVersions` — версии API
- `type` — тип операции
- `resources` — для каких ресурсов

---

## Результат

**Теперь перед тем, как этот объект будет создан, каждый раз будет выполняться вызов `hook-service`.**

**И в зависимости от ответа (результата вызова) это будет или разрешено, или отклонено.**

---

</details>

---

## Резюме

✅ **Типы контроллеров допуска:**
- **Mutating Admission Controllers** (мутирующие) — могут изменить запрос
- **Validating Admission Controllers** (проверяющие) — валидируют запрос и могут разрешить или отклонить

✅ **Примеры:**
- **NamespaceExists** — проверяющий контроллер (отклоняет запросы к несуществующим namespace'ам)
- **DefaultStorageClass** — мутирующий контроллер (автоматически добавляет storageClass к PVC)

✅ **Порядок применения:**
1. Сначала вызываются **мутирующие** контроллеры
2. Затем **проверяющие** контроллеры
- Это нужно, чтобы изменения учитывались при проверке

✅ **Webhook Admission Controllers:**
- **MutatingAdmissionWebhook** — для кастомных мутаций
- **ValidatingAdmissionWebhook** — для кастомных валидаций
- Позволяют создать собственные контроллеры с собственной логикой

✅ **Настройка Webhook:**
1. Развернуть веб-сервер (внутри или вне кластера)
2. Создать объект конфигурации (`ValidatingWebhookConfiguration` или `MutatingWebhookConfiguration`)
3. Настроить `clientConfig` (URL или Service)
4. Настроить `rules` (когда вызывать webhook)
5. Настроить TLS сертификаты (`caBundle`)

✅ **Объект AdmissionReview:**
- Содержит сведения о запросе (пользователь, операция, объект)
- Ответ содержит `response.allowed: true/false`

✅ **Важно:**
- Встроенные контроллеры — часть исходного кода Kubernetes
- Webhook'и — для кастомной логики
- Порядок выполнения: сначала мутирующие, потом проверяющие
- Если любой контроллер отклоняет запрос, работа останавливается

> 💡 **Совет:** Webhook Admission Controllers — мощный инструмент для создания собственных правил валидации и мутации. Главное понять принцип работы: веб-сервер получает AdmissionReview, обрабатывает его по своей логике и возвращает ответ с разрешением или отклонением запроса.

---

