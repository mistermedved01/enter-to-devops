[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🎯 K-07-Планирование-и-ресурсы](../../README.md#-k-07-планирование-и-ресурсы)

---

# 🎯K-07-5-Node-Affinity
>Node Affinity — расширенный механизм размещения Pod'ов на определенных узлах с поддержкой логических операторов (OR, NOT, EXISTS) и мягких требований

---

<details>
<summary><b>🔍Что такое Node Affinity?</b></summary>

---

## Зачем нужен Node Affinity?

**Основная цель:** Обеспечить размещение Pod'ов на определенных узлах с **расширенными возможностями**.

**Проблема с Node Selector:**
- ❌ Нет логических операторов (OR, NOT)
- ❌ Только оператор равенства (`=`)
- ❌ Нельзя указать "мягкие" требования (preferred)

**Решение:** Node Affinity предоставляет расширенные возможности для ограничения размещения Pod'ов.

> ⚠️ **Важно:** Node Affinity обладает большой мощью, но также и высокой сложностью. Для простых случаев используйте Node Selector.

---

## Сравнение с Node Selector

### Node Selector (простой способ)

```yaml
spec:
  nodeSelector:
    size: large
```

### Node Affinity (то же самое, но сложнее)

```yaml
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
```

**Оба варианта делают одно и то же** — размещают Pod на узле с меткой `size=large`.

**Но Node Affinity позволяет:**
- ✅ Использовать оператор `In` (OR логика)
- ✅ Использовать оператор `NotIn` (NOT логика)
- ✅ Использовать оператор `Exists` (проверка наличия метки)
- ✅ Указать мягкие требования (preferred)

---

</details>

<details>
<summary><b>🔧Структура Node Affinity</b></summary>

---

## Базовая структура

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  affinity:
    nodeAffinity:
      # Тип 1: Требование (required)
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
            - medium
      
      # Тип 2: Предпочтение (preferred)
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        preference:
          matchExpressions:
          - key: disk-type
            operator: In
            values:
            - ssd
  containers:
  - name: app
    image: myapp:latest
```

---

## Разбор структуры

### 1. Секция `affinity`

```yaml
spec:
  affinity:          # ← Секция для всех правил affinity
    nodeAffinity:    # ← Правила для узлов
```

### 2. Типы Node Affinity

**Доступные типы:**

| Тип | Описание | Поведение |
|-----|----------|-----------|
| `requiredDuringSchedulingIgnoredDuringExecution` | Требование | Pod не будет запланирован, если условие не выполнено |
| `preferredDuringSchedulingIgnoredDuringExecution` | Предпочтение | Scheduler попытается разместить, но может проигнорировать |
| `requiredDuringSchedulingRequiredDuringExecution` | Требование + выполнение | ⚠️ Планируется (еще не доступен) |

### 3. `nodeSelectorTerms`

Массив условий для выбора узла:

```yaml
nodeSelectorTerms:
- matchExpressions:  # ← Условие 1
  - key: size
    operator: In
    values: [large, medium]
- matchExpressions:  # ← Условие 2 (OR логика между элементами массива)
  - key: disk-type
    operator: Exists
```

**Логика:** Между элементами массива `nodeSelectorTerms` действует оператор **OR**.

### 4. `matchExpressions`

Массив выражений внутри одного условия:

```yaml
matchExpressions:
- key: size          # ← Ключ метки
  operator: In       # ← Оператор
  values:            # ← Значения (зависит от оператора)
  - large
  - medium
```

**Логика:** Между элементами массива `matchExpressions` действует оператор **AND**.

---

</details>

<details>
<summary><b>⚙️Операторы Node Affinity</b></summary>

---

## Доступные операторы

### 1. `In` — значение в списке (OR логика)

**Логика:** Метка узла должна иметь одно из указанных значений.

```yaml
matchExpressions:
- key: size
  operator: In
  values:
  - large
  - medium
```

**Пример:** Разместить Pod на узле с `size=large` ИЛИ `size=medium`.

**Аналог в Node Selector:** ❌ Невозможно

---

### 2. `NotIn` — значение НЕ в списке (NOT логика)

**Логика:** Метка узла НЕ должна иметь ни одно из указанных значений.

```yaml
matchExpressions:
- key: size
  operator: NotIn
  values:
  - small
```

**Пример:** Разместить Pod на любом узле, кроме тех, где `size=small`.

**Аналог в Node Selector:** ❌ Невозможно

---

### 3. `Exists` — метка существует

**Логика:** Метка должна существовать на узле (значение не важно).

```yaml
matchExpressions:
- key: size
  operator: Exists
```

**Пример:** Разместить Pod на любом узле, где есть метка `size` (неважно, какое значение).

**Важно:** Для `Exists` поле `values` **не требуется**.

**Аналог в Node Selector:** ❌ Невозможно

---

### 4. `DoesNotExist` — метка НЕ существует

**Логика:** Метка НЕ должна существовать на узле.

```yaml
matchExpressions:
- key: size
  operator: DoesNotExist
```

**Пример:** Разместить Pod на узлах, где НЕТ метки `size`.

**Важно:** Для `DoesNotExist` поле `values` **не требуется**.

**Аналог в Node Selector:** ❌ Невозможно

---

### 5. Другие операторы

**Дополнительные операторы (для числовых меток):**
- `Gt` (Greater than) — больше
- `Lt` (Less than) — меньше

> 📝 **Примечание:** Подробнее о всех операторах смотрите в официальной документации Kubernetes.

---

## Сравнительная таблица операторов

| Оператор | Логика | Требует values? | Пример использования |
|----------|--------|-----------------|---------------------|
| `In` | OR | ✅ Да | `size` в `[large, medium]` |
| `NotIn` | NOT | ✅ Да | `size` НЕ в `[small]` |
| `Exists` | Проверка наличия | ❌ Нет | Метка `size` существует |
| `DoesNotExist` | Проверка отсутствия | ❌ Нет | Метка `size` не существует |
| `Gt` | Больше | ✅ Да | `cpu-count > 4` |
| `Lt` | Меньше | ✅ Да | `cpu-count < 8` |

---

</details>

<details>
<summary><b>📋Практические примеры</b></summary>

---

## Пример 1: Простой случай (аналог Node Selector)

**Задача:** Разместить Pod на узле с меткой `size=large`.

### Node Selector (простой способ)

```yaml
spec:
  nodeSelector:
    size: large
```

### Node Affinity (то же самое)

```yaml
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
```

---

## Пример 2: Использование оператора `In` (OR логика)

**Задача:** Разместить Pod на узле с `size=large` ИЛИ `size=medium`.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: big-data-processor
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
            - medium
  containers:
  - name: processor
    image: big-data-image:latest
```

**Результат:** Pod будет размещен на узлах с меткой `size=large` или `size=medium`.

---

## Пример 3: Использование оператора `NotIn` (NOT логика)

**Задача:** Разместить Pod на любом узле, кроме тех, где `size=small`.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: regular-app
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: NotIn
            values:
            - small
  containers:
  - name: app
    image: myapp:latest
```

**Результат:** Pod будет размещен на узлах с `size=large`, `size=medium` или без метки `size`.

---

## Пример 4: Использование оператора `Exists`

**Задача:** Разместить Pod на узле, где есть метка `size` (значение не важно).

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: labeled-node-app
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: Exists
  containers:
  - name: app
    image: myapp:latest
```

**Результат:** Pod будет размещен на любом узле, где существует метка `size` (с любым значением).

**Когда использовать:**
- Мы уверены, что неподходящие узлы не имеют метки `size`
- Нужно проверить только наличие метки, а не значение

---

## Пример 5: Комбинирование условий (AND логика)

**Задача:** Разместить Pod на узле с `size=large` И `disk-type=ssd`.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: high-performance-app
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
          - key: disk-type
            operator: In
            values:
            - ssd
  containers:
  - name: app
    image: myapp:latest
```

**Результат:** Pod будет размещен только на узле, который имеет **обе** метки: `size=large` И `disk-type=ssd`.

**Логика:** Все элементы в массиве `matchExpressions` объединяются оператором **AND**.

---

## Пример 6: Комбинирование условий (OR логика)

**Задача:** Разместить Pod на узле с `size=large` ИЛИ `disk-type=ssd`.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: flexible-app
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
        - matchExpressions:
          - key: disk-type
            operator: In
            values:
            - ssd
  containers:
  - name: app
    image: myapp:latest
```

**Результат:** Pod будет размещен на узле, который имеет `size=large` ИЛИ `disk-type=ssd`.

**Логика:** Элементы массива `nodeSelectorTerms` объединяются оператором **OR**.

---

</details>

<details>
<summary><b>⏱️Типы Node Affinity: DuringScheduling и DuringExecution</b></summary>

---

## Жизненный цикл Pod и Node Affinity

Node Affinity работает в двух состояниях жизненного цикла Pod:

```
┌─────────────────────────────────────────────────────────┐
│              Жизненный цикл Pod                           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  DuringScheduling          DuringExecution              │
│  ┌──────────────┐         ┌──────────────┐              │
│  │ Pod создается│         │ Pod работает│              │
│  │ Scheduler    │────────▶│ на узле     │              │
│  │ назначает    │         │             │              │
│  │ на узел      │         │             │              │
│  └──────────────┘         └──────────────┘              │
│                                                          │
│  Node Affinity проверяется    Изменения меток           │
│  при планировании             могут игнорироваться       │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## DuringScheduling (во время планирования)

**Состояние:** Pod не существует и создается впервые.

**Что происходит:**
1. Scheduler анализирует правила Node Affinity
2. Ищет подходящие узлы
3. Назначает Pod на выбранный узел

**Если подходящий узел не найден:**
- Зависит от типа Node Affinity (`required` или `preferred`)

---

## DuringExecution (во время выполнения)

**Состояние:** Pod уже запущен и работает на узле.

**Что может измениться:**
- Метки узла могут быть изменены
- Метки узла могут быть удалены
- Узел может быть помечен по-другому

**Поведение Node Affinity:**
- Зависит от типа (`IgnoredDuringExecution` или `RequiredDuringExecution`)

---

## Доступные типы

### 1. `requiredDuringSchedulingIgnoredDuringExecution`

**Перевод:** "Требуется во время планирования, игнорируется во время выполнения"

**Поведение:**

**DuringScheduling:**
- ✅ Scheduler **требует** выполнения условий
- ❌ Если подходящий узел не найден → Pod **не будет запланирован**
- Pod останется в состоянии `Pending`

**DuringExecution:**
- ✅ Изменения меток узла **игнорируются**
- ✅ Pod продолжит работать, даже если метки изменятся
- ✅ Pod не будет вытеснен (evicted)

**Пример:**
```yaml
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
```

**Когда использовать:**
- Размещение Pod критично важно
- Нужна гарантия, что Pod разместится только на подходящих узлах
- Изменения меток не должны влиять на уже работающие Pod'ы

---

### 2. `preferredDuringSchedulingIgnoredDuringExecution`

**Перевод:** "Предпочтительно во время планирования, игнорируется во время выполнения"

**Поведение:**

**DuringScheduling:**
- ✅ Scheduler **пытается** разместить Pod согласно условиям
- ✅ Если подходящий узел не найден → Scheduler **проигнорирует** правила
- ✅ Pod будет размещен на любом доступном узле

**DuringExecution:**
- ✅ Изменения меток узла **игнорируются**
- ✅ Pod продолжит работать

**Пример:**
```yaml
spec:
  affinity:
    nodeAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        preference:
          matchExpressions:
          - key: disk-type
            operator: In
            values:
            - ssd
```

**Параметр `weight`:**
- Значение от 1 до 100
- Используется для ранжирования предпочтений
- Чем выше weight, тем выше приоритет

**Когда использовать:**
- Размещение Pod желательно, но не критично
- Нужна гибкость: если подходящий узел недоступен, Pod все равно запустится
- Оптимизация производительности (желательно SSD, но не обязательно)

---

### 3. `requiredDuringSchedulingRequiredDuringExecution` ⚠️

**Перевод:** "Требуется во время планирования и во время выполнения"

**Статус:** ⚠️ **Планируется** (еще не доступен в текущих версиях Kubernetes)

**Поведение (ожидаемое):**

**DuringScheduling:**
- ✅ Scheduler **требует** выполнения условий
- ❌ Если подходящий узел не найден → Pod **не будет запланирован**

**DuringExecution:**
- ✅ Изменения меток узла **проверяются**
- ❌ Если условия Node Affinity больше не выполняются → Pod **будет вытеснен** (evicted)
- ❌ Pod будет завершен и должен быть пересоздан

**Пример использования (когда будет доступен):**
```yaml
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingRequiredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
```

**Сценарий:**
1. Pod размещен на узле с `size=large`
2. Администратор удаляет метку `size=large` с узла
3. Pod **вытесняется** и завершается
4. Новый Pod не будет запланирован, если нет подходящих узлов

---

## Сравнительная таблица типов

| Тип | DuringScheduling | DuringExecution | Когда использовать |
|-----|-----------------|-----------------|-------------------|
| `requiredDuringSchedulingIgnoredDuringExecution` | ✅ Требуется | ⏸️ Игнорируется | Критичное размещение, стабильность |
| `preferredDuringSchedulingIgnoredDuringExecution` | 💡 Предпочтительно | ⏸️ Игнорируется | Оптимизация, гибкость |
| `requiredDuringSchedulingRequiredDuringExecution` | ✅ Требуется | ✅ Проверяется | ⚠️ Планируется |

---

</details>

<details>
<summary><b>🛠️Практические сценарии</b></summary>

---

## Сценарий 1: Критичное размещение (required)

**Задача:** Обеспечить, чтобы ресурсно затратный Pod для обработки big data попал на выделенную мощную ноду.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: big-data-processor
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
  containers:
  - name: processor
    image: big-data-image:latest
    resources:
      requests:
        memory: "64Gi"
        cpu: "16"
```

**Поведение:**
- ✅ Pod будет размещен **только** на узле с `size=large`
- ❌ Если такого узла нет → Pod останется в `Pending`
- ✅ Если метка узла изменится после размещения → Pod продолжит работать

---

## Сценарий 2: Гибкое размещение (preferred)

**Задача:** Желательно разместить на SSD, но не критично.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: database-app
spec:
  affinity:
    nodeAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        preference:
          matchExpressions:
          - key: disk-type
            operator: In
            values:
            - ssd
  containers:
  - name: db
    image: database:latest
```

**Поведение:**
- ✅ Scheduler попытается разместить на узле с `disk-type=ssd`
- ✅ Если SSD-узла нет → Pod будет размещен на любом доступном узле
- ✅ Приложение запустится в любом случае

---

## Сценарий 3: Комбинация required + preferred

**Задача:** Требуется большой узел, предпочтительно с SSD.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: high-performance-app
spec:
  affinity:
    nodeAffinity:
      # Требование: только большие узлы
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
      # Предпочтение: SSD диск
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        preference:
          matchExpressions:
          - key: disk-type
            operator: In
            values:
            - ssd
  containers:
  - name: app
    image: myapp:latest
```

**Поведение:**
1. ✅ Pod будет размещен **только** на узле с `size=large` (требование)
2. ✅ Из всех больших узлов выберется тот, где `disk-type=ssd` (предпочтение)
3. ✅ Если SSD-узла нет среди больших → выберется любой большой узел

---

## Сценарий 4: Избегание определенных узлов

**Задача:** Не размещать на малых узлах, на остальных можно.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: regular-app
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: NotIn
            values:
            - small
  containers:
  - name: app
    image: myapp:latest
```

**Поведение:**
- ✅ Pod будет размещен на узлах с `size=large`, `size=medium` или без метки `size`
- ❌ Pod **не будет** размещен на узлах с `size=small`

---

</details>

<details>
<summary><b>🔍Проверка и отладка</b></summary>

---

## Проверка размещения Pod

```bash
# Просмотр на каком узле размещен Pod
kubectl get pod big-data-processor -o wide

# Вывод:
# NAME                  READY   STATUS    NODE    ...
# big-data-processor    1/1     Running   node02  ...
```

---

## Отладка Pod в состоянии Pending

Если Pod остался в состоянии `Pending`, проверьте:

### 1. Проверка события Pod'а

```bash
kubectl describe pod big-data-processor
```

**Ищите сообщения типа:**
```
Events:
  Warning  FailedScheduling  pod/big-data-processor  no nodes available to schedule pod
```

### 2. Проверка условий Node Affinity

```bash
# Просмотр детальной информации о Pod
kubectl get pod big-data-processor -o yaml | grep -A 20 affinity
```

### 3. Проверка меток узлов

```bash
# Просмотр всех узлов с метками
kubectl get nodes --show-labels

# Проверка конкретного узла
kubectl get node node02 --show-labels

# Фильтрация узлов по меткам
kubectl get nodes -l size=large
```

---

## Типичные ошибки

### Ошибка 1: Нет узлов, соответствующих условиям

**Проблема:**
```yaml
requiredDuringSchedulingIgnoredDuringExecution:
  nodeSelectorTerms:
  - matchExpressions:
    - key: size
      operator: In
      values:
      - large
```

Но ни один узел не имеет метки `size=large`.

**Решение:**
```bash
# Добавить метку на узел
kubectl label node node02 size=large
```

### Ошибка 2: Неправильный оператор

**Проблема:**
```yaml
- key: size
  operator: In
  values:  # ❌ Отсутствует для оператора In
```

**Решение:** Убедитесь, что для операторов `In`, `NotIn`, `Gt`, `Lt` указано поле `values`.

### Ошибка 3: Использование values с Exists

**Проблема:**
```yaml
- key: size
  operator: Exists
  values:  # ❌ Не требуется для Exists
  - large
```

**Решение:** Для операторов `Exists` и `DoesNotExist` поле `values` не требуется.

---

</details>

<details>
<summary><b>📊Сравнение Node Selector, Node Affinity и Taints</b></summary>

---

## Когда что использовать?

| Механизм | Сложность | Возможности | Когда использовать |
|----------|-----------|-------------|-------------------|
| **Node Selector** | Простой | Только `=` | Простые случаи, разделение по окружениям |
| **Node Affinity** | Сложный | `In`, `NotIn`, `Exists`, `Gt`, `Lt` | Сложные требования, гибкая логика |
| **Taints & Tolerations** | Средний | Ограничение узлов | Выделение специальных узлов (GPU, SSD) |

---

## Node Affinity vs Node Selector

| Критерий | Node Selector | Node Affinity |
|----------|---------------|---------------|
| **Синтаксис** | Простой | Сложный |
| **Операторы** | Только `=` | `In`, `NotIn`, `Exists`, `Gt`, `Lt` |
| **Логика** | Только AND | AND, OR, NOT |
| **Мягкие требования** | ❌ Нет | ✅ Да (preferred) |
| **Когда использовать** | Простые случаи | Сложные требования |

---

## Node Affinity vs Taints & Tolerations

**Node Affinity:**
- ✅ "Притягивает" Pod'ы к узлам (positive selection)
- ✅ Указывается в Pod'е
- ✅ Гибкая логика

**Taints & Tolerations:**
- ✅ "Отталкивает" Pod'ы от узлов (negative selection)
- ✅ Указывается на узле
- ✅ Проще для выделения специальных узлов

**Можно использовать вместе:**
- Taints на узле (например, GPU-узел)
- Tolerations в Pod'е (разрешает размещение)
- Node Affinity в Pod'е (предпочтение GPU-узла)

---

</details>

---

## Резюме

✅ **Node Affinity** — расширенный механизм размещения Pod'ов на узлах с поддержкой логических операторов

✅ **Операторы:**
- `In` — значение в списке (OR)
- `NotIn` — значение не в списке (NOT)
- `Exists` — метка существует
- `DoesNotExist` — метка не существует
- `Gt`, `Lt` — сравнение для числовых меток

✅ **Типы:**
- `requiredDuringSchedulingIgnoredDuringExecution` — требование при планировании
- `preferredDuringSchedulingIgnoredDuringExecution` — предпочтение при планировании
- `requiredDuringSchedulingRequiredDuringExecution` — ⚠️ планируется (вытеснение при изменении меток)

✅ **Когда использовать:**
- Сложные требования размещения
- Нужна логика OR, NOT
- Мягкие требования (preferred)
- Комбинация условий

---