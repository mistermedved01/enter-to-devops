[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🎯 K-07-Планирование-и-ресурсы](../../README.md#-k-07-планирование-и-ресурсы)

---

# 🎯K-07-4-Node-Selectors
>Node Selectors — простой механизм ограничения размещения Pod'ов на определенных узлах через метки и селекторы

---

<details>
<summary><b>🔍Что такое Node Selectors?</b></summary>

---

## Проблема: неконтролируемое размещение Pod'ов

Представьте кластер с тремя рабочими нодами:

```
┌─────────────────────────────────────────────────────────┐
│                    Kubernetes Cluster                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Node 1 (small)          Node 2 (large)     Node 3 (small)│
│  ┌─────────────┐         ┌─────────────┐    ┌─────────────┐│
│  │ 2 CPU       │         │ 16 CPU      │    │ 4 CPU       ││
│  │ 4 GB RAM    │         │ 64 GB RAM   │    │ 8 GB RAM    ││
│  │             │         │             │    │             ││
│  └─────────────┘         └─────────────┘    └─────────────┘│
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Проблема:**
- ✅ Две ноды (Node 1, Node 3) — небольшие, с ограниченными ресурсами
- ✅ Одна нода (Node 2) — мощная, с большими ресурсами
- ❌ По умолчанию scheduler может назначить **любой** Pod на **любую** ноду
- ❌ Тяжелые задачи обработки данных могут попасть на слабые ноды
- ❌ Это приведет к нехватке ресурсов и проблемам с производительностью

**Решение:** Использовать **Node Selectors** для ограничения размещения Pod'ов на определенных узлах.

---

## Что такое Node Selectors?

**Node Selector** — это механизм Kubernetes, который позволяет указать в спецификации Pod'а условие размещения на узлах с определенными метками.

**Как это работает:**
1. ✅ Помечаем узлы метками (например, `size=large`)
2. ✅ Указываем `nodeSelector` в спецификации Pod'а
3. ✅ Scheduler ищет узлы с подходящими метками
4. ✅ Pod размещается только на подходящих узлах

> ⚠️ **Важно:** Node Selectors работают только с **одной меткой** и оператором **равенства (=)**. Для сложных условий используйте Node Affinity.

---

</details>

<details>
<summary><b>🔧Как использовать Node Selectors?</b></summary>

---

## Шаг 1: Помечаем узлы метками

Перед использованием `nodeSelector` нужно пометить узлы соответствующими метками.

### Команда для добавления метки на узел

```bash
kubectl label node <node-name> <key>=<value>
```

**Пример:**
```bash
# Помечаем Node 2 как "большой" узел
kubectl label node node02 size=large
```

### Проверка меток узла

```bash
# Просмотр всех меток узла
kubectl get node node02 --show-labels

# Вывод:
# NAME     STATUS   ROLES    AGE   VERSION   LABELS
# node02   Ready    <none>   5d    v1.28.0   ...,size=large,...
```

### Просмотр всех узлов с метками

```bash
kubectl get nodes --show-labels
```

---

## Шаг 2: Добавляем nodeSelector в Pod

После того, как узлы помечены, добавляем `nodeSelector` в спецификацию Pod'а.

### Пример Pod с nodeSelector

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: big-data-processor
  labels:
    app: data-processing
spec:
  containers:
  - name: processor
    image: big-data-image:latest
    resources:
      requests:
        memory: "32Gi"
        cpu: "8"
      limits:
        memory: "64Gi"
        cpu: "16"
  # Node Selector: разместить только на узлах с меткой size=large
  nodeSelector:
    size: large
```

**Что происходит:**
1. Pod создается с `nodeSelector: size: large`
2. Scheduler проверяет все узлы на наличие метки `size=large`
3. Находит Node 2 (единственный узел с этой меткой)
4. Назначает Pod на Node 2

---

## Схема работы Node Selector

```
┌─────────────────────────────────────────────────────────┐
│                    Pod создан с nodeSelector            │
│                    nodeSelector:                        │
│                      size: large                        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Scheduler проверяет узлы                    │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Node 1: size=small ❌ Не подходит                      │
│  Node 2: size=large ✅ Подходит!                        │
│  Node 3: size=small ❌ Не подходит                      │
│                                                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Pod назначен на Node 2                      │
└─────────────────────────────────────────────────────────┘
```

---

</details>

<details>
<summary><b>📋Практические примеры</b></summary>

---

## Пример 1: Размещение на узлах с GPU

**Сценарий:** У вас есть приложение для машинного обучения, которое требует GPU.

### 1. Помечаем узлы с GPU

```bash
# Узел с GPU
kubectl label node gpu-node-01 hardware=gpu

# Узел без GPU
kubectl label node cpu-node-01 hardware=cpu
```

### 2. Создаем Pod с nodeSelector

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ml-training
spec:
  containers:
  - name: trainer
    image: ml-training:latest
    resources:
      requests:
        nvidia.com/gpu: 1
  nodeSelector:
    hardware: gpu
```

---

## Пример 2: Разделение по окружениям

**Сценарий:** Разделяем узлы на production и development окружения.

### 1. Помечаем узлы по окружениям

```bash
# Production узлы
kubectl label node prod-node-01 environment=production
kubectl label node prod-node-02 environment=production

# Development узлы
kubectl label node dev-node-01 environment=development
kubectl label node dev-node-02 environment=development
```

### 2. Production Pod

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: production-app
spec:
  containers:
  - name: app
    image: myapp:v1.0.0
  nodeSelector:
    environment: production
```

### 3. Development Pod

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: development-app
spec:
  containers:
  - name: app
    image: myapp:dev
  nodeSelector:
    environment: development
```

---

## Пример 3: Размещение по географическому расположению

**Сценарий:** Размещаем Pod'ы в определенных дата-центрах.

### 1. Помечаем узлы по регионам

```bash
kubectl label node node-us-east-01 region=us-east
kubectl label node node-eu-west-01 region=eu-west
kubectl label node node-asia-01 region=asia
```

### 2. Pod для региона EU

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: eu-service
spec:
  containers:
  - name: service
    image: my-service:latest
  nodeSelector:
    region: eu-west
```

---

## Изменение меток узла

Если нужно изменить метку узла:

```bash
# Изменить существующую метку (добавить --overwrite)
kubectl label node node02 size=small --overwrite

# Удалить метку
kubectl label node node02 size-
```

> ⚠️ **Важно:** Изменение меток узла не влияет на уже запущенные Pod'ы. Они продолжат работать на текущих узлах. Новые Pod'ы будут размещаться согласно новым меткам.

---

</details>

<details>
<summary><b>⚠️Ограничения Node Selectors</b></summary>

---

## Что НЕ может nodeSelector?

Node Selector — это **простой** механизм с ограниченными возможностями:

### ❌ Нельзя использовать логические операторы

**Невозможно:**
- `size=large ИЛИ size=medium` (OR)
- `size=large И disk=ssd` (AND с несколькими метками)
- `size!=small` (NOT)

**Пример запроса, который НЕ реализуем через nodeSelector:**
```
Разместить Pod на:
- Большом узле (size=large) ИЛИ
- Среднем узле (size=medium) ИЛИ  
- Любом узле, кроме малого (size!=small)
```

### ❌ Только оператор равенства (=)

Node Selector поддерживает только точное совпадение меток:

```yaml
# ✅ Работает
nodeSelector:
  size: large

# ❌ НЕ работает
nodeSelector:
  size: ">=large"     # Нет операторов сравнения
  size: "large|medium"  # Нет OR
```

---

## Решение: Node Affinity

Для сложных условий размещения используйте **Node Affinity**:

### Node Affinity позволяет:

✅ **Логические операторы:**
- `In` — значение в списке (OR)
- `NotIn` — значение не в списке (NOT)
- `Exists` — метка существует
- `DoesNotExist` — метка не существует

✅ **Сравнение:**
- `Gt` (Greater than) — больше
- `Lt` (Less than) — меньше

✅ **Комбинации:**
- Несколько условий с `requiredDuringSchedulingIgnoredDuringExecution`
- Мягкие требования с `preferredDuringSchedulingIgnoredDuringExecution`

**Пример с Node Affinity:**
```yaml
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: size
            operator: In
            values:
            - large
            - medium
```

> 📝 **Следующий шаг:** Подробнее о Node Affinity в следующей лекции.

---

## Сравнительная таблица

| Критерий | Node Selector | Node Affinity |
|----------|---------------|---------------|
| **Сложность** | Простой | Сложный |
| **Операторы** | Только `=` | `In`, `NotIn`, `Exists`, `Gt`, `Lt` |
| **Логика** | Только AND | AND, OR, NOT |
| **Множественные значения** | ❌ Нет | ✅ Да |
| **Мягкие требования** | ❌ Нет | ✅ Да (preferred) |
| **Когда использовать** | Простые случаи | Сложные требования |

---

</details>

<details>
<summary><b>🛠️Проверка и отладка</b></summary>

---

## Проверка меток узлов

```bash
# Просмотр всех узлов с метками
kubectl get nodes --show-labels

# Просмотр конкретного узла
kubectl get node node02 --show-labels

# Фильтрация узлов по меткам
kubectl get nodes -l size=large
kubectl get nodes -l environment=production
```

---

## Проверка размещения Pod'а

```bash
# Просмотр на каком узле размещен Pod
kubectl get pod big-data-processor -o wide

# Вывод:
# NAME                  READY   STATUS    NODE    ...
# big-data-processor    1/1     Running   node02  ...
```

---

## Отладка Pod в состоянии Pending

Если Pod остался в состоянии `Pending`, проверьте:

### 1. Проверка события Pod'а

```bash
kubectl describe pod big-data-processor
```

**Возможные причины:**
- ❌ Нет узлов с подходящей меткой
- ❌ Узлы не имеют достаточных ресурсов
- ❌ Узлы имеют Taints без соответствующих Tolerations

### 2. Проверка меток узлов

```bash
# Проверяем, есть ли узлы с нужной меткой
kubectl get nodes -l size=large

# Если узлов нет, добавляем метку
kubectl label node node02 size=large
```

### 3. Проверка ресурсов узлов

```bash
# Проверяем доступные ресурсы на узле
kubectl describe node node02

# Ищем секции:
# - Allocatable (доступные ресурсы)
# - Allocated resources (использованные ресурсы)
```

---

## Типичные ошибки

### Ошибка 1: Метка не существует на узлах

**Проблема:**
```yaml
nodeSelector:
  size: large
```

Но ни один узел не имеет метки `size=large`.

**Решение:**
```bash
kubectl label node node02 size=large
```

### Ошибка 2: Опечатка в метке

**Проблема:**
```yaml
nodeSelector:
  size: lage  # Опечатка: должно быть "large"
```

**Решение:** Проверьте правильность написания метки в `nodeSelector` и на узлах.

### Ошибка 3: Использование нескольких меток

**Проблема:**
```yaml
nodeSelector:
  size: large
  disk: ssd
```

**Поведение:** Pod будет размещен только на узле, который имеет **обе** метки (`size=large` И `disk=ssd`). Это работает как логический AND.

---

</details>

<details>
<summary><b>📚Резюме</b></summary>

---

✅ **Node Selectors** — простой механизм ограничения размещения Pod'ов на узлах с определенными метками

✅ **Работает через:**
1. Метки на узлах (`kubectl label node`)
2. `nodeSelector` в спецификации Pod'а

✅ **Ограничения:**
- Только оператор равенства (`=`)
- Нет логических операторов (OR, NOT)
- Для сложных условий используйте Node Affinity

✅ **Когда использовать:**
- Простые требования размещения
- Разделение по окружениям (dev/prod)
- Размещение на специализированных узлах (GPU, SSD)

---

</details>