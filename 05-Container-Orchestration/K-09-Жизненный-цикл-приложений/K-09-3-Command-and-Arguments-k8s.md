[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔄 K-09-Жизненный-цикл-приложений](../../README.md#-k-09-жизненный-цикл-приложений)

---

# ☸️K-09-3-Command-and-Arguments-k8s
>Команды и аргументы в Pods Kubernetes: переопределение CMD и ENTRYPOINT через поля command и args, связь с Docker инструкциями

---

<details>
<summary><b>🎯Введение: команды и аргументы в Kubernetes Pods</b></summary>

---

## О чем эта лекция

Привет и добро пожаловать в лекцию, где мы рассмотрим команды и аргументы в PODs Kubernetes.

В предыдущей лекции мы создали простой докер-образ, который спит определенное количество секунд.

Мы назвали его `ubuntu-sleeper` и запустили его с помощью команды `docker run ubuntu-sleeper`.

По умолчанию он спит в течение пяти секунд, но ты можешь переопределить его, передав аргумент командной строки.

Теперь мы создадим POD, используя этот образ.

---

</details>

<details>
<summary><b>🚀Создание Pod с образом ubuntu-sleeper</b></summary>

---

## Базовый Pod манифест

Мы начинаем с пустого шаблона определения POD, вводим его название, указываем имя образа:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-sleeper-pod
spec:
  containers:
  - name: ubuntu-sleeper
    image: ubuntu-sleeper
```

Когда POD создается, он создает контейнер из указанного образа, и контейнер находится в спящем режиме в течение пяти секунд, прежде чем выйти.

Это происходит потому, что в Dockerfile образа `ubuntu-sleeper` указано:

```dockerfile
ENTRYPOINT ["sleep"]
CMD ["5"]
```

---

## Запуск Pod

```bash
kubectl apply -f pod.yaml
kubectl get pods
```

Через 5 секунд Pod завершится:

```
NAME                READY   STATUS      RESTARTS   AGE
ubuntu-sleeper-pod  0/1     Completed   0          10s
```

---

</details>

<details>
<summary><b>🔧Переопределение аргументов: поле args</b></summary>

---

## Проблема: нужно изменить время сна

Теперь, нам нужно, чтобы контейнер спал 10 секунд, как во второй команде.

Как указать дополнительный аргумент в файле определения POD?

Все, что дополнительно нужно добавить к команде `docker run`, будет помещено в свойство `args` файла определения POD в виде массива, как ты видишь здесь:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-sleeper-pod
spec:
  containers:
  - name: ubuntu-sleeper
    image: ubuntu-sleeper
    args: ["10"]  # Переопределяет CMD из Dockerfile
```

---

## Как это работает?

Давай попробуем связать это с Dockerfile из предыдущей лекции.

В Dockerfile есть `ENTRYPOINT`, а также указана инструкция `CMD`:

```dockerfile
ENTRYPOINT ["sleep"]
CMD ["5"]
```

`ENTRYPOINT` — это команда, которая выполняется при запуске, а `CMD` — это параметр по умолчанию, передаваемый команде.

С помощью параметра `args` в манифесте POD мы переопределяем инструкцию `CMD` из Dockerfile.

Итоговая команда будет: `sleep 10`

---

## Примеры использования args

### Пример 1: Один аргумент

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-sleeper-pod
spec:
  containers:
  - name: ubuntu-sleeper
    image: ubuntu-sleeper
    args: ["10"]
```

Выполнится: `sleep 10`

---

### Пример 2: Несколько аргументов

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
  - name: app
    image: my-app
    args: ["--port", "8080", "--host", "0.0.0.0"]
```

Если ENTRYPOINT в Dockerfile: `["my-app"]`

Выполнится: `my-app --port 8080 --host 0.0.0.0`

---

</details>

<details>
<summary><b>🎯Переопределение ENTRYPOINT: поле command</b></summary>

---

## Проблема: нужно заменить команду

Но что, если нам нужно переопределить `ENTRYPOINT`?

Скажем заменить ее как раньше мы делали с Docker, когда меняли с `sleep` на `super-sleep`?

В мире Docker мы бы запустили команду `docker run` с параметром `--entrypoint`, в котором указали бы новую команду:

```bash
docker run --entrypoint super-sleep ubuntu-sleeper 10
```

---

## Поле command в Kubernetes

В мире Kubernetes в файле определения POD есть соответствующее этому параметру поле, оно называется `command`.

Это поле, если оно задано, будет использовано Kubernetes чтобы запускать контейнер с измененной инструкцией `--entrypoint`.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-sleeper-pod
spec:
  containers:
  - name: ubuntu-sleeper
    image: ubuntu-sleeper
    command: ["super-sleep"]  # Переопределяет ENTRYPOINT из Dockerfile
    args: ["10"]
```

Выполнится: `super-sleep 10`

---

## Примеры использования command

### Пример 1: Замена команды

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-sleeper-pod
spec:
  containers:
  - name: ubuntu-sleeper
    image: ubuntu-sleeper
    command: ["super-sleep"]
    args: ["10"]
```

---

### Пример 2: Полная замена команды и аргументов

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
  - name: app
    image: nginx
    command: ["/bin/sh"]
    args: ["-c", "echo Hello World && sleep 3600"]
```

Выполнится: `/bin/sh -c echo Hello World && sleep 3600`

---

</details>

<details>
<summary><b>🔗Связь между Docker и Kubernetes</b></summary>

---

## Соответствие полей

Чтобы подвести итог: есть два поля которые соответствуют двум инструкциям в Dockerfile.

| Docker | Kubernetes | Описание |
|--------|------------|----------|
| `ENTRYPOINT` | `command` | Команда, которая выполняется |
| `CMD` | `args` | Аргументы по умолчанию |

---

## Как это работает вместе

Поле `command` переопределяет оригинальную инструкцию `ENTRYPOINT` из Dockerfile.

Поле `args` переопределяет инструкцию `CMD`.

Часто новичков это сбивает.

Помни, что:
- ✅ **`command` в Kubernetes** — это то же, что **`ENTRYPOINT` в Docker**
- ✅ **Аналог `CMD`** — это **`args`**

---

## Визуальное сравнение

### Docker

```dockerfile
# Dockerfile
ENTRYPOINT ["sleep"]
CMD ["5"]
```

```bash
# Запуск
docker run ubuntu-sleeper 10
# Выполнится: sleep 10
```

---

### Kubernetes

```yaml
# Pod манифест
apiVersion: v1
kind: Pod
spec:
  containers:
  - name: ubuntu-sleeper
    image: ubuntu-sleeper
    # command: ["sleep"]  # По умолчанию из ENTRYPOINT
    args: ["10"]          # Переопределяет CMD
```

Выполнится: `sleep 10`

---

</details>

<details>
<summary><b>💡Практические примеры</b></summary>

---

## Пример 1: Только переопределение args

Dockerfile:

```dockerfile
FROM ubuntu
ENTRYPOINT ["sleep"]
CMD ["5"]
```

Pod манифест:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: sleeper-10
spec:
  containers:
  - name: sleeper
    image: ubuntu-sleeper
    args: ["10"]
```

Результат: `sleep 10`

---

## Пример 2: Переопределение command и args

Dockerfile:

```dockerfile
FROM ubuntu
ENTRYPOINT ["sleep"]
CMD ["5"]
```

Pod манифест:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: super-sleeper
spec:
  containers:
  - name: sleeper
    image: ubuntu-sleeper
    command: ["super-sleep"]
    args: ["20"]
```

Результат: `super-sleep 20`

---

## Пример 3: Python приложение

Dockerfile:

```dockerfile
FROM python:3.9
ENTRYPOINT ["python"]
CMD ["app.py"]
```

Pod манифест (вариант 1 — только args):

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: python-app
spec:
  containers:
  - name: app
    image: my-python-app
    args: ["script.py"]
```

Результат: `python script.py`

---

Pod манифест (вариант 2 — command и args):

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: python-app
spec:
  containers:
  - name: app
    image: my-python-app
    command: ["python3"]
    args: ["-u", "script.py"]
```

Результат: `python3 -u script.py`

---

## Пример 4: Nginx с кастомной командой

Dockerfile:

```dockerfile
FROM nginx
ENTRYPOINT ["nginx"]
CMD ["-g", "daemon off;"]
```

Pod манифест:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  containers:
  - name: nginx
    image: nginx
    command: ["nginx"]
    args: ["-g", "daemon off;", "-c", "/custom/nginx.conf"]
```

Результат: `nginx -g daemon off; -c /custom/nginx.conf`

---

</details>

<details>
<summary><b>⚠️Важные моменты и частые ошибки</b></summary>

---

## Важные моменты

✅ **Формат массивов:**
- `command` и `args` должны быть массивами строк
- Каждый элемент массива — отдельный аргумент

✅ **Переопределение:**
- `command` полностью заменяет `ENTRYPOINT`
- `args` полностью заменяет `CMD`

✅ **Если не указано:**
- Если `command` не указан, используется `ENTRYPOINT` из образа
- Если `args` не указан, используется `CMD` из образа

---

## Частые ошибки

### Ошибка 1: Неправильный формат

**❌ Неправильно:**

```yaml
args: "10"  # Строка вместо массива
```

**✅ Правильно:**

```yaml
args: ["10"]  # Массив строк
```

---

### Ошибка 2: Смешивание команды и аргументов

**❌ Неправильно:**

```yaml
command: ["sleep 10"]  # Команда и аргумент вместе
```

**✅ Правильно:**

```yaml
command: ["sleep"]
args: ["10"]
```

---

### Ошибка 3: Путаница command и args

**❌ Неправильно:**

```yaml
# Пытаемся переопределить CMD через command
command: ["10"]  # Это заменит ENTRYPOINT, а не CMD!
```

**✅ Правильно:**

```yaml
# Переопределяем CMD через args
args: ["10"]
```

---

</details>

<details>
<summary><b>📊Сравнительная таблица</b></summary>

---

## Docker vs Kubernetes

| Аспект | Docker | Kubernetes |
|--------|--------|------------|
| **Фиксированная команда** | `ENTRYPOINT ["sleep"]` | `command: ["sleep"]` |
| **Аргументы по умолчанию** | `CMD ["5"]` | `args: ["5"]` |
| **Переопределение команды** | `--entrypoint super-sleep` | `command: ["super-sleep"]` |
| **Переопределение аргументов** | Параметры в `docker run` | `args: ["10"]` |
| **Формат** | JSON массив в Dockerfile | YAML массив в Pod манифесте |

---

## Как работает переопределение

| Сценарий | Dockerfile | Pod манифест | Результат |
|----------|------------|--------------|-----------|
| **По умолчанию** | `ENTRYPOINT ["sleep"]`<br>`CMD ["5"]` | (не указано) | `sleep 5` |
| **Только args** | `ENTRYPOINT ["sleep"]`<br>`CMD ["5"]` | `args: ["10"]` | `sleep 10` |
| **command + args** | `ENTRYPOINT ["sleep"]`<br>`CMD ["5"]` | `command: ["super-sleep"]`<br>`args: ["20"]` | `super-sleep 20` |
| **Только command** | `ENTRYPOINT ["sleep"]`<br>`CMD ["5"]` | `command: ["super-sleep"]` | `super-sleep 5` |

---

</details>

<details>
<summary><b>🧪Практика: создание и тестирование</b></summary>

---

## Шаг 1: Создание Pod с args

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: sleeper-10
spec:
  containers:
  - name: sleeper
    image: ubuntu-sleeper
    args: ["10"]
```

```bash
kubectl apply -f sleeper-10.yaml
kubectl get pods
```

---

## Шаг 2: Проверка выполнения

```bash
# Смотрим логи
kubectl logs sleeper-10

# Смотрим описание Pod
kubectl describe pod sleeper-10
```

В описании Pod ты увидишь:

```
Command:
  sleep
Args:
  10
```

---

## Шаг 3: Создание Pod с command и args

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: super-sleeper
spec:
  containers:
  - name: sleeper
    image: ubuntu-sleeper
    command: ["super-sleep"]
    args: ["20"]
```

```bash
kubectl apply -f super-sleeper.yaml
kubectl logs super-sleeper
```

---

## Шаг 4: Отладка

Если что-то не работает, проверь:

```bash
# Описание Pod
kubectl describe pod <pod-name>

# События
kubectl get events

# Логи
kubectl logs <pod-name>
```

---

</details>

---

## Резюме

✅ **Поле `args` в Kubernetes:**
- Переопределяет инструкцию `CMD` из Dockerfile
- Используется для изменения аргументов команды
- Формат: массив строк `["arg1", "arg2"]`

✅ **Поле `command` в Kubernetes:**
- Переопределяет инструкцию `ENTRYPOINT` из Dockerfile
- Используется для замены команды
- Формат: массив строк `["command"]`

✅ **Соответствие Docker и Kubernetes:**
- `ENTRYPOINT` (Docker) = `command` (Kubernetes)
- `CMD` (Docker) = `args` (Kubernetes)

✅ **Как это работает:**
- Если `command` не указан → используется `ENTRYPOINT` из образа
- Если `args` не указан → используется `CMD` из образа
- `command` полностью заменяет `ENTRYPOINT`
- `args` полностью заменяет `CMD`

✅ **Практика:**
- Используй `args` для изменения параметров команды
- Используй `command` для замены команды
- Всегда указывай в формате массива строк
- Помни: `command` = `ENTRYPOINT`, `args` = `CMD`

> 💡 **Совет:** Часто новичков это сбивает. Помни, что `command` в Kubernetes — это то же, что `ENTRYPOINT` в Docker. Аналог `CMD` — это `args`. Используй `kubectl describe pod` для проверки того, какие команды и аргументы реально используются в Pod. 

---