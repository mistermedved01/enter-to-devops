[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔄 K-09-Жизненный-цикл-приложений](../../README.md#-k-09-жизненный-цикл-приложений)

---

# 🔧K-09-13-Pod-In-Place-Resize
>Изменение ресурсов CPU и памяти контейнеров без пересоздания Pod: In-Place Pod Resize, политики изменения размера, статусы изменения, ограничения

---

<details>
<summary><b>📊Введение: In-Place Pod Resize</b></summary>

---

## Изменение ресурсов без пересоздания Pod

**FEATURE STATE:** Kubernetes v1.35 [stable] (включено по умолчанию)

In-Place Pod Resize позволяет изменять выделение CPU и памяти контейнера(ов) в работающем Pod, потенциально избегая прерывания работы приложения.

### Традиционный подход

Традиционно изменение требований к ресурсам Pod требовало удаления существующего Pod и создания замены, часто управляемой контроллером рабочей нагрузки.

### Новый подход

In-Place Pod Resize позволяет изменять выделение CPU/памяти контейнера(ов) в работающем Pod без необходимости его пересоздания.

### Ключевые концепции

- **Desired Resources (Желаемые ресурсы):** Поле `spec.containers[*].resources` представляет желаемые ресурсы для контейнера и является изменяемым для CPU и памяти.
- **Actual Resources (Фактические ресурсы):** Поле `status.containerStatuses[*].resources` отражает ресурсы, в настоящее время настроенные для работающего контейнера.
- **Triggering a Resize (Запуск изменения размера):** Можно запросить изменение размера, обновив желаемые запросы и лимиты в спецификации Pod.
- **Allocated Resources (Выделенные ресурсы):** Поле `status.containerStatuses[*].allocatedResources` отслеживает значения ресурсов, подтвержденные Kubelet, в основном используется для внутренней логики планирования.

---

</details>

<details>
<summary><b>📋Требования и настройка</b></summary>

---

## Требования

### Версия Kubernetes

- Kubernetes сервер должен быть версии 1.33 или выше
- Проверка версии: `kubectl version`

### Feature Gate

Feature Gate `InPlacePodVerticalScaling` должен быть включен для:
- Control plane
- Всех нод в кластере

### Версия kubectl

Версия клиента kubectl должна быть не менее v1.32 для использования флага `--subresource=resize`.

### Кластер

Рекомендуется запускать на кластере с минимум двумя нодами, которые не являются control plane хостами.

---

</details>

<details>
<summary><b>📊Статусы изменения размера Pod</b></summary>

---

## Pod Resize Status

Kubelet обновляет условия статуса Pod для указания состояния запроса на изменение размера.

### PodResizePending

Kubelet не может немедленно выполнить запрос. Поле `message` предоставляет объяснение причины.

**Причины (reasons):**

#### Infeasible (Невозможно)

Запрошенное изменение размера невозможно на текущей ноде (например, запрос больше ресурсов, чем есть на ноде).

**Пример:**

```yaml
conditions:
- type: PodResizePending
  status: "True"
  reason: Infeasible
  message: "Node didn't have enough capacity: cpu, requested: 800000, capacity: ..."
```

#### Deferred (Отложено)

Запрошенное изменение размера в настоящее время невозможно, но может стать возможным позже (например, если другой pod будет удален). Kubelet будет повторять попытку изменения размера.

### PodResizeInProgress

Kubelet принял изменение размера и выделил ресурсы, но изменения все еще применяются. Обычно это кратковременно, но может занять больше времени в зависимости от типа ресурса и поведения runtime. Любые ошибки во время применения сообщаются в поле `message` (вместе с `reason: Error`).

### Схема состояний изменения размера

```
┌─────────────────────────────────────────────────────────┐
│  Запрос на изменение размера                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │  kubectl patch pod --subresource resize          │   │
│  └──────────────────────────────────────────────────┘   │
│                        │                                │
│                        ▼                                │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Kubelet проверяет возможность изменения         │   │
│  └──────────────────────────────────────────────────┘   │
│                        │                                │
│        ┌───────────────┴───────────────┐               │
│        ▼                               ▼               │
│  ┌──────────────┐            ┌──────────────────┐     │
│  │ Невозможно   │            │ Возможно         │     │
│  │              │            │                  │     │
│  │ PodResize    │            │ PodResize        │     │
│  │ Pending      │            │ InProgress       │     │
│  │ reason:      │            │                  │     │
│  │ Infeasible   │            │ Применение       │     │
│  └──────────────┘            │ изменений        │     │
│                               └──────────────────┘     │
│                                        │                │
│                                        ▼                │
│                               ┌──────────────────┐     │
│                               │ Успешно          │     │
│                               │ status.resources │     │
│                               │ обновлен         │     │
│                               └──────────────────┘     │
└─────────────────────────────────────────────────────────┘
```

### Повторные попытки отложенных изменений размера

Если запрошенное изменение размера отложено (Deferred), kubelet будет периодически повторять попытку изменения размера, например, когда другой pod будет удален или масштабирован вниз.

Если есть несколько отложенных изменений размера, они повторяются в соответствии со следующим приоритетом:

1. **Приоритет Pod:** Pod с более высоким приоритетом (на основе PriorityClass) будут иметь запросы на изменение размера, повторяемые первыми.
2. **QoS класс:** Если два pod имеют одинаковый приоритет, изменение размера гарантированных pod будет повторяться перед изменением размера burstable pod.
3. **Время ожидания:** Если все остальное одинаково, pod, которые находились в состоянии Deferred дольше, будут иметь приоритет.

### Поля observedGeneration

**FEATURE STATE:** Kubernetes v1.35 [stable] (включено по умолчанию)

Поле верхнего уровня `status.observedGeneration` показывает `metadata.generation`, соответствующее последней спецификации pod, которую kubelet признал. Это можно использовать для определения самого последнего запроса на изменение размера, который kubelet обработал.

- В условии `PodResizeInProgress` поле `conditions[].observedGeneration` указывает `metadata.generation` podSpec, когда было инициировано текущее изменение размера в процессе.
- В условии `PodResizePending` поле `conditions[].observedGeneration` указывает `metadata.generation` podSpec, когда было последний раз предпринято выделение отложенного изменения размера.

---

</details>

<details>
<summary><b>⚙️Политики изменения размера контейнера</b></summary>

---

## Container Resize Policies

Можно контролировать, должен ли контейнер перезапускаться при изменении размера, установив `resizePolicy` в спецификации контейнера. Это позволяет детальный контроль на основе типа ресурса (CPU или память).

### Настройка политики

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: resize-demo
spec:
  containers:
  - name: pause
    image: registry.k8s.io/pause:3.8
    resizePolicy:
    - resourceName: cpu
      restartPolicy: NotRequired
    - resourceName: memory
      restartPolicy: RestartContainer
```

### Варианты политик

#### NotRequired (По умолчанию)

Применить изменение ресурса к работающему контейнеру без его перезапуска.

**Использование:**
- Изменения CPU обычно не требуют перезапуска
- Изменения памяти для приложений, поддерживающих динамическое изменение памяти

#### RestartContainer

Перезапустить контейнер для применения новых значений ресурсов. Часто необходимо для изменений памяти, поскольку многие приложения и runtime не могут динамически настроить выделение памяти.

**Использование:**
- Изменения памяти для большинства приложений
- Когда требуется полная переинициализация контейнера

### Поведение по умолчанию

Если `resizePolicy[*].restartPolicy` не указан для ресурса, по умолчанию используется `NotRequired`.

### Ограничения

Если общая `restartPolicy` Pod — `Never`, то любая политика изменения размера контейнера должна быть `NotRequired` для всех ресурсов. Нельзя настроить политику изменения размера, которая потребует перезапуска в таких Pod.

### Пример сценария

Рассмотрим контейнер, настроенный с `restartPolicy: NotRequired` для CPU и `restartPolicy: RestartContainer` для памяти:

- **Если изменены только ресурсы CPU:** контейнер изменяется на месте
- **Если изменена только память:** контейнер перезапускается
- **Если изменены и CPU, и память одновременно:** контейнер перезапускается (из-за политики памяти)

---

</details>

<details>
<summary><b>🚫Ограничения In-Place Pod Resize</b></summary>

---

## Ограничения для Kubernetes 1.35

### Типы ресурсов

Только ресурсы CPU и памяти могут быть изменены.

### Уменьшение памяти

Если политика перезапуска изменения размера памяти — `NotRequired` (или не указана), kubelet предпримет попытку предотвратить oom-kills при уменьшении лимитов памяти, но не предоставляет никаких гарантий.

**Важно:** Перед уменьшением лимитов памяти контейнера, если использование памяти превышает запрошенный лимит, изменение размера будет пропущено, и статус останется в состоянии "In Progress". Это считается best-effort, поскольку все еще подвержено состоянию гонки, когда использование памяти может резко возрасти сразу после проверки.

### QoS класс

Исходный класс качества обслуживания (QoS) Pod (Guaranteed, Burstable или BestEffort) определяется при создании и не может быть изменен изменением размера. Измененные значения ресурсов должны по-прежнему соответствовать правилам исходного QoS класса:

- **Guaranteed:** Запросы должны продолжать равняться лимитам как для CPU, так и для памяти после изменения размера.
- **Burstable:** Запросы и лимиты не могут стать равными одновременно для CPU и памяти (так как это изменило бы его на Guaranteed).
- **BestEffort:** Требования к ресурсам (запросы или лимиты) не могут быть добавлены (так как это изменило бы его на Burstable или Guaranteed).

### Типы контейнеров

- **Init контейнеры (не перезапускаемые):** не могут быть изменены
- **Ephemeral контейнеры:** не могут быть изменены
- **Sidecar контейнеры:** могут быть изменены

### Удаление ресурсов

Запросы и лимиты ресурсов не могут быть полностью удалены после установки; они могут быть изменены только на другие значения.

### Операционная система

Windows pod не поддерживают изменение размера на месте.

### Политики нод

Pod, управляемые политиками статического CPU или Memory manager, не могут быть изменены на месте.

### Swap

Pod, использующие swap память, не могут изменять запросы памяти, если политика изменения размера для памяти не `RestartContainer`.

### Будущие версии

Эти ограничения могут быть ослаблены в будущих версиях Kubernetes.

---

</details>

<details>
<summary><b>📝Пример 1: Изменение CPU без перезапуска</b></summary>

---

## Изменение размера CPU

### Создание Pod

Создайте Pod, предназначенный для изменения размера CPU на месте и изменения размера памяти с перезапуском:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: resize-demo
spec:
  containers:
  - name: pause
    image: registry.k8s.io/pause:3.8
    resizePolicy:
    - resourceName: cpu
      restartPolicy: NotRequired
    - resourceName: memory
      restartPolicy: RestartContainer
    resources:
      limits:
        memory: "200Mi"
        cpu: "700m"
      requests:
        memory: "200Mi"
        cpu: "700m"
```

### Создание Pod

```bash
kubectl create -f pod-resize.yaml
```

Этот pod запускается в QoS классе Guaranteed. Проверьте его начальное состояние:

```bash
kubectl get pod resize-demo --output=yaml
```

Наблюдайте `spec.containers[0].resources` и `status.containerStatuses[0].resources`. Они должны соответствовать манифесту (700m CPU, 200Mi memory). Обратите внимание на `status.containerStatuses[0].restartCount` (должен быть 0).

### Изменение размера CPU

Увеличьте запрос и лимит CPU до 800m. Используйте `kubectl patch` с аргументом командной строки `--subresource resize`:

```bash
kubectl patch pod resize-demo --subresource resize --patch \
  '{"spec":{"containers":[{"name":"pause", "resources":{"requests":{"cpu":"800m"}, "limits":{"cpu":"800m"}}}]}}'
```

**Альтернативные методы:**

```bash
# Редактирование
kubectl edit pod resize-demo --subresource resize

# Apply с server-side
kubectl apply -f <updated-manifest> --subresource resize --server-side
```

> **Примечание:** Аргумент командной строки `--subresource resize` требует версию клиента kubectl v1.32.0 или выше. Более старые версии будут сообщать об ошибке недопустимого subresource.

### Проверка результата

Проверьте статус pod после патча:

```bash
kubectl get pod resize-demo --output=yaml
```

Вы должны увидеть:

- `spec.containers[0].resources` теперь показывает `cpu: 800m`
- `status.containerStatuses[0].resources` также показывает `cpu: 800m`, что указывает на успешное изменение размера на ноде
- `status.containerStatuses[0].restartCount` остается 0, потому что политика изменения размера CPU была `NotRequired`

---

</details>

<details>
<summary><b>📝Пример 2: Изменение памяти с перезапуском</b></summary>

---

## Изменение размера памяти

Теперь измените размер памяти для того же pod, увеличив ее до 300Mi. Поскольку политика изменения размера памяти — `RestartContainer`, ожидается перезапуск контейнера.

### Изменение размера памяти

```bash
kubectl patch pod resize-demo --subresource resize --patch \
  '{"spec":{"containers":[{"name":"pause", "resources":{"requests":{"memory":"300Mi"}, "limits":{"memory":"300Mi"}}}]}}'
```

### Проверка результата

Проверьте статус pod вскоре после патча:

```bash
kubectl get pod resize-demo --output=yaml
```

Теперь вы должны наблюдать:

- `spec.containers[0].resources` показывает `memory: 300Mi`
- `status.containerStatuses[0].resources` также показывает `memory: 300Mi`
- `status.containerStatuses[0].restartCount` увеличился до 1 (или больше, если перезапуски происходили ранее), что указывает на перезапуск контейнера для применения изменения памяти

---

</details>

<details>
<summary><b>🔍Устранение неполадок: Невозможный запрос изменения размера</b></summary>

---

## Невозможный запрос изменения размера

Попробуйте запросить неразумное количество CPU, например, 1000 полных ядер (записанных как "1000" вместо "1000m" для милликор), что, вероятно, превышает емкость ноды.

### Попытка недопустимого изменения размера

```bash
kubectl patch pod resize-demo --subresource resize --patch \
  '{"spec":{"containers":[{"name":"pause", "resources":{"requests":{"cpu":"1000"}, "limits":{"cpu":"1000"}}}]}}'
```

### Проверка статуса

Запросите детали Pod:

```bash
kubectl get pod resize-demo --output=yaml
```

Вы увидите изменения, указывающие на проблему:

- `spec.containers[0].resources` отражает желаемое состояние (`cpu: "1000"`)
- Добавлено условие с `type: PodResizePending` и `reason: Infeasible`
- Сообщение условия объяснит причину (`Node didn't have enough capacity: cpu, requested: 800000, capacity: ...`)
- Важно: `status.containerStatuses[0].resources` все еще будет показывать предыдущие значения (`cpu: 800m, memory: 300Mi`), потому что невозможное изменение размера не было применено Kubelet
- `restartCount` не изменится из-за этой неудачной попытки

### Исправление

Для исправления необходимо снова пропатчить pod с допустимыми значениями ресурсов:

```bash
kubectl patch pod resize-demo --subresource resize --patch \
  '{"spec":{"containers":[{"name":"pause", "resources":{"requests":{"cpu":"800m"}, "limits":{"cpu":"800m"}}}]}}'
```

---

</details>

<details>
<summary><b>🔄Сравнение с традиционным подходом</b></summary>

---

## Традиционный подход vs In-Place Resize

### Традиционный подход

```
┌─────────────────────────────────────────────────────────┐
│  1. Обновление Deployment/StatefulSet                   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  spec.template.spec.containers[].resources         │   │
│  └──────────────────────────────────────────────────┘   │
│                        │                                │
│                        ▼                                │
│  2. Rolling Update                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │  - Старый Pod удаляется                          │   │
│  │  - Новый Pod создается                           │   │
│  │  - Прерывание работы приложения                   │   │
│  └──────────────────────────────────────────────────┘   │
│                        │                                │
│                        ▼                                │
│  3. Новый Pod с обновленными ресурсами                  │
└─────────────────────────────────────────────────────────┘
```

**Недостатки:**
- Прерывание работы приложения
- Потеря состояния (если не используется persistent storage)
- Дополнительное время на пересоздание

### In-Place Resize

```
┌─────────────────────────────────────────────────────────┐
│  1. Обновление ресурсов Pod напрямую                    │
│  ┌──────────────────────────────────────────────────┐   │
│  │  kubectl patch pod --subresource resize           │   │
│  └──────────────────────────────────────────────────┘   │
│                        │                                │
│                        ▼                                │
│  2. Kubelet применяет изменения                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  - CPU: изменение на месте (без перезапуска)     │   │
│  │  - Memory: изменение с перезапуском (если нужно)│   │
│  │  - Минимальное прерывание                        │   │
│  └──────────────────────────────────────────────────┘   │
│                        │                                │
│                        ▼                                │
│  3. Pod продолжает работать с новыми ресурсами          │
└─────────────────────────────────────────────────────────┘
```

**Преимущества:**
- Минимальное или отсутствующее прерывание работы
- Сохранение состояния
- Быстрое применение изменений

---

</details>

---

## Связанные темы

- **[K-09-1-Rolling-Updates-and-Rollbacks](K-09-1-Rolling-Updates-and-Rollbacks.md)** — обновления и откаты
- **[K-09-2-Command-and-Arguments-Docker](K-09-2-Command-and-Arguments-Docker.md)** — команды и аргументы в Docker контейнерах
- **[K-09-3-Command-and-Arguments-k8s](K-09-3-Command-and-Arguments-k8s.md)** — команды и аргументы в Kubernetes Pod
- **[K-09-4-Environment-Variables-in-Applications](K-09-4-Environment-Variables-in-Applications.md)** — переменные среды в приложениях
- **[K-09-5-Configure-ConfigMaps-in-Applications](K-09-5-Configure-ConfigMaps-in-Applications.md)** — настройка ConfigMaps в приложениях
- **[K-09-6-Configure-Secrets-in-Applications](K-09-6-Configure-Secrets-in-Applications.md)** — настройка Secrets в приложениях
- **[K-09-7-Multi-Container-PODs](K-09-7-Multi-Container-PODs.md)** — мультиконтейнерные Pod
- **[K-09-8-Init-Containers](K-09-8-Init-Containers.md)** — Init контейнеры
- **[K-09-9-Self-Healing-Apps](K-09-9-Self-Healing-Apps.md)** — самовосстанавливающиеся приложения
- **[K-09-10-Autoscaling-Benefits](K-09-10-Autoscaling-Benefits.md)** — преимущества автоматического масштабирования
- **[K-09-11-HPA-Architecture](K-09-11-HPA-Architecture.md)** — архитектура Horizontal Pod Autoscaler
- **[K-09-12-VPA-Architecture](K-09-12-VPA-Architecture.md)** — архитектура Vertical Pod Autoscaler
- **[K-09-13-Pod-In-Place-Resize](K-09-13-Pod-In-Place-Resize.md)** — изменение размера Pod на месте **← Текущая тема**

---

## Что дальше?

In-Place Pod Resize позволяет динамически настраивать ресурсы контейнеров без необходимости пересоздания Pod, что особенно полезно в сочетании с Vertical Pod Autoscaler для автоматической оптимизации использования ресурсов.

---
