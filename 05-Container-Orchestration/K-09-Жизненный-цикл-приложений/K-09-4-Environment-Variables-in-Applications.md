[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔄 K-09-Жизненный-цикл-приложений](../../README.md#-k-09-жизненный-цикл-приложений)

---

# 🔧K-09-4-Environment-Variables-in-Applications
>Переменные окружения в Kubernetes: прямое указание через env (name и value), использование valueFrom для ConfigMaps и Secrets

---

<details>
<summary><b>🎯Введение: переменные окружения в Kubernetes</b></summary>

---

## О чем эта лекция

Привет и добро пожаловать. В этой лекции мы увидим, как установить переменные среды в Kubernetes.

В мире Docker мы используем ключ `-e` для того, чтобы прокинуть какие-то данные в контейнер в качестве переменных окружения.

Этот же подход реализован в Kubernetes, но в нем он более разнообразный.

Данный файл определения POD использует тот же образ, что и команда docker выше.

---

## Сравнение с Docker

В Docker:

```bash
docker run -e ENV=production -e PORT=8080 my-app
```

В Kubernetes:

```yaml
apiVersion: v1
kind: Pod
spec:
  containers:
  - name: app
    image: my-app
    env:
    - name: ENV
      value: "production"
    - name: PORT
      value: "8080"
```

---

</details>

<details>
<summary><b>📝Прямое указание переменных окружения: env</b></summary>

---

## Свойство env в Pod манифесте

Итак, в Docker для установки переменной окружения мы запускаем контейнер с опцией `-e`, а в манифесте у нас свойство `env`, расположенное в секции контейнера.

`env` — это массив.

Таким образом, каждый элемент в свойстве `env` начинается с тире, обозначающего элемент в массиве.

У каждого элемента есть `name`, это название переменной и свойство `value`, соответственно ее значение.

---

## Формат env

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
  - name: app
    image: my-app
    env:
    - name: ENV
      value: "production"
    - name: PORT
      value: "8080"
    - name: DATABASE_URL
      value: "postgresql://localhost:5432/mydb"
```

---

## Структура элемента env

Каждый элемент массива `env` содержит:

- ✅ **`name`** — название переменной окружения
- ✅ **`value`** — значение переменной

```yaml
env:
- name: VARIABLE_NAME
  value: "variable_value"
```

---

## Пример полного Pod манифеста

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: web-app
spec:
  containers:
  - name: nginx
    image: nginx:1.21
    env:
    - name: NGINX_HOST
      value: "0.0.0.0"
    - name: NGINX_PORT
      value: "80"
    - name: ENVIRONMENT
      value: "production"
```

---

## Использование переменных в контейнере

Определив таким образом переменные среды, ты сможешь обращаться к ним из контейнера после запуска.

Например, в Python:

```python
import os
env = os.getenv('ENV')
port = os.getenv('PORT')
```

В Node.js:

```javascript
const env = process.env.ENV;
const port = process.env.PORT;
```

В Bash:

```bash
echo $ENV
echo $PORT
```

---

</details>

<details>
<summary><b>🔗Использование ConfigMaps и Secrets: valueFrom</b></summary>

---

## Альтернативный способ: valueFrom

То, что мы только что увидели, было прямым способом указания переменных среды с использованием простого формата пары `ключ-значение`.

Однако есть и другие способы установки переменных среды, такие как использование configmaps и secrets.

Разница с этим способом задания заключается в том, что вместо прямого указания значений переменных с помощью свойства `value`, мы говорим откуда брать эти значения свойством `valueFrom`.

А далее ссылку на объект, где лежат данные.

Мы обсудим configmaps и secrets в деталях в следующих лекциях.

---

## Использование ConfigMap через valueFrom

Вместо прямого указания значения:

```yaml
env:
- name: ROCKET_SIZE
  value: "average"  # Прямое значение
```

Используем `valueFrom` для ссылки на ConfigMap:

```yaml
env:
- name: ROCKET_SIZE
  valueFrom:
    configMapKeyRef:
      name: rockets-config
      key: ROCKET_SIZE
```

---

## Использование Secret через valueFrom

Аналогично для Secrets:

```yaml
env:
- name: API_KEY
  valueFrom:
    secretKeyRef:
      name: app-secrets
      key: api-key
```

---

## Полный пример с valueFrom

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
  - name: app
    image: my-app
    env:
    - name: ROCKET_SIZE
      valueFrom:
        configMapKeyRef:
          name: rockets-config
          key: ROCKET_SIZE
    - name: API_KEY
      valueFrom:
        secretKeyRef:
          name: app-secrets
          key: api-key
    - name: ENV
      value: "production"  # Прямое значение
```

---

## Структура valueFrom

Свойство `valueFrom` может ссылаться на:

- ✅ **`configMapKeyRef`** — значение из ConfigMap
- ✅ **`secretKeyRef`** — значение из Secret
- ✅ **`fieldRef`** — значение из полей Pod (например, `metadata.name`)
- ✅ **`resourceFieldRef`** — значение из ресурсов контейнера

---

## Пример с fieldRef

Можно использовать поля самого Pod:

```yaml
env:
- name: POD_NAME
  valueFrom:
    fieldRef:
      fieldPath: metadata.name
- name: POD_NAMESPACE
  valueFrom:
    fieldRef:
      fieldPath: metadata.namespace
- name: POD_IP
  valueFrom:
    fieldRef:
      fieldPath: status.podIP
```

---

## Преимущества valueFrom

✅ **Централизованное управление** — данные хранятся в ConfigMaps/Secrets
✅ **Безопасность** — секреты не хранятся в манифестах Pod
✅ **Переиспользование** — один ConfigMap/Secret для многих Pods
✅ **Динамические значения** — можно использовать поля Pod

---

</details>

<details>
<summary><b>⚖️Сравнение подходов</b></summary>

---

## Прямое значение vs valueFrom

| Подход | Использование | Преимущества | Недостатки |
|--------|---------------|--------------|------------|
| **`value`** | Простые, статические значения | Простота, наглядность | Сложно управлять при множестве Pods |
| **`valueFrom.configMapKeyRef`** | Конфигурационные данные | Централизованное управление | Требует создания ConfigMap |
| **`valueFrom.secretKeyRef`** | Секретные данные | Безопасность | Требует создания Secret |
| **`valueFrom.fieldRef`** | Метаданные Pod | Динамические значения | Ограниченный набор полей |

---

## Когда использовать каждый подход

Используй `value` для:
- ✅ Простых, статических значений
- ✅ Значений, специфичных для конкретного Pod
- ✅ Быстрого прототипирования

Используй `valueFrom` для:
- ✅ Конфигурационных данных (ConfigMap)
- ✅ Секретных данных (Secret)
- ✅ Метаданных Pod (fieldRef)
- ✅ Переиспользования значений между Pods

---

</details>

<details>
<summary><b>💡Практические примеры</b></summary>

---

## Пример 1: Только прямые значения

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: simple-app
spec:
  containers:
  - name: app
    image: my-app:latest
    env:
    - name: ENVIRONMENT
      value: "development"
    - name: LOG_LEVEL
      value: "debug"
    - name: PORT
      value: "3000"
```

---

## Пример 2: Комбинация value и valueFrom

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: web-app
spec:
  containers:
  - name: app
    image: web-app:latest
    env:
    # Прямые значения
    - name: ENVIRONMENT
      value: "production"
    # Из ConfigMap
    - name: DATABASE_URL
      valueFrom:
        configMapKeyRef:
          name: app-config
          key: database_url
    # Из Secret
    - name: API_KEY
      valueFrom:
        secretKeyRef:
          name: app-secrets
          key: api-key
    # Из полей Pod
    - name: POD_NAME
      valueFrom:
        fieldRef:
          fieldPath: metadata.name
```

---

## Пример 3: Использование fieldRef

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: app-with-metadata
spec:
  containers:
  - name: app
    image: my-app:latest
    env:
    - name: POD_NAME
      valueFrom:
        fieldRef:
          fieldPath: metadata.name
    - name: POD_NAMESPACE
      valueFrom:
        fieldRef:
          fieldPath: metadata.namespace
    - name: POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
    - name: NODE_NAME
      valueFrom:
        fieldRef:
          fieldPath: spec.nodeName
```

---

</details>

<details>
<summary><b>🔍Проверка переменных окружения</b></summary>

---

## Просмотр переменных в запущенном Pod

```bash
# Просмотр всех переменных окружения
kubectl exec <pod-name> -- env

# Просмотр конкретной переменной
kubectl exec <pod-name> -- printenv VARIABLE_NAME

# Просмотр переменных с фильтром
kubectl exec <pod-name> -- env | grep ROCKET
```

---

## Проверка манифеста Pod

```bash
# Описание Pod (показывает env переменные)
kubectl describe pod <pod-name>

# Просмотр манифеста Pod
kubectl get pod <pod-name> -o yaml
```

---

## Отладка проблем

Если переменные окружения не работают:

1. ✅ Проверь синтаксис YAML манифеста
2. ✅ Проверь, что Pod пересоздан после изменений
3. ✅ Проверь логи Pod: `kubectl logs <pod-name>`
4. ✅ Проверь описание Pod: `kubectl describe pod <pod-name>`
5. ✅ Проверь, что ConfigMap/Secret существует (если используешь valueFrom)

---

</details>

---

## Резюме

✅ **Прямое указание переменных окружения:**
- Используй свойство `env` в секции контейнера
- Формат: массив с `name` и `value`
- Подходит для простых, статических значений

✅ **Использование valueFrom:**
- Вместо `value` используй `valueFrom` для ссылки на внешние источники
- `configMapKeyRef` — для значений из ConfigMap
- `secretKeyRef` — для значений из Secret
- `fieldRef` — для метаданных Pod

✅ **Преимущества valueFrom:**
- Централизованное управление конфигурацией
- Безопасность (секреты не в манифестах)
- Переиспользование между Pods
- Динамические значения из полей Pod

✅ **Практика:**
- Используй `value` для простых случаев
- Используй `valueFrom` для конфигураций и секретов
- Комбинируй оба подхода при необходимости
- Проверяй переменные через `kubectl exec`

> 💡 **Совет:** Прямое указание через `value` подходит для простых случаев, но для production используй `valueFrom` с ConfigMaps и Secrets для централизованного управления и безопасности. Мы обсудим configmaps и secrets в деталях в следующих лекциях.

---
