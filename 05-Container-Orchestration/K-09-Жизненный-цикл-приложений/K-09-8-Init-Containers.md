[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔄 K-09-Жизненный-цикл-приложений](../../README.md#-k-09-жизненный-цикл-приложений)

---

# 🚀K-09-8-Init-Containers
>Init Containers в Kubernetes: предварительная инициализация, последовательное выполнение, обработка ошибок, примеры использования

---

<details>
<summary><b>🚀Введение: что такое Init Containers</b></summary>

---

## О чем эта лекция

Привет, и добро пожаловать на лекцию, где мы обсудим init containers.

В POD с несколькими контейнерами ожидается, что каждый контейнер будет запускать процесс, который остается активным в течение всего жизненного цикла POD.

Например, в мульти-контейнерном POD, о котором мы говорили ранее, в есть веб-приложение и агент мониторинга ресурсов. Зная о преимуществах PODs с несколькими контейнерами, мы полагаем что процесс, запущенный в контейнере агента останется активным, пока работает веб-приложение.

А если какой-либо из root-процессов этих контейнеров выходит из строя, судьба контейнеров будет общей - POD перезапустится.

---

## Проблема: когда нужна одноразовая инициализация

Но иногда тебе может понадобиться запустить процесс, который выполняется в контейнере-соседе, но результат этой работы будет использоваться основным приложением.

Например:
- Процесс, который извлекает код или двоичный файл из репозитория
- Процесс, ожидающий запуска внешней службы или базы данных, необходимых основному контейнеру
- Настройка разрешений или создание необходимых директорий
- Загрузка конфигурационных файлов

Эта задача будет запускаться только один раз при первоначальном создании POD, а в дальнейшем этот контейнер не требуется для функционирования основного.

---

## Решение: Init Containers

Вот тут-то нам и пригодятся init containers.

---

## Сравнение: обычный POD vs POD с Init Container

### Обычный POD (много-контейнерный)

```
┌─────────────────────────────────────┐
│           POD создается              │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────┐ │
│  │   Container  │  │   Container  │ │
│  │   (app)      │  │  (sidecar)   │ │
│  │              │  │              │ │
│  │  Запускаются │  │  Запускаются │ │
│  │  одновременно│  │  одновременно│ │
│  └──────────────┘  └──────────────┘ │
│                                     │
│  Оба работают в течение всего       │
│  жизненного цикла POD               │
└─────────────────────────────────────┘
```

### POD с Init Container

```
┌─────────────────────────────────────┐
│           POD создается              │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│  ┌──────────────────────────────┐   │
│  │   Init Container             │   │
│  │   (одноразовая задача)       │   │
│  │                              │   │
│  │  Выполняется → Завершается   │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
              │
              ▼ (только после успешного завершения)
┌─────────────────────────────────────┐
│  ┌──────────────────────────────┐   │
│  │   Main Container             │   │
│  │   (основное приложение)      │   │
│  │                              │   │
│  │  Запускается и работает      │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

</details>

<details>
<summary><b>⚙️Характеристики Init Containers</b></summary>

---

## Какими должны быть Init Containers

Раз эти контейнеры живут совсем недолго, мы можем сказать, какими они должны быть:

- **Легковесными** - не требуют много ресурсов
- **Простыми** - выполняют одну конкретную задачу
- **Быстрыми** - завершаются быстро
- **Идемпотентными** - можно безопасно запускать несколько раз с одинаковым результатом

---

## Идемпотентность

Идемпотентность означает, что операция может быть выполнена многократно без изменения результата. Это важно для init containers, так как они могут перезапускаться при ошибках.

### Пример идемпотентной операции

```bash
# Идемпотентная операция - можно выполнить много раз
mkdir -p /app/data
chmod 755 /app/data

# Не идемпотентная операция - создаст дубликаты
echo "log entry" >> /app/logs.txt
```

---

</details>

<details>
<summary><b>🔧Создание Init Containers</b></summary>

---

## Настройка Init Container

Init container настраивается в POD, как и все другие контейнеры, за исключением того, что он указывается внутри раздела `initContainers`, как ты видишь здесь.

### Базовая структура

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  initContainers:
  - name: init-setup
    image: busybox:latest
    command: ['sh', '-c', 'echo "Initialization complete"']
  containers:
  - name: app
    image: my-app:latest
```

---

## Порядок выполнения

Когда POD создается, первым запускается init container, и процесс в init container должен завершиться до запуска реального контейнера, в котором размещено приложение.

### Последовательность запуска

```
┌─────────────────────────────────────────────────────────┐
│  Шаг 1: POD создается                                    │
│  ┌──────────────────────────────────────────────────┐   │
│  │  kubectl create pod my-app                        │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Шаг 2: Init Container запускается                      │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Init Container: init-setup                      │   │
│  │  ┌────────────────────────────────────────────┐  │   │
│  │  │  Выполняет задачу инициализации            │  │   │
│  │  │  (загрузка, настройка, ожидание)           │  │   │
│  │  └────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼ (ждет завершения)
┌─────────────────────────────────────────────────────────┐
│  Шаг 3: Init Container завершается (exit code 0)        │
│  ┌──────────────────────────────────────────────────┐   │
│  │  ✅ Успешное завершение                          │   │
│  │  Init Container останавливается                  │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼ (только после успеха)
┌─────────────────────────────────────────────────────────┐
│  Шаг 4: Основные контейнеры запускаются                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Main Container: app                             │   │
│  │  ┌────────────────────────────────────────────┐  │   │
│  │  │  Основное приложение запускается           │  │   │
│  │  │  и работает в течение всего жизненного     │  │   │
│  │  │  цикла POD                                  │  │   │
│  │  └────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

> **⚠️ Важно:** Основные контейнеры не запустятся, пока init container не завершится успешно (exit code 0).

---

</details>

<details>
<summary><b>🔗Множественные Init Containers</b></summary>

---

## Последовательное выполнение

Мы также можем настроить несколько таких init containers. Тогда они будут запускаться друг за другом последовательно.

### Пример с несколькими Init Containers

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  initContainers:
  # Первый init container - ждет готовности базы данных
  - name: wait-for-db
    image: busybox:latest
    command: ['sh', '-c', 'until nc -z db-service 3306; do sleep 2; done']
  
  # Второй init container - загружает конфигурацию
  - name: download-config
    image: busybox:latest
    command: ['sh', '-c', 'wget -O /shared/config.json http://config-server/config']
    volumeMounts:
    - name: shared-data
      mountPath: /shared
  
  # Третий init container - настраивает разрешения
  - name: setup-permissions
    image: busybox:latest
    command: ['sh', '-c', 'chmod 755 /shared && chown app:app /shared']
    volumeMounts:
    - name: shared-data
      mountPath: /shared
  
  containers:
  - name: app
    image: my-app:latest
    volumeMounts:
    - name: shared-data
      mountPath: /app/config
  volumes:
  - name: shared-data
    emptyDir: {}
```

---

## Порядок выполнения множественных Init Containers

```
┌─────────────────────────────────────────────────────────┐
│  POD создается                                           │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Init Container #1: wait-for-db                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Ожидает готовности базы данных                  │   │
│  │  [████████████████████] 100%                     │   │
│  │  ✅ Завершен успешно                             │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼ (последовательно)
┌─────────────────────────────────────────────────────────┐
│  Init Container #2: download-config                      │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Загружает конфигурацию                          │   │
│  │  [████████████████████] 100%                     │   │
│  │  ✅ Завершен успешно                             │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼ (последовательно)
┌─────────────────────────────────────────────────────────┐
│  Init Container #3: setup-permissions                   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Настраивает разрешения                          │   │
│  │  [████████████████████] 100%                     │   │
│  │  ✅ Завершен успешно                             │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼ (только после всех успешных)
┌─────────────────────────────────────────────────────────┐
│  Main Container: app                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Основное приложение запускается                 │   │
│  │  Все зависимости готовы                         │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### Важные моменты

- **Последовательное выполнение** - каждый init container ждет завершения предыдущего
- **Все должны завершиться успешно** - если один упадет, POD перезапустится
- **Основной контейнер запускается только после всех** - гарантия готовности всех зависимостей

Каждый init container должен завершиться успешно перед запуском следующего.

---

</details>

<details>
<summary><b>⚠️Обработка ошибок в Init Containers</b></summary>

---

## Что происходит при ошибке?

Ок, а что будет, если процесс в init container не найдет файл и завершиться с ошибкой?

POD будет перезапущен.

### Поведение при ошибке

Если какой-либо из init containers не может быть завершен, Kubernetes повторно перезапускает POD до тех пор, пока цепочка init containers не завершится успешно.

Ну разумеется, если ты не установил `restartPolicy` в `Never`.

### Схема обработки ошибок

```
┌─────────────────────────────────────────────────────────┐
│  Попытка #1                                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Init Container выполняется...                    │   │
│  │  ❌ Ошибка! (exit code != 0)                     │   │
│  │  Файл не найден / Сервис недоступен              │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Kubernetes обнаруживает ошибку                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  POD перезапускается (restartPolicy: Always)     │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Попытка #2                                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Init Container выполняется снова...             │   │
│  │  ❌ Ошибка снова!                                │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Попытка #N (повторяется до успеха или ограничения)     │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Init Container выполняется...                  │   │
│  │  ✅ Успех! (exit code 0)                       │   │
│  │  Теперь можно запустить основной контейнер      │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### Поведение при разных restartPolicy

```
restartPolicy: Always (по умолчанию)
┌─────────────────────────────────────┐
│  Init Container ошибка              │
│         │                            │
│         ▼                            │
│  POD перезапускается                │
│  (бесконечно, пока не успех)        │
└─────────────────────────────────────┘

restartPolicy: Never
┌─────────────────────────────────────┐
│  Init Container ошибка              │
│         │                            │
│         ▼                            │
│  POD останавливается                │
│  Состояние: Init:Error              │
│  Основной контейнер НЕ запускается   │
└─────────────────────────────────────┘

restartPolicy: OnFailure
┌─────────────────────────────────────┐
│  Init Container ошибка              │
│         │                            │
│         ▼                            │
│  POD перезапускается                │
│  (только при ненулевом exit code)    │
└─────────────────────────────────────┘
```

---

## Пример с обработкой ошибок

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  restartPolicy: Always  # По умолчанию
  initContainers:
  - name: init-setup
    image: busybox:latest
    command: ['sh', '-c', 'test -f /required/file.txt || exit 1']
  containers:
  - name: app
    image: my-app:latest
```

Если init container завершится с ошибкой (exit code != 0), POD будет перезапущен.

---

## RestartPolicy

### Always (по умолчанию)

```yaml
spec:
  restartPolicy: Always
```

POD будет перезапускаться при ошибках в init containers.

### Never

```yaml
spec:
  restartPolicy: Never
```

POD не будет перезапускаться. Если init container завершится с ошибкой, POD останется в состоянии `Init:Error`.

### OnFailure

```yaml
spec:
  restartPolicy: OnFailure
```

POD будет перезапускаться только если контейнер завершится с ненулевым кодом выхода.

---

</details>

<details>
<summary><b>💾Общие Volumes с Init Containers</b></summary>

---

## Совместное использование данных

Init containers могут монтировать те же volumes, что и основные контейнеры, что позволяет передавать данные между ними.

### Схема работы с общим Volume

```
┌─────────────────────────────────────────────────────────┐
│  POD с общим Volume                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │                                                  │   │
│  │  Init Container: download-code                  │   │
│  │  ┌────────────────────────────────────────────┐ │   │
│  │  │  git clone repo → /app/src                 │ │   │
│  │  │  Записывает данные в volume                │ │   │
│  │  └────────────────────────────────────────────┘ │   │
│  │         │                                       │   │
│  │         ▼ (монтирует)                           │   │
│  │  ┌──────────────────────────────────────────┐  │   │
│  │  │  Volume: app-code                       │  │   │
│  │  │  ┌────────────────────────────────────┐ │  │   │
│  │  │  │  /app/src/                         │ │  │   │
│  │  │  │  ├── main.py                       │ │  │   │
│  │  │  │  ├── config.json                   │ │  │   │
│  │  │  │  └── ...                           │ │  │   │
│  │  │  └────────────────────────────────────┘ │  │   │
│  │  └──────────────────────────────────────────┘  │   │
│  │         │                                       │   │
│  │         ▼ (монтирует тот же volume)             │   │
│  │  ┌────────────────────────────────────────────┐ │   │
│  │  │  Main Container: app                        │ │   │
│  │  │  ┌──────────────────────────────────────┐  │ │   │
│  │  │  │  Читает данные из /app               │  │ │   │
│  │  │  │  Использует код, загруженный         │  │ │   │
│  │  │  │  init container'ом                   │  │ │   │
│  │  │  └──────────────────────────────────────┘  │ │   │
│  │  └────────────────────────────────────────────┘ │   │
│  │                                                  │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### Пример с общим Volume

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  initContainers:
  - name: download-code
    image: busybox:latest
    command: ['sh', '-c', 'git clone https://github.com/user/repo.git /app/src']
    volumeMounts:
    - name: app-code
      mountPath: /app
  
  containers:
  - name: app
    image: my-app:latest
    volumeMounts:
    - name: app-code
      mountPath: /app
  volumes:
  - name: app-code
    emptyDir: {}
```

Init container загружает код в volume, который затем доступен основному контейнеру.

---

## Важные замечания

- Init containers имеют доступ к тем же volumes, что и основные контейнеры
- Данные, созданные в init container, доступны основному контейнеру
- Init containers могут монтировать ConfigMaps и Secrets

---

</details>

<details>
<summary><b>📊Практические примеры использования</b></summary>

---

## Пример 1: Ожидание готовности базы данных

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: web-app
spec:
  initContainers:
  - name: wait-for-db
    image: busybox:latest
    command: ['sh', '-c']
    args:
    - |
      until nc -z mysql-service 3306; do
        echo "Waiting for database..."
        sleep 2
      done
      echo "Database is ready!"
  containers:
  - name: web-app
    image: nginx:latest
```

Init container ждет, пока база данных станет доступной, прежде чем запустится основное приложение.

### Схема работы

```
┌─────────────────────────────────────────────────────────┐
│  Init Container: wait-for-db                             │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Проверяет доступность mysql-service:3306        │   │
│  │  ┌────────────────────────────────────────────┐│   │
│  │  │  Попытка 1: ❌ Недоступна                    ││   │
│  │  │  Попытка 2: ❌ Недоступна                    ││   │
│  │  │  Попытка 3: ✅ Доступна!                    ││   │
│  │  └────────────────────────────────────────────┘│   │
│  │  ✅ Завершен успешно                            │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Main Container: web-app                                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Запускается с уверенностью, что БД готова      │   │
│  │  Может сразу подключаться к mysql-service        │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## Пример 2: Загрузка конфигурации

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: app-with-config
spec:
  initContainers:
  - name: download-config
    image: curlimages/curl:latest
    command: ['sh', '-c']
    args:
    - |
      curl -o /shared/app.conf https://config-server/app.conf
      echo "Config downloaded"
    volumeMounts:
    - name: config-volume
      mountPath: /shared
  
  containers:
  - name: app
    image: my-app:latest
    volumeMounts:
    - name: config-volume
      mountPath: /etc/app
  volumes:
  - name: config-volume
    emptyDir: {}
```

---

## Пример 3: Инициализация данных

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: app-with-data
spec:
  initContainers:
  - name: init-data
    image: busybox:latest
    command: ['sh', '-c']
    args:
    - |
      mkdir -p /data/uploads /data/cache
      chmod 755 /data/uploads /data/cache
      echo "Data directories initialized"
    volumeMounts:
    - name: data-volume
      mountPath: /data
  
  containers:
  - name: app
    image: my-app:latest
    volumeMounts:
    - name: data-volume
      mountPath: /app/data
  volumes:
  - name: data-volume
    emptyDir: {}
```

---

## Пример 4: Комплексный сценарий

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: production-app
spec:
  initContainers:
  # Шаг 1: Проверка доступности зависимостей
  - name: check-dependencies
    image: busybox:latest
    command: ['sh', '-c']
    args:
    - |
      nc -z db-service 3306 && \
      nc -z redis-service 6379 && \
      echo "All dependencies are ready"
  
  # Шаг 2: Загрузка секретов
  - name: load-secrets
    image: busybox:latest
    command: ['sh', '-c', 'echo "Secrets loaded"']
    envFrom:
    - secretRef:
        name: app-secrets
  
  # Шаг 3: Настройка окружения
  - name: setup-environment
    image: busybox:latest
    command: ['sh', '-c']
    args:
    - |
      mkdir -p /app/logs /app/tmp
      chmod 777 /app/logs /app/tmp
      echo "Environment setup complete"
    volumeMounts:
    - name: app-storage
      mountPath: /app
  
  containers:
  - name: app
    image: my-app:latest
    volumeMounts:
    - name: app-storage
      mountPath: /app
  volumes:
  - name: app-storage
    emptyDir: {}
```

---

</details>

---

## Резюме

### Основные концепции

- **Init Containers** выполняются перед основными контейнерами
- Они должны завершиться успешно (exit code 0) перед запуском основных контейнеров
- Множественные init containers выполняются **последовательно**, один за другим
- Init containers могут использовать те же volumes, что и основные контейнеры

### Характеристики Init Containers

- **Легковесные** - не требуют много ресурсов
- **Простые** - выполняют одну конкретную задачу
- **Быстрые** - завершаются быстро
- **Идемпотентные** - можно безопасно запускать несколько раз

### Обработка ошибок

- При ошибке в init container POD перезапускается (если `restartPolicy != Never`)
- Kubernetes будет повторять попытки до успешного завершения всех init containers
- При `restartPolicy: Never` POD останется в состоянии `Init:Error`

### Типичные случаи использования

- Ожидание готовности внешних сервисов (база данных, Redis и т.д.)
- Загрузка кода или конфигурации из репозитория
- Инициализация данных и создание необходимых директорий
- Настройка разрешений и прав доступа
- Предварительная валидация зависимостей

### Преимущества

- Разделение логики инициализации от основного приложения
- Гарантия готовности зависимостей перед запуском приложения
- Возможность использовать легковесные образы для инициализации
- Упрощение основного контейнера приложения

---

А на этом все, переходи к практике с init containers, а я жду тебя в следующей лекции!

---

## Связанные темы

- **[K-09-1-Rolling-Updates-and-Rollbacks](K-09-1-Rolling-Updates-and-Rollbacks.md)** — обновления и откаты
- **[K-09-2-Command-and-Arguments-Docker](K-09-2-Command-and-Arguments-Docker.md)** — команды и аргументы в Docker контейнерах
- **[K-09-3-Command-and-Arguments-k8s](K-09-3-Command-and-Arguments-k8s.md)** — команды и аргументы в Kubernetes Pod
- **[K-09-4-Environment-Variables-in-Applications](K-09-4-Environment-Variables-in-Applications.md)** — переменные среды в приложениях
- **[K-09-5-Configure-ConfigMaps-in-Applications](K-09-5-Configure-ConfigMaps-in-Applications.md)** — настройка ConfigMaps в приложениях
- **[K-09-6-Configure-Secrets-in-Applications](K-09-6-Configure-Secrets-in-Applications.md)** — настройка Secrets в приложениях
- **[K-09-7-Multi-Container-PODs](K-09-7-Multi-Container-PODs.md)** — мультиконтейнерные Pod
- **[K-09-8-Init-Containers](K-09-8-Init-Containers.md)** — Init контейнеры **← Текущая тема**
- **[K-09-9-Self-Healing-Apps](K-09-9-Self-Healing-Apps.md)** — самовосстанавливающиеся приложения
- **[K-09-10-Autoscaling-Benefits](K-09-10-Autoscaling-Benefits.md)** — преимущества автоматического масштабирования
- **[K-09-11-HPA-Architecture](K-09-11-HPA-Architecture.md)** — архитектура Horizontal Pod Autoscaler
- **[K-09-12-VPA-Architecture](K-09-12-VPA-Architecture.md)** — архитектура Vertical Pod Autoscaler
- **[K-09-13-Pod-In-Place-Resize](K-09-13-Pod-In-Place-Resize.md)** — изменение размера Pod на месте

---

## Что дальше?

В следующей теме [K-09-9-Self-Healing-Apps](K-09-9-Self-Healing-Apps.md) мы рассмотрим механизмы самовосстановления приложений в Kubernetes, изучим, как контроллеры автоматически восстанавливают работоспособность приложений при сбоях.

---