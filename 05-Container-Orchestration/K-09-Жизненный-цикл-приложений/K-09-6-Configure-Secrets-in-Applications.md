[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔄 K-09-Жизненный-цикл-приложений](../../README.md#-k-09-жизненный-цикл-приложений)

---

# 🔐K-09-6-Configure-Secrets-in-Applications
>Secrets в Kubernetes: создание (императивный и декларативный методы), кодирование base64, внедрение в Pods через envFrom, secretKeyRef и volumes, best practices безопасности

---

<details>
<summary><b>🔐Введение: зачем нужны Secrets</b></summary>

---

## О чем эта лекция

Привет и добро пожаловать на лекцию о Secrets в Kubernetes.

Здесь у нас представлено простое веб-приложение на python, которое подключается к базе данных mysql. В случае успеха приложение отображает успешное сообщение.

Если мы внимательно посмотрим на код, то увидим, что имя пользователя, пароль и имя хоста жестко прописаны в коде:

```python
import mysql.connector

# Проблема: хардкод учетных данных
db_host = "mysql-server"
db_user = "root"
db_password = "db_pass123"

connection = mysql.connector.connect(
    host=db_host,
    user=db_user,
    password=db_password
)

if connection.is_connected():
    print("Успешное подключение к базе данных!")
```

Конечно, хардкодить это плохая идея.

Как мы узнали из предыдущей лекции, одним из вариантов было бы переместить эти значения в ConfigMap. ConfigMap хранит данные конфигурации в простом текстовом формате, в `plain text`.

Так что приемлемо было бы переместить имя хоста и имя пользователя в configMap. Но это определенно не подходящее место для хранения пароля.

Вот это место, где в игру вступают secrets.

---

## Что такое Secrets?

Эти объекты Kubernetes используются для хранения конфиденциальной информации, такой как пароли, токены, ключи и другие чувствительные данные.

Они похожи на configMap, за исключением того, что хранятся в закодированном или хешированном формате. Но из контейнера все же эти значения выглядят уже как обычно, без кодировки.

---

## Два этапа работы с Secrets

Как и в случае с ConfigMaps, работа с secrets состоит из двух этапов:

1. **Создание secret** в начале
2. **Введение его в нужный контейнер**

---

</details>

<details>
<summary><b>⚡Создание Secrets: императивный подход</b></summary>

---

## Команда kubectl create secret

Императивной командой мы можем напрямую указать пары ключ-значение, прописав их в командной строке.

Чтобы создать secret из заданных напрямую значений, запусти команду `kubectl create secret generic`.

Далее в команде следует имя секрета и опция `--from-literal`. Этот параметр `--from-literal` используется для указания пар ключ-значение в самой команде.

### Пример создания Secret

```bash
kubectl create secret generic rockets-secret \
  --from-literal=DB_user=root \
  --from-literal=DB_password=db_pass123
```

В этом примере мы создаем secret с именем `rockets-secret` и двумя парами ключ-значение: `DB_user=root` и `DB_password=db_pass123`.

> **💡 Примечание:** Параметр `--from-literal` можно указать несколько раз для добавления нескольких пар ключ-значение.

---

## Создание Secret из файла

Однако если у тебя много секретной информации, это станет очень громоздкой и сложной конструкцией. Здесь лучше воспользоваться другим способом - секретные данные через файл.

Используй параметр `--from-file`, чтобы указать путь к файлу, содержащему необходимые данные в формате properties.

### Пример с файлом

```bash
# Создание secret из файла
kubectl create secret generic rockets-secret \
  --from-file=db.properties

# Или указать несколько файлов
kubectl create secret generic rockets-secret \
  --from-file=db.properties \
  --from-file=ssh-key=~/.ssh/id_rsa
```

Данные из этого файла читаются и сохраняются под именем secret. Можно указать несколько файлов или задать полю secret значение из файла, как ты видишь. Это, например, удобно для хранения ssh-ключей.

---

</details>

<details>
<summary><b>📝Создание Secrets: декларативный подход</b></summary>

---

## Файл определения Secret

Теперь давай рассмотрим декларативный подход. Для этого мы создаем файл определения, точно так же, как мы делали это для ConfigMap.

Файл имеет `apiVersion`, `kind`, `metadata` и `data`.

### Структура манифеста

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: rockets-secret
data:
  DB_user: cm9vdA==
  DB_password: ZGJfcGFzczEyMw==
```

- **ApiVersion:** `v1`
- **Kind:** `Secret`
- Под метаданными укажем название secret. Назовем его `rockets-secret`.
- В поле `data` добавим секретные данные в формате ключ-значение.

---

## Важно: кодирование base64

Я уже упоминал одну особенность secrets - они хранят чувствительные данные в закодированном формате. Здесь мы указали данные в виде обычного текста, что не очень безопасно.

Поэтому при создании secret с декларативным подходом мы должны указывать секретные значения в хешированном формате.

> **⚠️ Важно:** Кодировка - это не шифрование. Данные могут быть приведены в читаемому виду любым, кто получил к ним доступ. Поэтому имей в виду, что информацию в secrets можно дополнительно зашифровать и ограничить доступ к secret с помощью правил RBAC.

---

## Кодирование данных в base64

Теперь мы можем создать свой secret, применив манифест. Но прежде тебе требуется сконвертировать данные в такую закодированную форму.

Как преобразовать данные из обычного текста в такой закодированный формат? На машине с Linux можно воспользоваться командой `echo -n`, за которой следует текст, который требуется преобразовать.

### Команда для кодирования

```bash
# Кодирование одного значения
echo -n "root" | base64
# Вывод: cm9vdA==

# Кодирование пароля
echo -n "db_pass123" | base64
# Вывод: ZGJfcGFzczEyMw==
```

В данном случае `root`. Далее передать его по pipe утилите `base64`.

---

</details>

<details>
<summary><b>👀Просмотр и управление Secrets</b></summary>

---

## Просмотр списка Secrets

Чтобы просмотреть секреты, запусти команду `kubectl get secrets`.

```bash
kubectl get secrets
```

В выводе перечислены все созданные secrets. Как видишь помимо наших здесь есть другие, которые Kubernetes создал для внутренних целей.

### Пример вывода

```
NAME                  TYPE                                  DATA   AGE
rockets-secret        Opaque                                2      5m
default-token-xyz     kubernetes.io/service-account-token   3      1d
```

---

## Детальная информация о Secret

Чтобы просмотреть дополнительную информацию о созданном secret запустите команду `kubectl describe secret`.

```bash
kubectl describe secret rockets-secret
```

Она покажет атрибуты секрета, но скроет сами значения.

---

## Просмотр значений Secret

Для просмотра значений запусти команду `kubectl get secret` с выводом в формате YAML, используя параметр `-o`, как ты видишь здесь.

```bash
kubectl get secret rockets-secret -o yaml
```

Теперь ты можешь увидеть эти хешированные значения, осталось их только декодировать по 64-основанию.

---

## Декодирование значений

Используй ту же команду base64, которая была ранее для кодирования, но на этот раз с опцией `--decode`.

```bash
# Декодирование значения
echo "cm9vdA==" | base64 --decode
# Вывод: root

# Или из YAML напрямую
kubectl get secret rockets-secret -o jsonpath='{.data.DB_user}' | base64 --decode
```

---

</details>

<details>
<summary><b>🔗Внедрение Secrets в Pods</b></summary>

---

## Шаг 2: внедрение Secret в POD

Ок, мы создали секрет. Теперь перейдем к шагу 2 - встроить secret в POD.

Здесь у меня есть простой файл определения для POD, который запускает мое приложение.

---

## Использование envFrom

Чтобы внедрить переменную среды, добавь в контейнер новое свойство с именем `envFrom`.

Свойство `envFrom` представляет собой список, поэтому мы можем передать столько переменных среды, сколько требуется. Каждый элемент в этом списке соответствует секретному элементу. Укажи название secret, который мы создали ранее.

### Пример Pod с envFrom

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
  - name: app
    image: my-app:latest
    envFrom:
    - secretRef:
        name: rockets-secret
```

Создание ссылки на secret в файле определения POD делает эти конфиденциальные данные доступными в качестве переменных среды для приложения в контейнере.

---

## Использование secretKeyRef

Мы только что увидели внедрение секретов в качестве переменных среды в POD. Есть и другие способы ввести secret в POD.

Ты можешь также частично вводить secret, как отдельные переменные среды с помощью конструкции `secretKeyRef`.

### Пример с secretKeyRef

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
  - name: app
    image: my-app:latest
    env:
    - name: DB_USER
      valueFrom:
        secretKeyRef:
          name: rockets-secret
          key: DB_user
    - name: DB_PASSWORD
      valueFrom:
        secretKeyRef:
          name: rockets-secret
          key: DB_password
```

---

## Использование Volumes

Или ввести secret полностью в виде файлов из смонтированного тома.

Для подключения секретной информации выбирай способ, который тебя наиболее устраивает.

### Пример с Volume

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
  - name: app
    image: my-app:latest
    volumeMounts:
    - name: secret-volume
      mountPath: /etc/secrets
      readOnly: true
  volumes:
  - name: secret-volume
    secret:
      secretName: rockets-secret
```

В случае тома, каждый атрибут секрета будет представлен в отдельном файле, название файла будет именем атрибута в secret, а содержимое- значением этого секретного поля.

В этом случае, поскольку у нас есть четыре атрибута в нашем secret, то в смонтированной внутри контейнера папке создадутся четыре файла с названиями атрибутов.

Если мы посмотрим на содержимое `DB_password`, то мы увидим в нем пароль в открытом виде.

Механизм здесь такой же, как в Configmap, и если мы изменим secret, эти файлы также автоматически обновятся.

---

</details>

<details>
<summary><b>🛡️Безопасность Secrets: важные замечания</b></summary>

---

## Помни о безопасности

Напоследок несколько мыслей о хранении секретных данных.

Помни, что secrets кодируют данные в формате base64. Любой, у кого есть secret в кодировке base64, может легко его расшифровать. Таким образом, секреты нужно считать небезопасными.

---

## Концепция безопасности Secrets

Концепция безопасности secrets в Kubernetes немного сбивает с толку. Сообщество Kubernetes исходит из того, что secrets `более безопасный вариант` для хранения конфиденциальных данных.

Они безопаснее, чем хранение в виде обычного текста, поскольку снижают риск случайного раскрытия паролей и других чувствительных данных.

И здесь небезопасен не сам секрет, а методы, связанные с ним. Секреты не зашифрованы, поэтому в этом смысле это не безопаснее.

Однако некоторые продвинутые методы использования secrets делают его более безопасным.

---

## Best Practices для безопасности

Мы поговорим об этом в разделе security далее в курсе, но давай кратко пробежим best practices:

### ✅ Рекомендации

- **Перестань отслеживать secrets в SVC-репозиториях** - организуй процесс контроля изменений для secrets иначе
- **Используй шифрование при хранении** - чтобы они хранились в зашифрованном виде в ETCD
- **Отмени доступ любому пользователю для secrets-API** для действий `watch` и `list`
- **Пусть только PODs, которым необходимы эти конкретные данные получают к ним доступ**
- **Kubelet хранит секрет в tmpfs** - чтобы секрет не записывался на диск
- **Твои приложения также не должны сохранять их в постоянной памяти и не показывать в логах**
- **Использование SSL/TLS**
- **Аккуратнее с root привилегиями в кластере**

---

## Внешние решения для управления секретами

Есть также специальные решения:

### Cloud-решения

- **AWS Secrets Manager**
- **Google Cloud Platform KMS**
- **Azure Key Vault**

### Open-source решения

- **Helm Secrets**
- **HashiCorp Vault**
- **Conjur**

Которые помогут тебе в хранении.

> **💡 Примечание:** Я надеюсь, что в скором времени мы поговорим об этом подробно в курсе сертифицированного специалиста по безопасности Kubernetes от rotoro.cloud.

---

</details>

---

## Резюме

### Основные концепции

- **Secrets** используются для хранения конфиденциальной информации (пароли, токены, ключи)
- Secrets хранятся в **base64-кодированном** формате (не шифрование!)
- Работа с Secrets состоит из двух этапов: **создание** и **внедрение в Pod**

### Создание Secrets

- **Императивный подход:** `kubectl create secret generic` с `--from-literal` или `--from-file`
- **Декларативный подход:** YAML манифест с данными в base64
- Кодирование: `echo -n "value" | base64`
- Декодирование: `echo "encoded" | base64 --decode`

### Внедрение в Pods

- **envFrom:** все ключи Secret как переменные среды
- **secretKeyRef:** отдельные ключи Secret как переменные среды
- **Volumes:** Secret как файлы в смонтированном томе

### Безопасность

- Secrets **не зашифрованы**, только закодированы в base64
- Не хранить в Git-репозиториях
- Использовать шифрование ETCD
- Ограничить доступ через RBAC
- Использовать внешние решения (Vault, AWS Secrets Manager и т.д.)

---

## Связанные темы

- **[K-09-1-Rolling-Updates-and-Rollbacks](K-09-1-Rolling-Updates-and-Rollbacks.md)** — обновления и откаты
- **[K-09-2-Command-and-Arguments-Docker](K-09-2-Command-and-Arguments-Docker.md)** — команды и аргументы в Docker контейнерах
- **[K-09-3-Command-and-Arguments-k8s](K-09-3-Command-and-Arguments-k8s.md)** — команды и аргументы в Kubernetes Pod
- **[K-09-4-Environment-Variables-in-Applications](K-09-4-Environment-Variables-in-Applications.md)** — переменные среды в приложениях
- **[K-09-5-Configure-ConfigMaps-in-Applications](K-09-5-Configure-ConfigMaps-in-Applications.md)** — настройка ConfigMaps в приложениях
- **[K-09-6-Configure-Secrets-in-Applications](K-09-6-Configure-Secrets-in-Applications.md)** — настройка Secrets в приложениях **← Текущая тема**
- **[K-09-7-Multi-Container-PODs](K-09-7-Multi-Container-PODs.md)** — мультиконтейнерные Pod
- **[K-09-8-Init-Containers](K-09-8-Init-Containers.md)** — Init контейнеры
- **[K-09-9-Self-Healing-Apps](K-09-9-Self-Healing-Apps.md)** — самовосстанавливающиеся приложения
- **[K-09-10-Autoscaling-Benefits](K-09-10-Autoscaling-Benefits.md)** — преимущества автоматического масштабирования
- **[K-09-11-HPA-Architecture](K-09-11-HPA-Architecture.md)** — архитектура Horizontal Pod Autoscaler
- **[K-09-12-VPA-Architecture](K-09-12-VPA-Architecture.md)** — архитектура Vertical Pod Autoscaler
- **[K-09-13-Pod-In-Place-Resize](K-09-13-Pod-In-Place-Resize.md)** — изменение размера Pod на месте

---

## Что дальше?

В следующей теме [K-09-7-Multi-Container-PODs](K-09-7-Multi-Container-PODs.md) мы рассмотрим мультиконтейнерные Pod, изучим различные паттерны использования нескольких контейнеров в одном Pod и их взаимодействие.

---