[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [🔄 K-09-Жизненный-цикл-приложений](../../README.md#-k-09-жизненный-цикл-приложений)

---

# 🐳K-09-2-Command-and-Arguments-Docker
>Команды и аргументы в Docker контейнерах: CMD vs ENTRYPOINT, точки входа, переопределение команд, форматы записи (shell и JSON)

---

<details>
<summary><b>🎯Введение: команды в Docker контейнерах</b></summary>

---

## О чем эта лекция

Эта и следующая лекции ведут нас к пониманию команд и аргументов в контейнерных средах.

Эта тема не указана в качестве обязательной в учебной программе сертификации, но я думаю, что ее важно объяснить, поскольку этой теме обычно не уделяют внимания, что ведет к путанице.

Давай сначала освежим нашу память о командах в докер-контейнерах. В следующей лекции мы будем смотреть с перспективы Kubernetes, а сейчас поговорим о аргументах команд и точках входа в Docker.

---

</details>

<details>
<summary><b>❓Проблема: почему контейнер сразу останавливается?</b></summary>

---

## Простой сценарий

Предположим, что мы запустили контейнер с образом ubuntu:

```bash
docker run ubuntu
```

Когда мы ввели команду `docker run ubuntu`, Docker запустил экземпляр образа ubuntu и немедленно сделал выход из контейнера.

Если сейчас посмотреть список работающих контейнеров, то мы не увидим их:

```bash
docker ps
```

Если же запустить `docker ps -a`, тем самым запросив список всех контейнеров на хосте, мы увидим наш новый контейнер, он будет остановлен со статусом `exited`.

---

## Почему это не работает как виртуальная машина?

Как мы уже говорили, контейнеры не предназначены для размещения операционных систем.

Они подходят для некоторых процессов вроде:
- ✅ Запуска веб-сервера
- ✅ Базы данных
- ✅ Аналитической задачи

Когда задача завершена, контейнер останавливается.

Жизнь в нем поддерживает работающий внутри процесс.

Т.е. если веб-сервер, или база данных, или другой запущенный процесс внутри контейнера остановится или завершится, то контейнер немедленно выйдет.

---

## Что определяет процесс в контейнере?

Итак, что определяет, какой процесс будет запущен в контейнере?

Если посмотреть на Dockerfile такого популярного приложения как nginx, ты увидишь инструкцию `CMD`.

Эта инструкция определяет команду, которая будет запускаться в контейнере при запуске его из образа.

Это команда `nginx`.

Для официального образа MySQL в Dockerfile будет команда `mysqld`.

---

## Проблема с Ubuntu

Ранее мы пытались запустить контейнер с пустой ОС Ubuntu.

Посмотрим на Dockerfile этого образа и мы увидим, что команда по умолчанию там `bash`.

Но как мы знаем, `bash` — это не совсем такой процесс, вроде веб-сервера или базы данных.

Это оболочка, которая ждет ввода с терминала и, если она не может найти терминал, она завершается.

Когда мы запускали контейнер с Ubuntu ранее, Docker создавал контейнер из образа Ubuntu и запускал программу `bash` по умолчанию.

Терминал к контейнеру при запуске Docker не подключал.

Таким образом, программа `bash` не находила терминал и завершала работу.

А поскольку процесс, который был запущен при создании контейнера, завершался, то и контейнер также останавливался.

---

</details>

<details>
<summary><b>⚙️Инструкция CMD: команда по умолчанию</b></summary>

---

## Что такое CMD?

Инструкция `CMD` определяет команду, которая будет запускаться в контейнере при запуске его из образа.

Это команда по умолчанию.

---

## Примеры CMD в популярных образах

Nginx:

```dockerfile
CMD ["nginx"]
```

MySQL:

```dockerfile
CMD ["mysqld"]
```

Ubuntu:

```dockerfile
CMD ["bash"]
```

---

## Переопределение CMD при запуске

Как нам указать другую команду при старте контейнера?

Один из вариантов — добавить нужную команду к `docker run` и таким образом переопределить команду по умолчанию, указанную в образе.

В этом случае мы выполним `docker run ubuntu` с командой `sleep 5` в качестве дополнительного параметра:

```bash
docker run ubuntu sleep 5
```

Теперь, когда контейнер стартует, будет запущена команда `sleep`.

Она будет выполняться 5 секунд, после чего удачно завершится, что соответственно и завершит выполнение контейнера.

---

</details>

<details>
<summary><b>🏗️Создание собственного образа с CMD</b></summary>

---

## Как сделать изменения постоянными?

Скажем, тебе нужно всегда запускать команду `sleep`.

Создай свой собственный образ из базового образа ubuntu и укажи новую команду с помощью инструкции `CMD`.

---

## Форматы записи CMD

Есть несколько разных возможностей указать команду:

### Формат JSON-массива (рекомендуется)

```dockerfile
FROM ubuntu
CMD ["sleep", "5"]
```

Не забудь, если ты укажешь это в JSON, первым элементом массива должен быть исполняемый файл.

В этом случае инструкция CMD написана неверно:

```dockerfile
CMD ["sleep 5"]  # ❌ НЕПРАВИЛЬНО
```

"sleep 5" команда и параметр указаны вместе. Это не сработает, команда и параметры должны быть разнесены в отдельные элементы массива:

```dockerfile
CMD ["sleep", "5"]  # ✅ ПРАВИЛЬНО
```

### Формат shell

```dockerfile
FROM ubuntu
CMD sleep 5
```

---

## Создание образа

С помощью Dockerfile, который ты видишь, я создам свой новый образ `ubuntu-sleeper` с помощью команды:

```bash
docker build -t ubuntu-sleeper .
```

Теперь можно просто запустить его контейнер используя:

```bash
docker run ubuntu-sleeper
```

Я получил предполагаемый результат — контейнер запускается, ждет 5 секунд и выходит.

Это количество секунд захардкожено в моей инструкции `CMD`.

---

</details>

<details>
<summary><b>🔧Проблема: как изменить параметры без изменения образа?</b></summary>

---

## Проблема с захардкоженными значениями

Как мне изменить этот промежуток, не меняя образ?

Один из путей — запустить контейнер с добавлением новой команды:

```bash
docker run ubuntu-sleeper sleep 10
```

В этом случае команда `sleep 10` выполнится при старте и успешно отработает положенные ей 10 секунд.

Но это решение так себе.

Само название `ubuntu-sleeper` подразумевает, что контейнер будет спать, не хотелось бы дополнительно указывать инструкцию вроде "проспи 10 секунд".

---

## Более изящное решение

Что-то более изящное, например не указывать команду `sleep`, а указать только количество нужных секунд, что-то вроде:

```bash
docker run ubuntu-sleeper 10
```

Я хочу, чтобы контейнер меня при этом понял и автоматически вызвал команду `sleep` с моим параметром.

Вот здесь появляется инструкция `ENTRYPOINT`.

---

</details>

<details>
<summary><b>🎯Инструкция ENTRYPOINT: фиксированная точка входа</b></summary>

---

## Что такое ENTRYPOINT?

Инструкция `ENTRYPOINT` похожа на инструкцию `CMD`, в ней ты также указываешь программу или команду, которая будет запущена в контейнере.

Но есть ключевое отличие:

- **`CMD`** — параметры командной строки **полностью заменяют** значение CMD
- **`ENTRYPOINT`** — параметры командной строки **добавляются** к значению ENTRYPOINT

---

## Пример с ENTRYPOINT

Создадим Dockerfile:

```dockerfile
FROM ubuntu
ENTRYPOINT ["sleep"]
```

Теперь при запуске:

```bash
docker run ubuntu-sleeper 10
```

В этом случае `10` будет добавлено в значение `ENTRYPOINT` и будет собрана единая команда.

Таким образом контейнер запустится с командой `sleep 10`.

---

## Разница между CMD и ENTRYPOINT

В случае инструкции `CMD` переданные параметры командной строки будут полностью заменены:

```dockerfile
# Dockerfile
CMD ["sleep", "5"]

# Запуск
docker run ubuntu-sleeper sleep 10
# Выполнится: sleep 10 (CMD полностью заменен)
```

Тогда как в случае `ENTRYPOINT` эти параметры будут добавлены:

```dockerfile
# Dockerfile
ENTRYPOINT ["sleep"]

# Запуск
docker run ubuntu-sleeper 10
# Выполнится: sleep 10 (10 добавлен к ENTRYPOINT)
```

---

</details>

<details>
<summary><b>⚠️Проблема: что если не передать параметры?</b></summary>

---

## Что случится без параметров?

Что случится, если я запущу `docker run ubuntu-sleeper` без добавления количества секунд?

```bash
docker run ubuntu-sleeper
```

Здесь к команде `sleep` добавится пустой параметр, т.е. образуется просто команда `sleep`, и я получу сообщение об отсутствии операнда.

Значит, хорошо бы иметь значение по умолчанию на такой случай.

---

</details>

<details>
<summary><b>🔗Комбинация ENTRYPOINT и CMD</b></summary>

---

## Использование ENTRYPOINT + CMD вместе

Этого можно добиться используя обе инструкции сразу: `ENTRYPOINT` и `CMD`.

В `ENTRYPOINT` пропишу исполняемую команду, а в `CMD` значение аргумента по умолчанию:

```dockerfile
FROM ubuntu
ENTRYPOINT ["sleep"]
CMD ["5"]
```

---

## Как это работает?

Теперь команда для старта в контейнере соберется из `ENTRYPOINT` и значения из `CMD`, т.к. мы не передали аргументов в командную строку и тем самым не переопределили `CMD`.

Таким образом получим команду `sleep 5`:

```bash
docker run ubuntu-sleeper
# Выполнится: sleep 5 (ENTRYPOINT + CMD)
```

Если же мы укажем параметр, то инструкция `CMD` будет переопределена:

```bash
docker run ubuntu-sleeper 10
# Выполнится: sleep 10 (ENTRYPOINT + переданный параметр)
```

---

## Важно: формат JSON

**Запомни, чтобы это сработало, всегда нужно указывать `ENTRYPOINT` и `CMD` в формате JSON:**

```dockerfile
# ✅ ПРАВИЛЬНО
ENTRYPOINT ["sleep"]
CMD ["5"]

# ❌ НЕПРАВИЛЬНО (не сработает как ожидается)
ENTRYPOINT sleep
CMD 5
```

---

</details>

<details>
<summary><b>🔄Переопределение ENTRYPOINT во время выполнения</b></summary>

---

## Параметр --entrypoint

Что, если нам потребуется изменить `ENTRYPOINT` во время выполнения?

Использовать какую-то другую команду, например заменить `sleep` на `super-sleep`.

Это возможно используя параметр `--entrypoint`:

```bash
docker run --entrypoint super-sleep ubuntu-sleeper 10
```

В этом случае контейнер будет запущен с командой `super-sleep 10` внутри.

---

## Когда это полезно?

Переопределение `ENTRYPOINT` полезно для:
- ✅ Отладки контейнера
- ✅ Запуска альтернативных команд
- ✅ Временного изменения поведения контейнера

---

</details>

<details>
<summary><b>📊Сравнительная таблица: CMD vs ENTRYPOINT</b></summary>

---

## Основные различия

| Характеристика | CMD | ENTRYPOINT |
|----------------|-----|------------|
| **Переопределение** | Полностью заменяется параметрами командной строки | Параметры добавляются к ENTRYPOINT |
| **Использование** | Значения по умолчанию | Фиксированная команда |
| **Гибкость** | Легко переопределить | Требует `--entrypoint` для изменения |
| **Формат** | JSON массив или shell | JSON массив (рекомендуется) |

---

## Примеры использования

### Только CMD

```dockerfile
FROM ubuntu
CMD ["sleep", "5"]
```

```bash
docker run ubuntu-sleeper          # sleep 5
docker run ubuntu-sleeper sleep 10 # sleep 10 (CMD заменен)
```

### Только ENTRYPOINT

```dockerfile
FROM ubuntu
ENTRYPOINT ["sleep"]
```

```bash
docker run ubuntu-sleeper          # sleep (ошибка: нет параметра)
docker run ubuntu-sleeper 10       # sleep 10
```

### ENTRYPOINT + CMD (рекомендуется)

```dockerfile
FROM ubuntu
ENTRYPOINT ["sleep"]
CMD ["5"]
```

```bash
docker run ubuntu-sleeper          # sleep 5 (ENTRYPOINT + CMD)
docker run ubuntu-sleeper 10       # sleep 10 (ENTRYPOINT + параметр)
```

---

</details>

<details>
<summary><b>💡Практические примеры</b></summary>

---

## Пример 1: Веб-сервер с настраиваемым портом

```dockerfile
FROM nginx
ENTRYPOINT ["nginx"]
CMD ["-g", "daemon off;"]
```

Использование:

```bash
docker run my-nginx                    # nginx -g daemon off;
docker run my-nginx -g "daemon on;"    # nginx -g daemon on;
```

---

## Пример 2: Утилита с параметрами

```dockerfile
FROM ubuntu
ENTRYPOINT ["curl"]
CMD ["--help"]
```

Использование:

```bash
docker run my-curl                    # curl --help
docker run my-curl https://example.com # curl https://example.com
```

---

## Пример 3: Python скрипт

```dockerfile
FROM python:3.9
ENTRYPOINT ["python"]
CMD ["app.py"]
```

Использование:

```bash
docker run my-python                  # python app.py
docker run my-python script.py       # python script.py
```

---

</details>

<details>
<summary><b>⚠️Частые ошибки и как их избежать</b></summary>

---

## Ошибка 1: Неправильный формат JSON

**❌ Неправильно:**

```dockerfile
CMD ["sleep 5"]  # Команда и параметр вместе
```

**✅ Правильно:**

```dockerfile
CMD ["sleep", "5"]  # Команда и параметр разделены
```

---

## Ошибка 2: Смешивание форматов

**❌ Неправильно:**

```dockerfile
ENTRYPOINT sleep
CMD ["5"]  # Смешивание shell и JSON форматов
```

**✅ Правильно:**

```dockerfile
ENTRYPOINT ["sleep"]
CMD ["5"]  # Оба в JSON формате
```

---

## Ошибка 3: ENTRYPOINT без параметров по умолчанию

**❌ Неправильно:**

```dockerfile
ENTRYPOINT ["sleep"]  # Нет значения по умолчанию
```

**✅ Правильно:**

```dockerfile
ENTRYPOINT ["sleep"]
CMD ["5"]  # Есть значение по умолчанию
```

---

</details>

---

## Резюме

✅ **Инструкция `CMD`:**
- Определяет команду по умолчанию
- Полностью заменяется параметрами командной строки
- Используется для значений по умолчанию

✅ **Инструкция `ENTRYPOINT`:**
- Определяет фиксированную точку входа
- Параметры командной строки добавляются к ENTRYPOINT
- Используется для фиксированной команды

✅ **Комбинация `ENTRYPOINT` + `CMD`:**
- `ENTRYPOINT` — фиксированная команда
- `CMD` — аргументы по умолчанию
- Параметры командной строки заменяют `CMD`, но не `ENTRYPOINT`

✅ **Форматы записи:**
- JSON массив (рекомендуется): `CMD ["sleep", "5"]`
- Shell формат: `CMD sleep 5`
- Для комбинации ENTRYPOINT + CMD всегда используй JSON формат

✅ **Переопределение:**
- `CMD` переопределяется параметрами командной строки
- `ENTRYPOINT` переопределяется через `--entrypoint`

✅ **Практика:**
- Используй `ENTRYPOINT` для фиксированной команды
- Используй `CMD` для аргументов по умолчанию
- Всегда указывай оба в JSON формате для совместной работы
- Контейнеры живут пока работает процесс внутри них

> 💡 **Совет:** В большинстве случаев используй комбинацию `ENTRYPOINT` + `CMD`. Это дает гибкость (можно переопределить аргументы) и предсказуемость (команда всегда известна). Помни: контейнеры не предназначены для размещения ОС, они для запуска процессов. Когда процесс завершается, контейнер останавливается.

---

## Связанные темы

- **[K-09-1-Rolling-Updates-and-Rollbacks](K-09-1-Rolling-Updates-and-Rollbacks.md)** — обновления и откаты
- **[K-09-2-Command-and-Arguments-Docker](K-09-2-Command-and-Arguments-Docker.md)** — команды и аргументы в Docker контейнерах **← Текущая тема**
- **[K-09-3-Command-and-Arguments-k8s](K-09-3-Command-and-Arguments-k8s.md)** — команды и аргументы в Kubernetes Pod
- **[K-09-4-Environment-Variables-in-Applications](K-09-4-Environment-Variables-in-Applications.md)** — переменные среды в приложениях
- **[K-09-5-Configure-ConfigMaps-in-Applications](K-09-5-Configure-ConfigMaps-in-Applications.md)** — настройка ConfigMaps в приложениях
- **[K-09-6-Configure-Secrets-in-Applications](K-09-6-Configure-Secrets-in-Applications.md)** — настройка Secrets в приложениях
- **[K-09-7-Multi-Container-PODs](K-09-7-Multi-Container-PODs.md)** — мультиконтейнерные Pod
- **[K-09-8-Init-Containers](K-09-8-Init-Containers.md)** — Init контейнеры
- **[K-09-9-Self-Healing-Apps](K-09-9-Self-Healing-Apps.md)** — самовосстанавливающиеся приложения
- **[K-09-10-Autoscaling-Benefits](K-09-10-Autoscaling-Benefits.md)** — преимущества автоматического масштабирования
- **[K-09-11-HPA-Architecture](K-09-11-HPA-Architecture.md)** — архитектура Horizontal Pod Autoscaler
- **[K-09-12-VPA-Architecture](K-09-12-VPA-Architecture.md)** — архитектура Vertical Pod Autoscaler
- **[K-09-13-Pod-In-Place-Resize](K-09-13-Pod-In-Place-Resize.md)** — изменение размера Pod на месте

---

## Что дальше?

В следующей теме [K-09-3-Command-and-Arguments-k8s](K-09-3-Command-and-Arguments-k8s.md) мы рассмотрим, как переопределять команды и аргументы в Kubernetes Pod, используя поля `command` и `args`, и изучим их связь с Docker CMD и ENTRYPOINT.

---
