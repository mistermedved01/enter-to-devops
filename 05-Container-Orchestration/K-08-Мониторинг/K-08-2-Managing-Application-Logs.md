[🏠 Главная](../../README.md) → [☸️ Container-Orchestration](../../README.md#-container-orchestration) → [📊 K-08-Мониторинг](../../README.md#-k-08-мониторинг)

---

# 📋K-08-2-Managing-Application-Logs
>Управление логами приложений в Kubernetes: просмотр логов контейнеров через kubectl logs, работа с мульти-контейнерными Pod'ами и стриминг логов в реальном времени

---

<details>
<summary><b>🐳Логирование в Docker</b></summary>

---

## Простое приложение для генерации логов

Для курса было изготовлено простое приложение, которое:
- Создает случайные события
- Отправляет их на стандартный вывод
- Имитирует веб-сервер

Посмотри на github `rotoro-cloud`, если тебе интересно.

---

## Запуск контейнера

Мы запускаем Docker-контейнер под названием `log-generator`:

```bash
docker run log-generator
```

Приложение сразу начинает создавать вывод событий:
- Создавая видимость логов веб-сервера
- Передает их на стандартный вывод

---

## Стандартный вывод в контейнерах

Если ты не в курсе, то передача логов на стандартный вывод — это общепринятая практика в контейнерном мире.

Почему:
- Контейнеры должны отправлять логи на `stdout` и `stderr`
- Это позволяет Docker и Kubernetes собирать и управлять логами
- Не нужно настраивать отдельные файлы логов

---

## Просмотр логов в Docker

Если я запущу контейнер Docker в фоновом режиме, в detached mode с параметром `-d`:

```bash
docker run -d log-generator
```

Я не увижу эти логи.

Если требуется просмотреть журнал логов, то мы используем команду:

```bash
docker logs <container-id>
```

Параметр `-f` дает возможность наблюдать за логами контейнера в реальном времени:

```bash
docker logs -f <container-id>
```

---

</details>

<details>
<summary><b>☸️Логирование в Kubernetes</b></summary>

---

## Создание Pod с приложением

Теперь вернемся в Kubernetes.

Мы создаем Pod с тем же Docker-образом, используя файл определения:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: log-generator-pod
spec:
  containers:
  - name: log-generator
    image: log-generator:latest
```

Создание Pod'а:

```bash
kubectl create -f pod-definition.yaml
```

Или:

```bash
kubectl apply -f pod-definition.yaml
```

---

## Просмотр логов Pod'а

Когда Pod запущен, мы можем просмотреть его логи с помощью команды:

```bash
kubectl logs <pod-name>
```

Пример:

```bash
kubectl logs log-generator-pod
```

Вывод покажет логи контейнера, работающего внутри Pod'а.

---

## Стриминг логов в реальном времени

Используя такой же параметр `-f`, мы получим стрим логов из Pod'а:

```bash
kubectl logs -f log-generator-pod
```

То есть сможем наблюдать их в реальном времени, также, как и с командой `docker logs` в прошлый раз.

Для остановки стриминга нажми `Ctrl+C`.

---

## Важное замечание

Обрати внимание:
- Эти логи относятся к контейнеру, работающему внутри Pod'а
- В Kubernetes Pod может содержать несколько контейнеров

---

</details>

<details>
<summary><b>📦Мульти-контейнерные Pod'ы</b></summary>

---

## Pod с несколькими контейнерами

Как мы узнали ранее, в Kubernetes Pod может содержать несколько контейнеров.

Я изменю файл определения Pod'а, чтобы задействовать дополнительный контейнер:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: log-generator-pod
spec:
  containers:
  - name: log-generator
    image: log-generator:latest
  - name: image-watermark-processor
    image: watermark-processor:latest
```

Пересоздадим Pod:

```bash
kubectl delete pod log-generator-pod
kubectl create -f pod-definition.yaml
```

---

## Проблема: какой контейнер?

Если сейчас мы запустим команду `kubectl logs` с именем Pod'а:

```bash
kubectl logs log-generator-pod
```

Журнал логов какого контейнера будет отображаться?

Если у нас мульти-контейнерный Pod, мы должны явно указать имя контейнера в команде.

В противном случае:
- Команда не сработает
- `kubectl` попросит тебя указать имя контейнера

Ошибка будет выглядеть так:

```
Error from server (BadRequest): a container name must be specified for pod log-generator-pod, choose one of: [log-generator image-watermark-processor]
```

---

## Решение: указание контейнера

Чтобы указать контейнер, используй флаг `-c` или `--container`:

```bash
kubectl logs -f log-generator-pod -c log-generator
```

Или:

```bash
kubectl logs -f log-generator-pod --container=log-generator
```

Теперь мы получим сообщения журнала из указанного контейнера.

---

## Пример команды

В этом случае я напишу команду:

```bash
kubectl logs -f log-generator-pod -c log-generator
```

Где:
- `-f` — стриминг в реальном времени
- `log-generator-pod` — имя Pod'а
- `-c log-generator` — имя контейнера

Теперь мы получим сообщения журнала из контейнера `log-generator`.

---

## Просмотр логов всех контейнеров

Если нужно просмотреть логи всех контейнеров в Pod'е:

```bash
kubectl logs log-generator-pod --all-containers=true
```

Или:

```bash
kubectl logs log-generator-pod --all-containers
```

---

</details>

<details>
<summary><b>📝Дополнительные параметры kubectl logs</b></summary>

---

## Просмотр логов из предыдущего контейнера

Если контейнер был перезапущен, можно посмотреть логи из предыдущего экземпляра:

```bash
kubectl logs log-generator-pod --previous
```

Или с указанием контейнера:

```bash
kubectl logs log-generator-pod -c log-generator --previous
```

---

## Ограничение количества строк

Показать только последние N строк:

```bash
kubectl logs log-generator-pod --tail=100
```

Показать логи с определенного времени:

```bash
kubectl logs log-generator-pod --since=1h
kubectl logs log-generator-pod --since=30m
```

---

## Просмотр логов из нескольких Pod'ов

Если у тебя есть несколько Pod'ов с одинаковым лейблом:

```bash
kubectl logs -l app=log-generator
```

Или:

```bash
kubectl logs -f -l app=log-generator
```

---

</details>

<details>
<summary><b>💡Важные замечания</b></summary>

---

## Для разработчика приложения

Это простая функция ведения логов, реализованная в Kubernetes.

И это все, что действительно нужно знать разработчику приложения, чтобы начать работу с Kubernetes:

- ✅ Использовать `kubectl logs` для просмотра логов
- ✅ Отправлять логи на стандартный вывод (`stdout`/`stderr`)
- ✅ Указывать контейнер в мульти-контейнерных Pod'ах

---

## Для администратора Kubernetes

Но это далеко не все, что нужно знать администратору Kubernetes об этом.

Администратору нужно знать:
- ✅ Централизованное логирование
- ✅ Агрегация логов из множества Pod'ов
- ✅ Долгосрочное хранение логов
- ✅ Поиск и анализ логов
- ✅ Интеграция с системами мониторинга
- ✅ Решения типа ELK Stack, Loki, Fluentd

---

## В рамках сертификации

Тем не менее, в рамках программы сертификации это все знания, которые требуются в домене `monitoring`.

Для экзамена достаточно знать:
- ✅ Команду `kubectl logs`
- ✅ Параметр `-f` для стриминга
- ✅ Указание контейнера в мульти-контейнерных Pod'ах

---

</details>

<details>
<summary><b>🔮Observability в Kubernetes</b></summary>

---

## Широкая тема

Тема `Observability` в Kubernetes очень широкая.

Подобно тому, как мы сравнивали Kubernetes с конструктором:
- Решения мониторинга — это как покупка набора новых деталей
- По объему чуть ли не равная самому конструктору

---

## Сложность мониторинга

Из-за обилия объектов в кластере и их динамичности, мониторинг в Kubernetes может поражать причудливым сплетением компонентов.

Причины сложности:
- ✅ Множество объектов (Pods, Deployments, Services, и т.д.)
- ✅ Динамичность (Pod'ы создаются и удаляются)
- ✅ Множество источников данных (метрики, логи, трейсы)
- ✅ Разные решения для разных задач

---

## Дополнительные курсы

**Надеюсь, я смогу рассказать об этом в курсе `Prometheus - мониторинг и логирование в Kubernetes`.**

**Там будет рассмотрено:**
- ✅ Prometheus для сбора метрик
- ✅ Grafana для визуализации
- ✅ Централизованное логирование
- ✅ Алертинг
- ✅ Полная observability stack

---

</details>

---

## Резюме

✅ **Логирование в Docker:**
- Приложения отправляют логи на стандартный вывод (`stdout`/`stderr`)
- Команда `docker logs <container-id>` для просмотра логов
- Параметр `-f` для стриминга в реальном времени

✅ **Логирование в Kubernetes:**
- Команда `kubectl logs <pod-name>` для просмотра логов Pod'а
- Параметр `-f` для стриминга логов в реальном времени
- Логи относятся к контейнеру, работающему внутри Pod'а

✅ **Мульти-контейнерные Pod'ы:**
- В Kubernetes Pod может содержать несколько контейнеров
- Нужно явно указать имя контейнера: `kubectl logs <pod-name> -c <container-name>`
- Без указания контейнера команда не сработает

✅ **Дополнительные параметры:**
- `--previous` — логи из предыдущего экземпляра контейнера
- `--tail=N` — последние N строк
- `--since=1h` — логи с определенного времени
- `-l app=name` — логи из Pod'ов с определенным лейблом

✅ **Для разработчика:**
- Это все, что нужно знать для начала работы с Kubernetes
- Простая функция ведения логов

✅ **Для администратора:**
- Нужно знать гораздо больше о централизованном логировании
- Агрегация, хранение, анализ логов
- Интеграция с системами мониторинга

✅ **В рамках сертификации:**
- Это все знания, которые требуются в домене `monitoring`
- Достаточно знать `kubectl logs` и его основные параметры

✅ **Observability:**
- Очень широкая тема в Kubernetes
- Может быть сложной из-за динамичности кластера
- Требует дополнительных решений (Prometheus, ELK Stack, и т.д.)

> 💡 **Совет:** Для разработчика достаточно знать `kubectl logs` и отправлять логи на стандартный вывод. Для администратора нужно изучить централизованное логирование и решения типа ELK Stack или Loki. В рамках сертификации достаточно базовых знаний о `kubectl logs`.

---

